# Roadmap implementation evidence

> Hatifect — единственная platform identity. Любые прежние Q01/Q02 evidence относятся
> только к указанному в них candidate; для нового кандидата требуется повторная
> canonical/package/runtime проверка.

## U03-b active reload and U04 host environment checkpoint

Implementation [`902d834`](https://github.com/ihatectf/Hatifect/commit/902d83488e501e971ddc24f0fcd852f9068899df), common merge `db09ede`, final alpha46 source [`0cd356f`](https://github.com/ihatectf/Hatifect/commit/0cd356f0fd252a0744fe6a871e46ba631193f876). Own exact-candidate C **PASS1528 .NET +366 Python**, G **PASS1801 +366**, P **PASS90**; all48 source/version hashes and actual outputs independently reviewed. Fresh native reload **PASS18** (`57ee8df6`) and environment **PASS25** (`26d7b6c8`) share fingerprint `a5eed91fe33c86c54c7cfda90d796f4aa40934c6da5edd1760ef606fae06bc06`; process0, options restored, all8 package/producer/game DLLs byte-identical. [Full commands, regressions, review and limitations](U03_U04_HOST_ACCEPTANCE.md) preserve earlier pre-version identities. Common publication is coordinated by GQ. Full U03/U04 remain **IN_PROGRESS**. Next UI slice: actual A→title→B save-switch with pending actions, then concrete typed consumer/messages and remaining environment/planner/consumer acceptance.

Date: 2026-09-06. Working branch: `feature/flowline-next-ten-slices`; source baseline: `2f08a4f4db36ef0b1ba58e385fc0ab5044cae867`.

The authoritative scope is [ROADMAP.md](ROADMAP.md), restored verbatim from commit `1e741ef` because this task branch predates that documentation commit. The earlier exploratory Flow multiplayer/adapters/policies list is superseded as an execution plan; no new product work outside the approved roadmap is being added. It does not replace U01–U10, R01–R04, T01–T03 or their acceptance. The Flow foundation is committed and published in task branch as `cdf9d2e86e90f0174251243147f8a2f9480d14e8`; its integration with the verified UI SDK/editor/D01 is owned by the UI task. The full roadmap IDs remain incomplete until all their acceptance and integration evidence are present.

This report preserves separate evidence for Flow foundation `cdf9d2e`, F11 `e31c1ef`/`ff43d0b`, F16-a `54c5af5`, F16-b `51eee23`, and the combined UI foundation. Flow artifact paths belong to `${HOME}/Developer/Hatifect`; the UI integration section names its own worktree. Earlier checks do not certify a later combined candidate. Shared B01/B02 statuses and D01 are authoritative in [ROADMAP.md](ROADMAP.md) and [SEMANTIC_V2.md](SEMANTIC_V2.md).

## Flow owner implementation status

| IDs | State | Authoritative implementation/evidence | Remaining acceptance |
|---|---|---|---|
| B01 | DONE | Integrated audit `de766c5`; authoritative baseline and C evidence in ROADMAP.md / ROADMAP_BASELINE.md | No remaining baseline acceptance |
| B02 | DONE | Integrated D01 `1fd9d57`; SEMANTIC_V2.md and its C evidence recorded in ROADMAP.md | U01 implementation remains separate |
| U01 | DONE | Integrated `856ff52`: typed graph, independent IDs/aliases/labels and v1/v2 wire; complete U01 evidence below supersedes historical gaps | No remaining U01 acceptance; later UI/runtime stages stay separate |
| U02 | DONE | Atomic publication/CA/Parcel/Network, typed deltas/reset/history, current focus/scroll/rendering and bounded Update preparation; alpha.36 `356e52e`, U/C/G/P/PERF below | Structural/cold Reset remains O(N), Update root copy O(N/128); large Reset cost is explicitly measured, not a steady-frame budget PASS |
| U03 | IN_PROGRESS | Typed action foundation and Terminal generations; alpha.46 common reload/environment acceptance PASS on 61e8a94, published as eb193b7 with exact CI success below | Actual action save-switch, concrete typed consumer/messages and remaining full RUNTIME acceptance are required |
| U04 | IN_PROGRESS | Published alpha.45 foundation/capture; alpha.46 common host acceptance PASS on 61e8a94 with package/native evidence below | Coherent consumer locale projection, remaining environment/planner and representative performance acceptance are required |
| U05–U10 | PLANNED | Layout/input/virtualization and form validation already exist | Specified runtime/authoring/theme/component/extension acceptance remains required |
| R01 | IN_PROGRESS | Resource thread-affinity candidate; [targeted evidence](R01_RESOURCE_OWNERSHIP.md) | Full generation ownership manifest and remaining criteria |
| R02–R04 | PLANNED | Existing single-asset Last Known Good and retryable resource teardown | Bundle preparation, migration and multi-host commit/rollback |
| T01–T03 | PLANNED | Existing Tooling/Server, symbols/diagnostics and DevTools | New metadata/protocol, preview matrix, editor workflow and examples |
| F11 | DONE | Integrated `5f2d10c`: immutable snapshots, typed commands/session/revision, observer fencing, provider capabilities and stable availability reasons; combined C/G and fresh runtime evidence below | F12 after U01/U02/U04; F16 continues independently |
| F12–F13 | IN_PROGRESS | ParcelExperience/ParcelSurface, typed controls and localized projections; host-free behavior tests | New semantic-v2 integration; complete fake-session in-game lifecycle/input evidence |
| F14–F17 | DONE | D03/D04, SaveBoundCargoPort and FlowGameSession; supported F16 failure matrix plus F17-a production A→B→A and F17-b actual bounded performance below | Checked Flow candidate complete; subsequent combined UI candidates retain their own integration/acceptance gates |
| F18–F19 | IN_PROGRESS | NetworkExperience: stations/routes, inventory fingerprints, shipping/history, return/recovery | Ordinary player entry without console, Quick/View integration, reason codes, locale/input/scale acceptance |
| F20 | DONE | F20-a `6d0ac14` and F20-b `a0520d2`: bounded diagnostics, actual routing/dispatch/save/restore profile, resources PASS12 and PERF PASS9; D06 chooses bounded retention | No remaining F20 acceptance for the chosen provider; combined candidates keep their own evidence and player-facing limits follow F18/F19 |
| I01 | IN_PROGRESS | CA continues consuming exact UI packages; prior alpha.30 isolated build passes | Migration to the new semantic/authoring contract and current runtime/visual acceptance |
| Q01–Q03 | IN_PROGRESS | Prior baseline package and game results retained separately | Full specified modernized scope, current fingerprints, matrix, performance/editor evidence and final archive |

## Completed bounded checks, not stage completion

- `./tools/hatifect-test ui --platform`: **PASS**, 277 tests, including 7 new platform screen/keyboard ownership cases. Evidence: `artifacts/validation/run-50gt4vku/summary.json` and TRX. Independent reviewer found and then rechecked the native-menu handoff fix; no open findings in that bounded UI diff. Actual split-screen visual/input acceptance remains unperformed.
- `./tools/hatifect-test flow --platform`: **PASS**, 634 Flow tests + 61 game adapter tests. Evidence: `artifacts/validation/run-nu3kvz6n/summary.json` and TRX. Added mutex/fingerprint admission, peer host lost-reply idempotency, per-peer target isolation, save barrier/disconnect and fault fencing. Newtonsoft wire roundtrip is exercised by the peer host test.
- Earlier single-player candidate: production `flow.chest.roundtrip` request `c64b077c-986a-450e-87a8-8e57a7d0c9f6` passed 8 checks, 347 update frames, 3 loads and 2 Saving/Saved pairs. Full item fidelity and absence of duplicate delivery were observed. Exact earlier candidate evidence is in `.testagent/ten-slices/status.md` and `artifacts/flowline-ten-slices/README.md`; it does not certify later edits.
- `./tools/hatifect-check`: **PASS**, 904 .NET + 297 Python tests, `artifacts/validation/run-lx8bdso7/summary.json`.
- `./tools/hatifect-check --platform`: **PASS**, 1,051 .NET + 297 Python tests, `artifacts/validation/run-91tslfgt/summary.json`.
- `./tools/hatifect-isolated-ui-ca`: **PASS**, 42 projected files, 8 alpha.30 packages, 79 tests, 2 CA DLLs with UI sources absent. Canonical command exit 0 and terminal summary confirmed these counts; its temporary projection/TRX was automatically cleaned. The platform run above separately retains the CA TRX.
- Final peer endpoint review found a transient authority loss incorrectly persisted as recovery when capturing a target. Fixed by the existing live application availability gate; unexpected adapter failures still fence. `./tools/hatifect-test flow --platform`: **PASS**, 634 Flow + 63 Stardew tests, `artifacts/validation/run-1bybxd3f/summary.json`. This is the only code delta after the full checks above. Both before/after snapshot publication cases verify rejection, no persisted recovery, retained replay and successful new sequence after authority returns.
- Initial C check `run-_ze48r98` found two Python source-contract assertions referring to the previous teardown location/signature; updated assertions verify the actual owning event binding and retry state. A later build `run-0e14ytla` stalled in MSBuild GetTargetFrameworks; the exact owned process was diagnosed, terminated and the canonical full checks above passed. The first new authority test run `run-ptlwzwse` reached the final metadata assertion but failed because the Net container enumerable is not its dictionary; corrected the assertion to the station key and reran successfully. No production test was removed or weakened.

## Current foundation runtime and packaging

- `./tools/hatifect-ui-test semantic.lifecycle`: **PASS**, request `293e82f4-f390-4361-a7d5-e41537648de9`; three lifecycle/focus/tree checks, isolated process exit 0. Screenshot inspected: diagnostics/inspector controls and focused diagnostics are present. This is lifecycle acceptance only, not the complete UI visual/input/locale/scale matrix.
- `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS**, request `2b32ce8c-508d-497a-8652-5d9b44e65826`; all 8 checks, 347 frames, 3 loads, 2 Saving + 2 Saved, no errors. Flow runtime fingerprint: `1071d92126a168395176a214be00d7b12200a044a1b9b43990fb04b4e43d779a`. Real chest custody, item fidelity, save/reload and no duplicate delivery verified through the production session.
- Fresh isolated deployment: `artifacts/validation/run-m55a6p6g/summary.json`; packaging tests intentionally skipped because the separate gates above are authoritative. No RC promotion is claimed.
- `python3 tools/release.py verify-package .smapi-test/isolated/Mods/Hatifect`: **PASS**, 14 runtime DLLs. Generated acceptance reports were byte-matched to preserved request artifacts before deleting only those generated files. Archive and DLL hashes: `artifacts/flowline-foundation/`.
- Independent bounded review: UI screen/keyboard handoff fixes and peer endpoint authority/replay fixes rechecked; no remaining findings in the reviewed scopes. Multiplayer client/wiring and split-screen runtime remain unimplemented/unverified and disabled.

## F11-a: revision subscription and projection lifecycle

Owner: Flow semantic consumer; existing application event/snapshot contract and public UI API are unchanged. Both network and parcel views subscribe before their initial read, retain notifications raised during read/projection for the next pump, and reject nested pumps before they can consume dirty state. Failed reads remain retryable; failed construction detaches the subscription; a failed ordinary unsubscribe retires callbacks/actions immediately and retains a handle for retry.

- `./tools/hatifect-test flow`: **PASS**, 646 tests (`run-seoy49ou`), including 12 new cases for both projections.
- Final `./tools/hatifect-check`: **PASS**, 916 .NET + 297 Python tests (`run-3akheh8x`), including the final reentrant-pump guard.
- Independent source/actual TRX review rechecked the subscribe/read and reentrant-read counterexample. Existing snapshot/session/observer tests remain intact. No new UI API, persistence format, game adapter or package boundary is introduced, so separate game/visual/package gates are NOT_APPLICABLE to this bounded F11-a change. Full F11 and later runtime acceptance remain required.
- Limit: a provider whose event removal keeps throwing during a constructor failure cannot provide a usable cleanup handle; this does not imply unconditional cleanup against an arbitrarily broken event implementation. Production Flow application event accessors are ordinary managed events.

## Current next slice

F11 and the chosen F14–F17 provider are complete with the separately attributed evidence below. F20 implementation `a0520d2` and evidence `db7acd6` are published on the Flow task branch; the resource profile closes D06 with bounded retention. Combined develop integration remains with the UI owner, who also owns U01–U10/R/T. Published U02 `356e52e` / alpha.36 is available in develop `54e14e7`; this Flow source candidate still uses its separately tested alpha.35 baseline. F12 implementation waits for completed U04, then F13 requires completed U03/F12 and F18/F19 require U07 and the earlier Flow UI stages. [F12 readiness and acceptance](FLOW_UI_READINESS.md) identifies the remaining consumer states and actual UI lifecycle evidence. No new stage is declared complete by this readiness assessment. Multiplayer remains disabled.

## F11-b: owner-derived availability and rejection reasons

Owner: Flow Core/Application and Dispatch. The D01 contract supplies provider mode, supported operations and an immutable five-action availability set with stable rejection codes/reason keys. Reservation projection and execution share route, queue and capacity admission. Semantic consumers localize those reasons in EN/RU and explicitly identify the diagnostic provider. Game sessions and the disabled peer wire preserve the metadata; terminal peer projections release their previous live contents. No persistence envelope or physical inventory/custody format changes.

- `./tools/hatifect-check --platform`: **PASS**, 1,076 .NET + 297 Python tests (`run-ub2txdqz`).
- After retaining the original public constructors and deconstruction signatures, `./tools/hatifect-check`: **PASS**, 927 .NET + 297 Python tests (`run-vh75lnho`).
- Final `./tools/hatifect-test flow --platform`: **PASS**, 657 Flow + 65 Stardew tests (`run-t2mgi20d`), including terminal peer disposal and actual parcel binding-context creation. These are the bounded code deltas after the full platform check.
- Added 11 `FlowAvailabilityTests` cases, serializer/forged-wire coverage in `PeerProtocolTests`, and terminal cleanup coverage in `PeerHostTests`. Existing tests remain intact. Tests cover admission/rejection agreement, stale session/revision, immutable cached reads, provider fencing, EN/RU reasons, legacy constructors and STJ/Newtonsoft roundtrips.
- Binary compatibility: an executable compiled against the foundation Core DLL successfully invoked both original constructors and both generated `Deconstruct` methods after replacing that DLL with the current build, without recompiling the consumer. Evidence: `artifacts/flowline-f11/binary-compatibility.json` and `binary-consumer.cs`.
- Independent review identified and rechecked the constructor compatibility and terminal projection issues. Final source/diff review found no remaining issue in this bounded contract change. Separate physical runtime acceptance remains recorded against the foundation; it is not relabelled as F11 candidate evidence.
- U01 dependency remains explicit: current UI binding regenerates identity from labels, so the full NetworkExperience binding/compilation path with multiword localized labels is still invalid. The UI owner is fixing that shared path. The new parcel availability element uses a valid existing binding label and is tested through `CreateBindingContext`. F12/F18 UI/runtime acceptance is still incomplete.

## F16-a: selected stack quantity

Owner: game-save adapter and physical chest access. `FlowSendCommand.Quantity` is additive and nullable; the original constructor/deconstruction and whole-stack behavior remain. The source fingerprint and quantity are checked under the lease, selected cargo XML is prepared without changing the source quantity, and extraction performs one slot replacement with an untagged remainder. Version 2 of the game aggregate retains the complete source quantity; version 1 whole-stack saves migrate explicitly. Core custody transitions, checkpoint and receipt formats remain unchanged.

- `./tools/hatifect-check --platform`: **PASS**, 1,107 .NET + 297 Python tests (`run-o7zrqs5e`), including 659 Flow and 92 Stardew cases.
- `./tools/hatifect-check`: **PASS**, 929 .NET + 297 Python tests (`run-ogcutlsc`).
- Added 24 `PartialStackTests` cases, 2 EN/RU quantity form cases, 2 quantity wire cases and a partial quantity variant of the existing stale-on-mutex test. Exact post-write checks distinguish a harmless throwing observer from mutation of the same remainder object; unknown outcomes survive reload without physical replay. Tagging failures retain a saveable fenced aggregate, including when no transfer exists to reconcile.
- Independent reviewer found no actionable regressions in the bounded quantity/custody/migration/consumer diff and independently confirmed the actual final Flow/Stardew TRX, including both tagging-observer cases and legacy JSON without the new field.
- Fresh isolated deployment: `run-lj6ytpwy`. Packaging tests are explicitly SKIPPED by live preparation; separate checks above are the test evidence, and no RC promotion is claimed.
- `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS**, request `d60b0e0c-6d5f-4560-b07c-37bf28b1c906`, 8 checks, 347 frames, 3 loads, 2 actual Saving/Saved pairs, process exit 0 and no errors. Whole8 plus partial13→5+8 preserved total21, the exact untagged source remainder and delivered item fidelity; repeated delivery was rejected. Flow runtime fingerprint: `4b56cbc49bfcdfae5332e67e0db9818aa0707a64fdb4dafc177603a843d356c9`.
- `python3 tools/release.py verify-package .smapi-test/isolated/Mods/Hatifect`: **PASS**. The generated acceptance report was byte-matched to the retained runtime artifact before removing only that generated report. Current package inventory/archive are in `artifacts/flowline-f16/`.
- At F16-a the executor launched one process and cleaned its owned save on exit. F16-b adds the bounded continuation below. Full process-crash and production A→B→A acceptance remain incomplete.

## Combined UI SDK/editor and Flow foundation integration

The integration branch `feature/roadmap-implementation` combines `31aaa0b` with Flow foundation `cdf9d2e`, using UI package version `1.0.0-alpha.31`. The following evidence belongs to `/private/tmp/hatifect-ui-next-ten-slices`, independently from the historical Flow checks above.

- Preserved the typed form factories, committed/draft state, optional standalone/Terminal/HUD host API, themes, textures, live asset reload and editor functionality. Added the Flow external validation source and explicit element-ID overloads without changing the frozen `IUiSemanticSurfaceApi` v1 source (SHA-256 `9de29d5a7c53964ae192efb4f4f7590e1dc75a64d2fe94605f86dac3861c6335`). Both consumers require the exact combined package version through the shared package authority.
- Current-draft `IsValid` now shares validation semantics with `Apply` without publishing errors or committing values. Screen guards cover the SDK hosts as well as overlays. Foreign disposal makes a native menu inert immediately and retires only its own slot when the owner screen resumes. Failed watcher subscriptions and teardown retain retryable cleanup.
- `./tools/hatifect-test ui --platform`: **PASS**, 370 tests, `artifacts/validation/run-33413xw6/summary.json`. Six new cases exercise draft validation, mutation/reentrancy, foreign slot retirement/replacement and partial watcher subscription/removal failures. Independent read-only review rechecked the actual native menu guards and all three fixes; no open findings in that scope. Helper tests do not establish actual split-screen visual/input acceptance.
- `./tools/hatifect-check`: **PASS**, 991 .NET + 301 Python, `artifacts/validation/run-r03g7g_3/summary.json`.
- `./tools/hatifect-check --platform`: **PASS**, 1,147 .NET + 301 Python, `artifacts/validation/run-vqyw7gtd/summary.json`.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS**, 43 projected files, eight freshly built alpha.31 packages, 80 tests, two CA DLLs and no UI source. Projection and TRX retained at `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.wyewblmo`.
- Earlier `run-i5fgi7bx` failed on the fixed release version assertion; the assertion now requires alpha.31. `run-z_79vs4k` stalled under Rosetta in native process creation; its owned process was sampled and terminated, and that run remains **FAIL**. The successful gates above were new executions after the review fixes.

U01 remains **IN_PROGRESS**: explicit builder IDs are not yet preserved by compiler binding, and localized multiword Flow Network labels still fail `CreateBindingContext`. The next U01 acceptance must exercise the real `NetworkExperience` through binding and compilation. Combined runtime evidence is recorded below; diagnostics lifecycle smoke does not prove that Network screen works. No additional roadmap ID is marked DONE by this integration.

### Native menu callback correction after merge 6b381c0

The first combined `semantic.lifecycle` request `9ff9a650-5342-47f2-bf51-87badd02e018` exposed a real teardown defect before publication. The native log reported a Rosetta synchronous-exception error and the sampled stack repeated two frames. Inspection of the installed game IL established the cause: `Game1.set_activeClickableMenu` calls the previous menu's `IDisposable.Dispose` before storing the replacement. Retiring the slot from inside that callback recursively called the same setter. Canonical cancellation preserved the request as **BLOCKED** (exit 130); the discovered implementation defect is **FAIL**, not an environment-only failure.

`Retire` now only marks the menu inert. Owner-screen callbacks perform deferred exact-instance cleanup under a reentrancy guard with retry after a failed write. `./tools/hatifect-test ui --platform`: **PASS**, 372 tests, `artifacts/validation/run-jxmzuv1b/summary.json`. Two new tests model disposal before native slot assignment and recursive/failed deferred clear. Independent review verified the fix against `artifacts/ui-flow-integration/game-menu-setter.il.txt` and the real `exitThisMenu` ordering. The initial review covered helpers and wiring but missed the setter callback; the corrected review does not substitute for a new game run.

The corrected candidate passed `./tools/hatifect-check --platform`: **PASS**, 1,149 .NET + 301 Python, `artifacts/validation/run-icy5beoe/summary.json`. Fresh `./tools/hatifect-isolated-ui-ca --keep`: **PASS**, eight alpha.31 packages, 43 files, 80 tests and two CA DLLs without UI sources; retained projection `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.lbrr_99z`, complete log `artifacts/ui-flow-integration/final-isolation.log`. The correction changes only private Stardew menu ownership and its regressions; host-free code and the previously checked API contract are unchanged.

### Fresh combined runtime, candidate 803c909

- `./tools/hatifect-ui-test semantic.lifecycle`: **PASS**, request `d95c475d-3c45-417d-a72e-3093e1ef45ee`, all three checks and no exceptions. The newly built isolated candidate rendered its diagnostics surface, held one focused node, and closed/reopened with one owner. The screenshot was inspected: Diagnostics/Inspector controls and the focus outline are visible. This validates the lifecycle regression; it does not cover the full locale/input/scale or split-screen matrix.
- `./tools/hatifect-smoke save.bootstrap`: **PASS**, request `5146ce6c-2feb-451b-a196-d6520b35b466`. Stardew created and reload-verified a synthetic fixture in this worktree's isolated save root. Real user saves were not imported.
- `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS**, request `46967acd-30bb-4c93-89ff-ce9c1b95c9e3`, all eight checks, 347 frames, three loads, two Saving/Saved pairs and no errors. The production session retained real chest cargo across saving/loading and did not duplicate delivery. Evidence is under `artifacts/runtime/<request-id>/result.json`; exact counts are in `diagnostics/flow-chest-roundtrip.json`.
- CA's actual third-party mod was absent in this isolated run and the adapter disabled itself normally. These scenarios do not claim CA visual/runtime acceptance. Q01–Q03 and the known U01 Network binding gap remain open.

F11 integration is recorded below; U01 preserves explicit identity/alias/label through the real Network binding/compile path and adds the complete typed graph/wire acceptance. The UI task remains the single `develop` integration owner.

## Flow task integration of UI foundation with F16-a

The Flow task merged published UI foundation `3f11e5d` into its published F16-a `54c5af5`. Only the status document conflicted; all source/code bytes from the incoming UI change match that reviewed upstream commit, and both owners' evidence sections remain. This integration does not replace either runtime candidate's identity.

- `./tools/hatifect-check --platform`: **PASS**, 1,203 .NET + 301 Python, `artifacts/validation/run-v86kdbsx/summary.json` (659 Flow + 92 Stardew).
- `./tools/hatifect-check`: **PASS**, 1,016 .NET + 301 Python, `artifacts/validation/run-5itzrxbq/summary.json`.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS**, 43 projected files, eight alpha.31 packages, 80 tests, two CA DLLs and no UI source. Retained projection/TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.l_zpwa5h`.
- Final source/staged review: `artifacts/flowline-ui-integration/`. Runtime evidence for the full next candidate will be generated by the next F16 crash slice; earlier normal roundtrip evidence remains scoped to its recorded fingerprint. No release or new UI acceptance is claimed by this task-branch integration.

## F11 combined integration checks

Combined with the published UI foundation `3f11e5d` in `/private/tmp/hatifect-ui-next-ten-slices`: `./tools/hatifect-check` **PASS**, 1,014 .NET + 301 Python (`artifacts/validation/run-9ryxhfgj/summary.json`); `./tools/hatifect-check --platform` **PASS**, 1,174 .NET + 301 Python (`artifacts/validation/run-wxasxwxo/summary.json`), including 657 Flow and 65 Flow game-adapter tests. The final committed Flow source was independently inspected before integration; the old public constructor/deconstruction signatures and current SDK form behavior remain compatible. No UI package/API or persistence format changed in this F11 delta. Fresh `./tools/hatifect-live-prepare` completed for integration commit `5f2d10c` (`artifacts/ui-flow-integration/f11-prepare.log`), followed by `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS**, all 8 assertions, 347 frames, 3 loads, two Saving/Saved pairs and no errors or exceptions (`artifacts/runtime/7336456e-5400-4a77-aed6-4109aedc7c9f/result.json`). F11 acceptance is **DONE**; Network UI binding, F12 and the broader physical failure matrix remain separate open work. Previous physical runs remain attached to `803c909`.


## U01: typed semantic graph and lossless binding

**DONE**, implementation commit `856ff52`. This section supersedes the historical U01 gaps above; those remain attached to their earlier candidates.

Owner: Semantics/Experience with Tooling and existing Runtime/Stardew hosts; worktree `/private/tmp/hatifect-ui-next-ten-slices`, based on published `22a6441`. UI packages advance together to `1.0.0-alpha.32`; no assembly or third-party dependency is added. The frozen surface API v1 and existing form contract remain source-identical. Builder additions and their compatibility were reviewed before refreshing its source fingerprint.

The implementation separates stable identity, authoring alias and localized label, validates nominal CLR/nullability contracts, and binds all seven relation kinds with typed projection input slots. Invalid endpoints, duplicate producers, unsupported capabilities, type/nullability mismatch, invalid provenance and data cycles produce addressed diagnostics before activation. Failed Build can be retried without subscribing or sealing the builder. Collections validate actual item payload nullability. Typed action metadata must reference the actual authored action instance.

Graph structure is copied and immutable. Auxiliary sources are observed without generating widgets; runtime roles use alias while displayed names use label. Flow Network now binds and compiles in EN/RU with its actual immutable read model, collection/selection/details and query/filter graph. CA has two distinct filter inputs, two owner selection relations and Open/Favorite targets, verified through the public exact-package wire API. The remaining passive opaque CA Mode presentation is explicitly bounded to U07/I01 migration; related-state notifications still await U02 atomic publication.

Schema v2 preserves graph IDs, aliases/labels, descriptors, capabilities, slots, relations, mapping, provenance and presented membership. Canonical legacy contexts retain byte-identical strict schema-v1 export/import, including previously accepted raw names. The server advertises both versions, accepts mixed-version contexts, counts auxiliary graph symbols against existing budgets, and retains previous binding/document snapshots on rejected refresh. Descriptor validation/equality/hash share visited branches; depth 16 and 65,536 expanded wire entries bound descriptor work.

- `./tools/hatifect-check`: **PASS**, 1,097 .NET + 301 Python, `artifacts/validation/run-kws__hw_/summary.json`, including seven invalid-provenance regressions from final review.
- `./tools/hatifect-test ui`: **PASS**, 439 tests, `artifacts/validation/run-qp12e9yj/summary.json`, including the final positive provenance roundtrip assertions added after C.
- `./tools/hatifect-check --platform`: **PASS**, 1,251 .NET + 301 Python, `artifacts/validation/run-wuk1tkev/summary.json`, including 81 CA, 658 Flow, 65 Flow Stardew and 15 UI Stardew tests. The only subsequent production delta is the transport-neutral provenance guard, verified by the final C gate above; no host/game behavior changed after this platform run.
- Final `./tools/hatifect-isolated-ui-ca --keep`: **PASS**, 43 projected files, eight freshly packed alpha.32 packages, 81 tests and two CA DLLs without UI sources. Retained projection and actual TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.yhqkyb47`. The preceding `hatifect-ui-ca-isolated.x9g001r0` also passed before the provenance guard; it is not substituted for this final package verification.
- Added `SemanticGraphTests`, `TypedExperienceGraphTests`, `SemanticGraphMetadataTests`, `SemanticGraphProtocolTests`, actual Flow/CA graph fixtures, EN/RU runtime composition and the v2 framed-stdio transcript while preserving its v1 case. Tests cover negative graph imports, retry after failure, shared descriptor branches, legacy names and source/action identity. Existing tests were not removed or weakened.
- Earlier `run-d7xrcauo` failed the changed builder fingerprint, which was refreshed only after compatibility review. `run-95vjrdo5` failed one protocol retry test because its initial binding revision already equalled the retry revision; corrected the fixture to start at revision zero. These runs remain **FAIL**. Independent review found capability-name bypass, shared descriptor expansion and asymmetric provenance validation; all received implementation fixes and focused regressions.
- Final independent read-only review rechecked the fixes against current sources and actual C/U TRX: no open findings in this bounded U01 diff. Package isolation and game/visual evidence are reported separately; the reviewer did not claim to run them.

Observed presentation impact: History Browse enables the existing Gallery policy on Wide/Medium, retains List on Compact/Controller, and selects generated MasterDetail for Network. U01 does not introduce a consumer layout override. Full visual/input/locale/scale runtime acceptance remains in F12/I01/Q03; U01 runtime, visual and performance acceptance are **NOT_APPLICABLE** as separate gates, with graph work kept outside draw/layout/dispatch. The earlier F11 chest runtime is not relabelled as U01 evidence. Next UI work is U02 and U04, followed by F12 with already completed F11.
## F16-b: actual process crash after a confirmed in-flight save

Published commit: `51eee23`. Owner: fixed scenario orchestration in the live harness and production Flow diagnostics. `flow.chest.crash-after-save` uses one request-owned canonical save and exactly two fixed SMAPI launches under one timeout. After actual Saved the first process holds the existing save barrier; the executor validates the bounded marker against request/PID/save owner/saved bytes/Flow DLL fingerprint, sends SIGKILL only to its owned group and requires the observed raw exit `-9`. The second process validates the saved marker and distinct process/session identity before continuing. No intermediate save provisioning or cleanup occurs; old child IDs are retired before a failed second launch can leave stale ownership. Public Flow/UI APIs, request schema, Core/Persistence and save formats are unchanged.

- `./tools/hatifect-test flow --platform`: **PASS**, 659 Flow + 92 Stardew (`run-pkii1yop`).
- Final `./tools/hatifect-check --platform`: **PASS**, 1,203 .NET + 315 Python (`run-t3t1h105`), including the final UI host handoff condition and current exact-package CA tests.
- `./tools/hatifect-check`: **PASS**, 1,016 .NET + 315 Python (`run-pht4qkv7`). The subsequent one-condition change is in the game-only UI host, excluded from this host-free graph and covered by final G/runtime.
- Added 14 Python tests in `test_saved_crash.py`. Real subprocess tests verify SIGKILL, raw exit, owned descendants, unrelated-process survival and a distinct second process retaining the same saved bytes. Other cases cover malformed/linked/stale/foreign markers, fixture ownership, changed saved bytes, cancellation, unexpected first exits, failed second launch, public FAIL evidence and cleanup. These test executables are fixtures, distinct from the actual game result below.
- Independent review found a race where a failed prepare report could be overwritten by a successful resume report. Any terminal prepare report now stops continuation and is archived; the new regression passed in the actual 315-test run. Final bounded source review closed with no open finding.
- Fresh isolated deployment: `run-uwtjbc_f`. Packaging tests are explicitly SKIPPED by the canonical prepare command; separate C/G runs supply test evidence. No RC promotion is claimed.
- `./tools/hatifect-smoke flow.chest.crash-after-save`: **PASS**, request `628c1799-df43-4c51-8b82-b8d6c4432979`, all 9 checks, no exceptions, 28,146 ms. Prepare PID68184: signal9, raw-9, normalized137; resume PID68206: exit0; both teardown error lists empty. Three loads and two actual Saving/Saved pairs conserved whole8 and partial13→5+8 with exact remainder/item fidelity and rejected repeat delivery. Phase journals, marker and termination proof are in `artifacts/runtime/<request-id>/diagnostics/`. Flow fingerprint: `67df64afd3e51c398bb4ba8fb59708d3e55c6305c565289909e8028e23c36243`.
- Same-candidate `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS**, request `e410b0d2-8d12-4577-ad6a-09e4fe07051e`, 8 checks, 347 frames, 3 loads, two Saving/Saved pairs, PID68389 exit0 and no errors. This separately confirms ordinary lifecycle behavior after the shared-driver change.
- `python3 tools/release.py verify-package .smapi-test/isolated/Mods/Hatifect`: **PASS**, 21 files / 14 runtime DLLs. Generated reports were byte-matched to preserved request artifacts before removing only the generated copies. Inventory, archive and source/staged review are retained under `artifacts/flowline-f16-crash/`.
- Earlier attempt `b444bf17-e31c-4c1d-9357-9ce0b5469b47` is **FAIL**: the generic UI automation controller did not delegate the new Flow scenario and exited before the crash boundary. The fixed handoff is covered by the fresh runs above. Initial `run-29d150qg` failed on an incorrectly precreated test fixture journal; production overwrite protection was retained. `run-8u5pt1fz` stopped on NuGet signature-source access and `run-jfq8ex7w` on concurrent package-feed preparation; fresh sequential checks/deployment passed without disabling validation.

This closes only the bounded Saved/InTransit crash increment. Other confirmed custody states, unsaved/interrupted save boundaries, production A→B→A and full F16/F17 acceptance remain open. Separate visual and UI package-boundary changes are NOT_APPLICABLE here; UI graph/schema/version remain unchanged. The existing actual G and package-consumer checks cover the private harness handoff.


## Combined U01 and F16-a/b integration

The shared candidate combines published U01 `7c77f4c` with Flow handoff `51eee23` (including `54c5af5` partial quantities and `87ee5c1` prior combined-base verification). The conflict resolution retains quantity admission, source fingerprint and whole-stack behavior, and gives the new quantity form its explicit U01 alias `ShipmentQuantity` and Flow-owned schema. All eight UI packages remain alpha.32; both historical evidence sections are retained with their original candidate scope.

- `./tools/hatifect-check --platform`: **PASS**, 1,287 .NET + 315 Python, `artifacts/validation/run-dl08uyka/summary.json` in `/private/tmp/hatifect-ui-next-ten-slices`; 660 Flow and 92 Flow Stardew tests include EN/RU quantity validation and partial-stack custody/migration.
- `./tools/hatifect-check`: **PASS**, 1,099 .NET + 315 Python, `artifacts/validation/run-0j90sfgw/summary.json`.
- U01's final exact-package CA projection remains authoritative for the unchanged UI producer/CA sources. The newly merged Flow consumer and current CA are additionally built and tested by the combined G gate. This integration does not mark full F16 complete; the further delivery/unsaved/save-isolation matrix remains open.


Fresh isolated runtime for integration commit `43ff04d`: `./tools/hatifect-live-prepare` **PASS**, `run-ms2374do` (packaging tests intentionally SKIPPED; the separate C/G evidence above is authoritative). `./tools/hatifect-smoke flow.chest.crash-after-save` **PASS**, request `8ec34b40-9dea-45a6-907c-bea42a281243`, all 9 checks, 28,185 ms, 340 frames, three loads, two Saving/Saved pairs and no errors/exceptions. The owned prepare process PID74396 exited with actual raw `-9` after signal 9; a fresh process restored the same saved cargo and conserved partial source 13 = cargo 5 + remainder 8 without duplicate delivery. This certifies the combined alpha.32 candidate at the Saved/InTransit boundary; later F16 crash states and the full UI visual/input matrix remain separate.


## U02-a: publication, collection changes and atomic CA

**U02 remains IN_PROGRESS.** Implementation commit: `c5db895`. This checked implementation subset belongs to Experience/Runtime and the CA semantic consumer in `/private/tmp/hatifect-ui-next-ten-slices`, based on `69fd5b2`. All eight UI packages and their consumer authority advance together to alpha.33. Frozen surface API v1, builder/form source contracts, Flow domain/persistence and third-party dependencies remain unchanged.

`UiPublication` validates a complete draft and exchanges one immutable view before callbacks. Sources and snapshots expose monotonic versions; collection content/selection revisions remain distinct. Invalid preparation, stale/replayed/conflicting deltas, thread/reentry/disposal and observer failures have explicit outcomes. Collections retain at most 64 change batches of at most 128 operations; slow readers receive a current Reset. Captures preserve order, stable-ID lookup and selection without repeated copying. Runtime captures one view per publication before composition, including nested form inputs and selection facades; editing still reaches the original owner. Payload/icon/text changes advance ItemRevision while the previous measurement hash is preserved.

CA now commits mode, category, status, handoff, both collections and its backing projection together. Rendered category/storage facades send requests before mutation; failed provider or projection preparation preserves the old UI. Nested requests and retired publication owners cannot invoke the provider. No Changed callback executes a provider command. Existing category/favorite/recent/open policies and frozen public UI host remain in use.

- Final C `./tools/hatifect-check`: **PASS**, 1,153 .NET + 315 Python, `artifacts/validation/run-aqkerbik/summary.json`. This includes 493 UI and 660 Flow tests, scalar/selection snapshot versions and the final source-inspection allocation measurement.
- G `./tools/hatifect-check --platform`: **PASS**, 1,350 .NET + 315 Python, `artifacts/validation/run-s84huttc/summary.json`: 90 CA, 660 Flow, 92 Flow Stardew, 15 UI Stardew and 493 UI tests. Subsequent code only makes captured scalar/selection versions explicit and requires Version on the new collection snapshot capability; final C and freshly packed isolated P cover that transport-neutral delta.
- U `./tools/hatifect-test ui`: **PASS**, 491 tests, `run-sgh0_qh7`, before the two additional legacy-version cases covered by final C/G. Earlier foundation gates `run-yomslrgm` (459) and `run-or7x2fo1` (475) passed their then-current subsets.
- CA `./tools/hatifect-test ca --platform`: **PASS**, 89 tests, `run-tn2y9rqr`; the final direct-publication-retirement regression passed in G and P (90).
- Final P `./tools/hatifect-isolated-ui-ca --keep`: **PASS**, 44 projected files, eight alpha.33 packages, 90 tests, two CA DLLs and no UI sources. Retained projection/TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.3ty5ebjp`.
- PERF fixtures 100/1,000/10,000 items, 600 measurements each: cached capture allocates **0 bytes**; selection does not call item projection again, retains payload storage and passes unchanged 2 ms p95 / 4 ms p99 / 16 KiB budgets. Independently inspected `run-sgh0_qh7` TRX measured selection p95 0.003250/0.003583/0.003292 ms, p99 0.003458/0.021625/0.003459 ms and approximately 1,776 bytes per dispatch. Final C repeats these cases. This does not certify full Reset/delta preparation latency or actual in-game UI performance.
- New tests: CollectionSnapshotTests, PublicationTests, PublishedCollectionTests, PublicationSceneTests; old-scene/external-source guards in CollectionVirtualizationTests; capture/selection PERF cases; nine CA publication/request/lifecycle cases. Existing behavioral tests and budgets are retained.
- Independent read-only review found and rechecked shared nominal registry, pre-registration callback fencing, poisoning the whole batch after arbitrary preparation/replay equality failure, and direct CA publication retirement. All have executed regressions; final source/evidence pass has no open finding in this subset.
- Historical failures remain **FAIL**: `run-pwpzt7ie` missing test namespace; `run-qweb03be` xUnit2031 assertion syntax; `run-clavw236` 48 first-use timing-path bytes in a capture measurement. The complete measured capture path is now warmed, retaining its zero-allocation assertion. `run-w625y9lk` failed only the pre-existing tooling allocation assertion (4,600 > 4,096 bytes); moving xUnit calls outside the measurement preserves all 10,000 identity checks and the original 4,096-byte budget, and final C passes.

Runtime/visual for this bounded source/package change are **NOT_APPLICABLE**; actual CA and complete UI input/locale/scale acceptance remain in I01/Q03. Full U02 is not DONE: atomic Flow read model, removed-item focus/scroll fallback and remaining collection update measurements are next. The queued independent Flow F16 handoffs are integrated only after this checked source checkpoint, with new combined evidence.

## F16-c: confirmed delivery survives a process crash

Owner: the existing Flow diagnostic driver and fixed live-harness scenario policy. `flow.chest.crash-after-delivery` follows the normal first save/reload, delivers both parcels and saves again. Only `saved-delivered` with loads2/Saving2/Saved2 permits the owned SIGKILL; a first-save marker cannot satisfy this scenario. The new process must load a fresh session with both parcels Delivered, retain exact destination items/remainder for 120 update ticks and reject both repeat-delivery commands. The existing InTransit boundary remains separate. No public API, request/marker field set, save format or Core/Persistence change.

- `./tools/hatifect-test tools`: **PASS**, 317 Python (`run-_rwp5bay`). Added delivered phase/counter rejection and a real two-subprocess fixture for the second fixed scenario; existing first-boundary tests and manifest checks remain.
- `./tools/hatifect-check --platform`: **PASS**, 1,203 .NET +317 Python (`run-xetndbxq`), including 659 Flow, 92 Stardew and 80 exact-package CA tests. `./tools/hatifect-check`: **PASS**, 1,016 .NET +317 Python (`run-yw5xa814`).
- Fresh isolated deployment: `run-3o3qkopf`. Canonical packaging tests remain SKIPPED; the separate gates above are test evidence, without RC promotion.
- `./tools/hatifect-smoke flow.chest.crash-after-delivery`: **PASS**, request `db552b12-1f5a-4313-a3c8-95fe71728eae`, all 9 assertions, no errors/exceptions, 29,135 ms. Marker requires the second actual Saved; prepare PID70870 receives signal9/raw-9, resume PID71073 exits0, both teardown lists empty. Final diagnostics: 3 loads, 2 Saving/Saved pairs, 154 resume frames including the 120-tick quantity/fidelity/repeat-delivery check.
- Same-candidate `./tools/hatifect-smoke flow.chest.crash-after-save`: **PASS**, request `e6e12955-dbb0-4df8-95a4-1a863c4096b9`, 9 assertions, PID71439 raw-9 → PID71602 exit0, 3 loads, 2 Saving/Saved pairs and no errors. Both scenarios conserve whole8 plus partial13→5+8 and use Flow fingerprint `f7fc744de7f9b6004f5fea897e71221d71923600c5c7e07cb22ee180b4408bca`.
- Independent review found no actionable regression and verified final C/G, both real request artifacts, deployed DLL hashes and the archive. SMAPI logs show two save traces in Delivered prepare and none in resume, versus one per process for InTransit. Root second-pass review matched the final source and staged files.
- Package verification **PASS**, 21 files /14 DLLs; generated reports were byte-matched to retained evidence before cleanup. Archive, inventory and source/staged review: `artifacts/flowline-f16-delivered-crash/`.

Full F16/F17 remain IN_PROGRESS. Saved Delivered is now covered by an actual process restart; unsaved effects, interrupted writes, other custody boundaries and production A→B→A remain separate acceptance work. Visual, new UI package boundary and environment-specific checks changes are NOT_APPLICABLE to this increment.

## F16-d: complete-save rollback after unsaved extraction

Owner: existing Flow game diagnostics and fixed harness policy. `flow.chest.crash-after-unsaved-extraction` saves both Reserved parcels and complete tagged source stacks8/13 before the first transport tick. Only after the real Saved does transport extract whole8 and selected5; the source becomes the exact untagged remainder8 and destination stays empty. A fixed marker requires the original confirmed disk hash, exact1/1/1 lifecycle counters and this distinct memory boundary before the executor kills its owned process. The new process must restore Reserved and full source XML, including each parcel's CargoId, before its first tick. A new `unsaved-rollback` assertion proves that restoration; subsequent delivery/save/reload/120 ticks and repeat-delivery rejection remain mandatory. No public API, Core/Persistence, save aggregate or request/marker field-set changes.

- `./tools/hatifect-test tools`: **PASS**, 319 Python (`run-w5b8famd`). Two added tests cover fixed phase/counter/scenario rejection and a real two-process continuation with identical confirmed saved bytes; existing b/c and ownership/failure tests remain intact.
- `./tools/hatifect-check --platform`: **PASS**, 1,203 .NET +319 Python (`run-84z67xfh`), including 659 Flow and 92 Stardew tests. `./tools/hatifect-check`: **PASS**, 1,016 .NET +319 Python (`run-g3xbx1ju`).
- Fresh isolated deployment: `run-dcakgrid`, selected package/build stages PASS. Its packaging tests are explicitly SKIPPED; separate gates above provide test evidence, with no RC promotion.
- `./tools/hatifect-smoke flow.chest.crash-after-unsaved-extraction`: **PASS10**, request `6c8b70e3-20f9-4f63-9301-1d1c76f177a3`, 29,074 ms. Marker `saved-reserved-unsaved-extraction` has counters1/1/1; PID76164 receives signal9/raw-9, PID76269 resumes and exits0, both teardown lists empty. Final diagnostics:3 loads,2 Saving/Saved pairs,341 resume frames, no errors/exceptions. Full source XML8/13 is restored before delivery, total21 remains conserved, and both duplicate-delivery commands are rejected.
- Same-candidate `./tools/hatifect-smoke flow.chest.crash-after-save`: **PASS9**, request `afa6d7fb-6893-41b9-aca6-6087b8052d0a`,31,530 ms, PID76525 raw-9 → PID76579 exit0,3 loads/2 pairs/340 resume frames and no errors. Both reports and deployed Flow DLLs use fingerprint `2d2b18916f389f42f3ae2e150185d1c8eefd27dac63e2e56021b631a9e09d07b`.
- Independent source review found no actionable regression; actual final G/C, new rollback report, raw termination/new-process evidence, game logs and deployed fingerprint were independently checked. Each process performs one actual game save. Both owned copies are cleaned; golden remains unchanged.
- Package contract **PASS**,21 files/14 DLLs. Generated report bytes matched the preserved runtime artifact before removing the generated copy. Archive, hashes, inventory and full source/staged review are retained under `artifacts/flowline-f16-unsaved-extraction/`.

This closes only unsaved extraction rollback. Full F16/F17 remain IN_PROGRESS; unsaved delivery, other confirmed custody boundaries, interrupted writes and production A→B→A remain distinct acceptance. Visual, UI package-boundary and environment-specific checks changes are NOT_APPLICABLE to this diagnostic increment.

## F16-e: complete-save rollback after unsaved delivery

Owner: existing Flow diagnostics and fixed harness policy. `flow.chest.crash-after-unsaved-delivery` saves both InTransit parcels, remainder8 and empty destination, then lets production deliver whole8/partial5 without another save. The marker requires Delivered in memory, unchanged saved bytes and fixed phase `saved-in-transit-unsaved-delivery` with exact1/1/1 counters. After owned SIGKILL, resume must load InTransit and empty destination before its first tick, set `unsaved-rollback`, then deliver once and finish the existing second save/third load/120-tick/repeated-command checks. Shared unsaved-effect hash handling preserves F16-d's distinct Reserved/source XML proof. No production transfer, public API, persistence format or request/marker field set changes.

- `./tools/hatifect-test tools`: **PASS**,321 Python (`run-f538blak`). Two new tests cover rejection of saved-delivery/other rollback phases and a real two-process fixture for this exact scenario. Existing b/c/d behavior and authority tests remain intact.
- `./tools/hatifect-check --platform`: **PASS**,1,203 .NET +321 Python (`run-98xrois1`); `./tools/hatifect-check`: **PASS**,1,016 .NET +321 Python (`run-kvtpidk3`). Flow659/Stardew92 remain included in G.
- Fresh isolated deployment `run-mlflie31`: selected stages **PASS**; packaging tests explicitly SKIPPED and no RC promotion claimed.
- `./tools/hatifect-smoke flow.chest.crash-after-unsaved-delivery`: **PASS10**, request `47df7d9b-722a-4866-86df-5ebe23369715`,32,830 ms, PID78634 signal9/raw-9 → PID78732 exit0. Marker1/1/1, final3 loads/2 Saving/Saved pairs/340 resume frames. Destination rolls back to empty, parcel custody resumes InTransit, final quantity remains21, and duplicate commands are rejected; errors/exceptions/teardown lists are empty.
- Same-candidate `./tools/hatifect-smoke flow.chest.crash-after-delivery`: **PASS9**, request `8246ec99-dcc9-4780-bf90-4f82894c2998`,31,307 ms. Marker `saved-delivered`2/2/2, PID79007 raw-9 → PID79086 exit0, final3 loads/2 pairs/154 resume frames. Saved Delivered remains intact without new materialization. Both reports use fingerprint `9e9d04e70a6d890da67244deece400ffd6c29761606ea99608c87155a6184fdf`.
- Independent source and actual evidence review found no actionable regression: final C/G, both request reports/process journals/SMAPI logs, deployed Flow fingerprint and cleanup verified. New unsaved-delivery logs contain one actual save per process; SavedDelivered has two in prepare and zero in resume.
- Package contract **PASS**,21 files/14 DLLs. Generated report matched preserved runtime bytes before removal. Archive, inventory, hashes and final source/staged attestation: `artifacts/flowline-f16-unsaved-delivery/`.

F16/F17 remain IN_PROGRESS. The next bounded family covers cancellation/source changes, then capacity refusal/retry/return and production save isolation. Unit fault injection, an actual process crash and an interrupted game-save write remain distinct evidence categories. Visual, UI package-boundary and environment-specific checks changes are NOT_APPLICABLE here.

## F16-f: cancellation, changed sources and explicit reuse

Owner: a separate `FlowChestCancellationAcceptance` over the production session, with exact startup/load/tick/title/failure/disposal hooks and fixed harness ownership. `flow.chest.cancellation` creates wine stacks8/13/17, admits selected3/5/7, rejects stale/fresh duplicate admission without mutation and explicitly cancels the first parcel before extraction. One unit is moved from the second source to a third request-owned chest and metadata changes on the third source before the first owning tick. Both changed-source effects must settle Cancelled with zero delivery attempts and no recovery fault. Full XML8/12/17 plus holding1 and empty destination survive real Saving/Saved/reload and120 ticks. A new admission from the first cancelled source delivers2, leaving6/12/17+holding1; a second save/reload/120 ticks preserves quantity38 and rejects old Reserve/new duplicate delivery. Existing roundtrip/crash drivers, production effects, public API and persistence format are unchanged.

- `./tools/hatifect-test tools`: **PASS**,322 Python (`run-_tqx40n6`). Added strict missing/failed check validation for the new report; extended canonical save ownership/golden preservation and denial of crash continuation to the new ordinary scenario. Existing assertions remain intact.
- `./tools/hatifect-check --platform`: **PASS**,1,203 .NET +322 Python (`run-eg21a8lk`), including659 Flow/92 Stardew. `./tools/hatifect-check`: **PASS**,1,016 .NET +322 Python (`run-frht02ih`).
- Fresh isolated deployment `run-ssc48glg`: selected stages **PASS**; packaging tests explicitly SKIPPED, no RC promotion.
- `./tools/hatifect-smoke flow.chest.cancellation`: **PASS10**, request `856cfa3b-76c4-48f1-9165-d8904254f693`,23,499 ms, PID81850 exit0,468 frames,3 loads/2 Saving/Saved pairs. Three distinct Cancelled IDs and a separate re-admitted Delivered parcel; quantity38, no exceptions/errors/teardown errors.
- Same-candidate `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS8**, request `1ee6623e-a520-49e7-ac47-bc86ba98b2cd`,22,568 ms, PID82210 exit0,347 frames,3 loads/2 pairs; no errors. Both reports use deployed Flow fingerprint `6189f9635bc64c6660e1101370db3bba7ff50626a05d12337fdcaf4c9db88289`.
- Independent source and actual evidence review found no actionable regression. Final G/C, both reports, full process journals, two real save traces per run, deployed fingerprint and cleanup were checked. Both request-owned copies were removed.
- Package contract **PASS**,21 files/14 DLLs. Generated report matched preserved runtime bytes before removal. Archive, hashes, inventory and full source/staged attestation: `artifacts/flowline-f16-cancellation/`.

The supported remaining F16 runtime matrix is now explicitly finite in [FLOW_PROVIDER_CONSISTENCY.md](FLOW_PROVIDER_CONSISTENCY.md): capacity refusal/retry/return and Saved Returned/taken-item persistence. Interrupted game-save file generation has no automatic-repair guarantee under the complete-save protocol; it is a documented limitation with complete-backup fallback, not a missing PASS or proof of file-write atomicity. Synthetic known-receipt recovery and observer fault injection remain separately identified. F16/F17 stay IN_PROGRESS. Visual, UI package-boundary and environment-specific checks changes are NOT_APPLICABLE to this increment.

## F16-g: capacity refusal, explicit return and taken-item persistence

Owner: separate `FlowChestReturnAcceptance` over the production session, with fixed harness scenario `flow.chest.return`. A full destination refuses whole8/partial5 and retains custody; an explicit retry delivers whole8. Filling the source and requesting partial return produces a saved ReturnRequested checkpoint, then ReturnRejected on load. Freeing one source slot and explicitly retrying returns5 with the shared attempt counter1→2→3. Five real saves/six loads cover these states and the final relocation of both physical cargo items to a third owned chest. Exact XML of cargo, source remainder8 and every filler is checked at each boundary; moved fillers are preserved. Four120-tick periods prove failed states do not retry automatically and terminal states do not materialize cargo again. A save barrier preserves ReturnRequested both before Saving and after Saved until title, so its first return effect occurs only after the checked load. Existing roundtrip/cancellation drivers, production effects, public API and persistence format are unchanged.

- `./tools/hatifect-test tools`: **PASS**,323 Python (`run-zd19b3n5`). Added mandatory missing/failed report-check coverage for the return family; canonical save/golden ownership and denial of crash continuation include this ordinary scenario. Existing cancellation assertions remain intact through a shared test helper.
- `./tools/hatifect-check --platform`: **PASS**,1,203 .NET +323 Python (`run-k_7ge2ck`), including659 Flow/92 Stardew. C attempt `run-d37zxd3x` stalled inside dotnet build with no child compiler or CPU progress; a stack sample was preserved and only that verified PID was terminated after SIGTERM failed. Its recorded result is FAIL/build-9. The unchanged-source canonical retry `./tools/hatifect-check` (`run-yrbpo33q`) is **PASS**,1,016 .NET +323 Python; no source/test weakening or settings change was needed.
- Fresh isolated deployment `run-rwdozqq7`: selected stages **PASS**; packaging tests explicitly SKIPPED, no RC promotion.
- `./tools/hatifect-smoke flow.chest.return`: **PASS14**, request `0c633d4f-6ad7-45af-ab67-1bb3fcc7ddf9`,36,853 ms, PID85725 exit0,728 frames,6 loads/5 Saving/Saved pairs; quantity21 retained, no exceptions/errors/teardown errors.
- Same-candidate `./tools/hatifect-smoke flow.chest.roundtrip`: **PASS8**, request `1578b900-510f-4895-976b-a170ba509cfe`,25,073 ms, PID86303 exit0,347 frames,3 loads/2 pairs; no errors. Both reports use deployed Flow fingerprint `ec7fc2d675d20ec8ff90de5ee24c2af071f2b9af2253bca7458c23efe37d3d26`.
- Independent source and actual evidence review found no actionable regression. Full SMAPI logs confirm all five real saves and six loads for the new family and both saves/three loads for roundtrip. Both request-owned copies were removed.
- Package contract **PASS**,21 files/14 DLLs. Generated report matched preserved runtime bytes before removal. Archive, hashes, inventory and final source/staged attestation: `artifacts/flowline-f16-return/`.

The ordinary capacity/retry/return and taken-item runtime family is complete. The fixed Saved Returned process checkpoint remains the next F16 increment; F17 still needs production A→B→A and performance evidence. Complete-save rollback, interrupted-save limitations and unit fault injection retain their distinct guarantees in [FLOW_PROVIDER_CONSISTENCY.md](FLOW_PROVIDER_CONSISTENCY.md). Visual, UI package-boundary and environment-specific checks changes are NOT_APPLICABLE here.

## F16-h: confirmed returned cargo survives process termination

Owner: private crash branch of the return acceptance driver plus one fixed harness registry entry. `flow.chest.crash-after-return` follows the complete capacity/retry/return chain, then requires the fourth real Saved with Delivered/2 and Returned/3, exact cargo/filler XML and counters4/4/4. It holds the existing save barrier and writes the unchanged21-field marker with fixed phase `saved-returned`. The executor validates owner/PID/hash/fingerprint, sends SIGKILL only to its owned process group and restarts on the same canonical save without reprovisioning. Load5 requires a distinct session/process, derives the third owned chest within a bounded15×15 area and verifies the full fixture before recording process-restart. The final two120-tick periods, terminal-command rejection, taken-item movement, Save5 and Load6 still execute in the resumed process. Normal return retains14 checks; the crash scenario adds process-restart. Old roundtrip crash logic, production effects, public API, game-save format and request/marker fields are unchanged.

- `./tools/hatifect-test tools`: **PASS**,326 Python (`run-qnmjc29g`). Three added behavioral tests cover exact returned phase/fourth-save counters, actual SIGKILL/new synthetic process on the same fixture and mandatory return/restart checks. Existing canonical save/golden and crash manifest checks include the new fixed scenario.
- `./tools/hatifect-check --platform`: **PASS**,1,203 .NET +326 Python (`run-0e1guvk_`), including659 Flow/92 Stardew. `./tools/hatifect-check`: **PASS**,1,016 .NET +326 Python (`run-dpy9h6ms`).
- Fresh isolated deployment `run-ep3yk4yh`: selected stages **PASS**; packaging tests explicitly SKIPPED, no RC promotion.
- `./tools/hatifect-smoke flow.chest.crash-after-return`: **PASS15**, request `21f4c1a2-d90c-436b-9403-c4381d614da8`,47,042 ms. PID89331 raw−9/normalized137 → PID89809 exit0. Marker4/4/4; final6 loads/5 Saving/Saved pairs/280 resume frames, quantity21. Full logs retain four completed game saves in prepare and one in resume; no exceptions/errors/teardown errors.
- Same-candidate `./tools/hatifect-smoke flow.chest.return`: **PASS14**, request `e9ca3209-e0ec-49da-841b-c182347919a1`,41,946 ms, PID90270 exit0,728 frames,6 loads/5 pairs, no errors. Both reports use deployed Flow fingerprint `501a2f9f127531489ff7463da239d62cf5ec5fd3c238f9b71d5f1fd1a4610ab2`.
- Independent source, G/C and actual evidence review found no actionable regression; both full process histories and report/deployed fingerprint were verified. Both request-owned copies were removed. Generated report matched preserved runtime bytes before removal.
- Package contract **PASS**,21 files/14 DLLs. Archive, inventory, hashes and final source/staged attestation: `artifacts/flowline-f16-returned-crash/`.

This completes the explicitly supported F16 failure matrix on the checked task candidate: Saved InTransit/Delivered/Returned process boundaries, complete-save rollback after unsaved extraction/delivery, cancellation/source changes, full-container refusal/retry/return and taken-item persistence. It does not claim recovery from interrupted game-save file generation or arbitrary observer ambiguity. Synthetic recovery evidence remains separately labelled. Combined develop integration and F17 production A→B→A/performance remain open. Visual, UI package-boundary and environment-specific checks changes are NOT_APPLICABLE to this increment.


## Combined U02-a and F16-c–g integration

The candidate combines U02-a `c5db895`/`905a7b8` with the immutable Flow handoff `2a36f342` (F16-c–g). The only conflict was append-only evidence; both complete histories are preserved. Flow source/harness/private UI delegation match the handoff, while all alpha.33 authorities and frozen API remain identical to the UI parent.

- `./tools/hatifect-check --platform`: **PASS**, 1,350 .NET +323 Python, `artifacts/validation/run-k2m2ta_q/summary.json` in `/private/tmp/hatifect-ui-next-ten-slices`. This includes 90 exact-package CA, 660 Flow, 92 Flow Stardew, 15 UI Stardew and 493 UI tests.
- `./tools/hatifect-check`: **PASS**, 1,153 .NET +323 Python, `artifacts/validation/run-36tfabkp/summary.json`.
- Independent bounded integration review found no open regression in ownership, exact scenario routing, package/API compatibility or evidence preservation. Existing U02-a isolated P remains authoritative for unchanged UI producer/CA source; the current game-only scenario delegation is additionally covered by G.
- Actual Flow F16 runtime evidence remains attached to its original candidate hashes above; it is not relabelled as a fresh run of this merge. Separate runtime/visual acceptance is NOT_APPLICABLE to this source integration. Full U02 and F16/F17 retain their outstanding acceptance. Next local implementation is atomic Flow semantic projection.


## U02-b: atomic Parcel projection checkpoint

Implementation commit: `22f56fd`. The first Flow publication owner is ParcelExperience in Flow.UI.Semantic. Its backing immutable snapshot/parcel and cargo, route, state, availability and command result exchange in one UiPublication batch. Execute retains the existing coalesced contract: the result appears on the next Pump together with the refreshed domain model. Failed reads/formatting preserve every old source and pending result for retry; F11 subscribe-before-read, dirty-during-publication and retryable unsubscribe semantics remain intact. Provider reads/effects are guarded against reentry and retirement; closing during a read/formatter stops further callbacks, and closing after a committed command preserves the domain effect without a late UI update. No Core/Persistence, game adapter, UI package producer or frozen API changes.

- `./tools/hatifect-test flow`: **PASS**,667 tests, `run-22tkrd0e`, before eight additional retirement cases.
- Final `./tools/hatifect-check`: **PASS**,1,168 .NET +323 Python, `artifacts/validation/run-rcf5vel_/summary.json`:675 Flow and493 UI.
- Added15 executed cases in ParcelPublicationTests: atomic observation and old capture, failed preparation/retry with pending result, nested owner read/execute, formatter dispatch guard, command completion after close, direct publication retirement and all read/item/origin/destination retirement boundaries. Existing subscription tests now assert their actual IUiSemanticSource contract; behavioral assertions remain unchanged.
- Independent review found late close during Pump/formatting; the implementation now stops at each callback boundary and all eight view/publication retirement cases pass. Initial `run-p_hz3qo9` remains FAIL: one new fixture incorrectly cast the Actions payload to string before reaching its scenario. The corrected object payload comparison retains every element and passed the repeat and final C.
- Platform/runtime/visual and isolated-package gates are NOT_APPLICABLE to this bounded non-platform consumer migration. Existing published alpha.33 producer and CA remain unchanged. Full U02 remains IN_PROGRESS: atomic NetworkExperience, focus/scroll fallbacks and remaining update measurements are next.


## U02-b2: atomic Network projection and preserved request intent

Implementation commit: `0fa2aa7`. NetworkExperience now owns one publication for its backing model, station/link/recovery collections, route/status/target, cached inventory, forms and validation, history query/filter/page/selection/details and command result. Selection/text facades route intent before mutation and retain captured reads for Runtime. Labels use the candidate station map during preparation. Supplementary recovery/route/inventory reads are fenced by session/revision/state and a notification epoch before commit; Network and Parcel action guards also fence an owner read that returns an old captured snapshot after a synchronous notification.

Local text/page requests read a fresh model and overlay bounded pending intent: the latest value per five fixed text sources plus one clamped page. Failure retains the old view and intent for the next Pump. Every candidate includes a pending inventory refresh; a later edit cannot swallow a failed explicit/post-send refresh. A new filter resets the page only if its candidate commits, preserving the older pending page on rejection. The ordinary domain Pump keeps cached physical inventory; removed source clears it without a read. Whole/partial send keeps the full slot fingerprint and one owner command; post-send model/result/inventory/quantity validation publish together. Closed or reentrant owners cannot dispatch, and retirement during any owner read or formatter stops subsequent work.

- Final `./tools/hatifect-check`: **PASS**,1,212 .NET +323 Python, `artifacts/validation/run-rrk5cn7b/summary.json` in `/private/tmp/hatifect-ui-next-ten-slices`:719 Flow and493 UI.
- `./tools/hatifect-test flow`: **PASS** on earlier subsets:675 (`run-kgwcarhy`, existing behavior),689 (`run-heyl41zw`, first14 adversarial cases). Final C executes42 new Network cases plus2 additional Parcel action-epoch cases. Existing Network/availability tests changed only their internal source implementation types, preserving all behavioral assertions.
- New cases cover coherent labels/backing model/history/details/forms, stale supplementary reads, failed preparation without a new notification, pending edit/page/inventory priority, whole-send with invalid partial draft, no physical read on ordinary Pump/source removal, zero Execute after stale action reads, all owner/formatter retirement entrypoints, observer failure and one committed command across failed post-send refresh.
- Independent read-only review identified and rechecked pending inventory loss, stale action reads, dropped text input and filter/page precedence. Each has an executed regression. Retirement callbacks stop before further reads/commands; observer errors preserve the committed result.
- Historical failures remain FAIL. `run-yxuyjat_`:695/696 Flow passed; one new test incorrectly expected another Pump after Network already refreshed during its guarded action attempt. Its corrected timing still asserts zero commands and current model. `run-nihmtych` stopped before .NET on the unchanged Python fake-executable stdout/stderr fixture timing out with124. The exact fixture passed separately with its original2-second timeout (`artifacts/validation/nihmtych-fixture-diagnostic.txt`), and all323 Python tests passed in final C; no harness source, timeout or assertion was changed.
- UI producer/package version remains alpha.33; Core/Persistence, game adapters and public/frozen API are unchanged. Platform/runtime/visual/isolated-package gates are NOT_APPLICABLE to this bounded consumer-only checkpoint; the queued Flow integration gets its own combined G evidence. Full U02 is still IN_PROGRESS: focus/scroll anchors/fallbacks and remaining update measurements follow.

## Flow integration of U02-a with completed F16 matrix

The Flow task combines published develop `816f12e531d233cee895082969e2931201ee368c` (U01/U02-a and F16-c–g) with its checked F16-h `eb30f620642ff48d968d876c76c19de7210c4e7d`. The only conflict was appended roadmap evidence; both complete sections are preserved. All74 incoming files other than this status document match the published upstream bytes, and all12 h files other than this document match the verified h commit. UI package authorities remain exactly alpha.33; no independent producer/API/CA modifications are introduced.

- `./tools/hatifect-check --platform`: **PASS**,1,350 .NET +326 Python (`artifacts/validation/run-akm8dyi_/summary.json` in `${HOME}/Developer/Hatifect`), including660 Flow/92 Flow Stardew/90 CA.
- `./tools/hatifect-check`: **PASS**,1,153 .NET +326 Python (`artifacts/validation/run-54u01ze5/summary.json`).
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS**,44 projected files, eight alpha.33 packages,90 tests,two CA DLLs and no UI source. Retained projection/TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.s74zo_nd`.
- Independent bounded source/authority review found no lost Flow driver, scenario requirement, save barrier or evidence, and no package/API ownership regression. Final source/staged attestation is retained in `artifacts/flowline-u02-integration/`.
- Existing runtime evidence remains attached to the original F16 candidate fingerprints. Runtime/visual and environment-specific checks are NOT_APPLICABLE to this source-only integration; a fresh runtime candidate will accompany F17.

The UI task owns the next atomic Parcel/Network semantic publication work. This Flow task continues F17 production A→B→A and bounded idle/paused measurements; it preserves the published UI producer while implementing game diagnostics and harness ownership. F16 acceptance remains bounded by complete-save coordination and its explicitly documented unsupported interrupted-write/unknown-result cases.


## F17-a: production A → B → A with whole-save isolation

Owner: private `FlowChestIsolationAcceptance` over the production session and role-aware harness provisioning. Fixed `flow.chest.isolation` derives canonical A/B names and world IDs4242424242/4242424243 from the request and companion UUID; no request authority, public API, persistence format or physical effect implementation changes. A saves partial5 in transit with remainder8; B starts with an empty transport, delivers its distinct partial7 with remainder10 and saves. Returning to A restores the original in-transit parcel before the owning tick and delivers it exactly once. All item XML, physical plus custody quantity, world/session/parcel identities, retired-session closure and stale-session command rejection are checked. Three real saves, three loads, two titles and120 final ticks complete the scenario.

Both entire bounded save trees are fingerprinted before the first load. Every load/title compares both trees, every Saving/Saved compares the inactive tree, and only confirmed Saved replaces the active baseline. Independent review found and closed an initial gap that could incorporate an extra B file into its first Saved baseline; the corrected driver was rebuilt and actually run. Python tests cover distinct canonical names/roles, byte-preserving B world-ID reseeding, golden/A immutability, invalid XML and foreign collisions, exact role-aware cleanup and rejection of missing/false required checks. Old fake isolation and crash continuation keep their prior authority.

Actual evidence on the UI alpha.33 combined task candidate:

- `./tools/hatifect-test tools`: **PASS330**, `run-tzhakh28`; four new test cases and existing crash-authority rejection expanded.
- Corrected `./tools/hatifect-check --platform`: **PASS1,350 .NET +330 Python**, `run-3lb8seo0` (660 Flow,92 Stardew,90 CA). Earlier `run-u7ygd5gi` predates the initial-tree fix and is not its certification.
- `./tools/hatifect-check`: **PASS1,153 .NET +330 Python**, unchanged retry `run-p8zk6vvb`.
- Fresh `./tools/hatifect-live-prepare`: **PASS selected build/package stages**, `run-5a7zuob2`; its test stage remains SKIPPED, not RC-eligible. Prepare `run-_k1bi_jb` and initial C `run-dn8xac94` are retained as **FAIL build−9** with native samples: their own verified MSBuild processes stopped producing output, had no child compilers/CPU activity and ignored SIGTERM; only those exact PID/parent/solution identities were then killed. The incomplete binlog replay is diagnostic, not successful build evidence. Earlier prepare `run-cwt0pds1` was superseded before any runtime run.
- `./tools/hatifect-smoke flow.chest.isolation`: **PASS12**, request `6db3a658-e0df-4422-9d00-220144fd4759`,28,136 ms,PID98019 exit0,528 frames,3 loads/3 Saving/Saved pairs/2 titles. Three distinct sessions and two distinct worlds/parcels; full logs show all three saves completed without exceptions.
- Same-candidate `./tools/hatifect-smoke flow.chest.crash-after-return`: **PASS15**, request `ac28797d-6ea0-4133-a870-7edc77e7073f`,52,165 ms,PID98626 raw−9 → PID98769 exit0. Marker4/4/4, final6 loads/5 Saving/Saved pairs/280 resume frames and quantity21; prepare/resume logs retain4+1 completed game saves.
- Both runtime reports match deployed Flow fingerprint `ee8bb0fd954ca14c36811182374c72de5e2d328511494111b610e4b4b5c7fb02`. Errors, exceptions and teardown errors are empty. Provisioning evidence records both A/B roles; all three request-owned copies are removed and golden fixture0285ac28b8cd2b128ddebbdc validates unchanged. The last generated report was byte-matched to the captured artifact before removal; clean package archived under `artifacts/flowline-f17-isolation/`.

This closes production A→B→A runtime isolation on the checked candidate. F17 still needs bounded actual idle/paused/due-work performance evidence; full F14–F17 remains IN_PROGRESS. Visual, UI package-boundary and environment-specific checks are NOT_APPLICABLE to this diagnostic increment. UI owns atomic Parcel/Network publication and the next UI producer version; this task continues Flow performance without changing those owners.

## Combined atomic Flow UI and F16-h integration

The candidate combines atomic Parcel/Network publication `22f56fd`/`0fa2aa7` with immutable Flow handoff `d1dcc649`. Only append-only roadmap evidence conflicted; both complete histories are preserved. All twelve incoming source/harness/test files match the handoff bytes. UI producer authorities remain alpha.33, and the frozen surface API and atomic CA consumer are unchanged.

- `./tools/hatifect-check --platform`: **PASS**, 1,409 .NET +326 Python, `artifacts/validation/run-ztdrwe6_/summary.json` in `/private/tmp/hatifect-ui-next-ten-slices`.
- `./tools/hatifect-check`: **PASS**, 1,212 .NET +326 Python, `artifacts/validation/run-9007o4mm/summary.json`:719 Flow and493 UI.
- Independent read-only integration review confirmed source ownership, exact F16-h routing/save barriers, package/API compatibility and both evidence histories. Actual TRX counts and Python results match both summaries, with no failed or skipped tests. Existing isolated alpha.33 package evidence remains attached to the unchanged producer/CA candidate.
- Original F16 actual-game evidence keeps its original fingerprints; these source integration gates do not claim a new runtime or visual run. Runtime/visual and environment-specific checks are NOT_APPLICABLE to this merge. Full U02 remains IN_PROGRESS; Runtime stable focus/scroll anchors and update measurements are next. F17 production isolation continues independently in the Flow task.

## U02-c: retained collection focus, scroll anchors and measured update costs

Implementation commit: `4482856`. Owner: UI Runtime, candidate alpha.34. Reconciliation keeps focused collection IDs outside the visible window and retains the old index before replacing scene indexes. Removal uses the next item at that index or the last remaining item; an empty selectable collection has a positive framework-derived content extent and a focusable container, with first-item focus on refill. These fallbacks never select an item or transfer pointer press/hover. Navigation uses logical collection positions across virtual windows and reveals its target through the owning portal/runtime. Passive wheel and ordinary scene refresh retain offscreen focus without scrolling back. External Uniform sources retain the already-known index without a reverse lookup or a full scan. Both focus orders share the same action-eligibility observation.

Uniform and Adaptive virtualization retain stable anchor ID/local offset, including a grid item moving between columns. A deleted anchor resolves to the nearest surviving predecessor in the old immutable capture, then to the first new item. One old capture and one resolved fallback request pair are retained per active collection; removal scans occur only on content transitions. Sources without an immutable old capture use the first-item fallback. Steady compositor traversal uses an index instead of allocating a collection enumerator.

- Final `./tools/hatifect-check`: **PASS**,1,248 .NET +326 Python, `artifacts/validation/run-r2fnevdg/summary.json` in `/private/tmp/hatifect-ui-next-ten-slices`:719 Flow and529 UI, including248 Runtime.
- `./tools/hatifect-test ui`: **PASS**,511 (`run-u62dyc2h`) and521 (`run-mnycfynk`) on earlier subsets. Final C executes36 new cases:24 behavioral interaction/compatibility cases, six delta/reset measurements and six worst predecessor-removal measurements. Existing virtualization, exact-row reuse, geometry, publication, selection and steady performance assertions remain intact.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS**,44 projected files, eight alpha.34 packages,90 tests,two CA DLLs and no UI source. Retained workspace/TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.b4dt8tfl`. All17 version authorities/followers move together; frozen surface API types and source hashes remain unchanged.
- `./tools/hatifect-check --platform`: **PASS**,1,445 .NET +326 Python, `artifacts/validation/run-708htee_/summary.json`, including90 CA,719 Flow,92 Flow Stardew and15 UI Stardew. Independent evidence review verified all C/G TRX counts, all36 new cases, projected P bytes and all eight package DLLs against the current producer output.
- Independent read-only review found known-index loss on unindexed navigation/removal and Uniform grid anchor replacement. Each fix has an executed regression. A second implementation pass also eliminated duplicate CanExecute observation. Earlier C `run-dc2nkqiw` passed1,247 .NET +326 Python before the last eligibility regression.

Performance evidence is retained in `artifacts/u02-runtime-anchors/performance.json`, extracted from final C TRX on macOS with x64 SDK8.0.424/.NET6.0.36. Each size uses600 samples. Capture remains0 B; selection costs1776 B with p95 about0.002042 ms at100/1000/10000 items. The six new anchor cases each verify600 subsequent renders allocate0 B and perform no layout rebuild.

| Items | Reset p95/p99 ms | Delta update p95/p99 ms | Reset / delta bytes per publication | Uniform / Adaptive removed-anchor p95 ms |
| --- | --- | --- | --- | --- |
| 100 | 0.035666 / 0.069958 | 0.044041 / 0.279584 | 32867 / 43812 | 0.026041 / 0.031750 |
| 1000 | 0.603292 / 0.782125 | 0.624333 / 0.671292 | 275965 / 378938 | 0.120333 / 0.246708 |
| 10000 | 4.489000 / 5.050541 | 5.252334 / 5.629666 | 2592235 / 3536365 | 1.472667 / 1.612458 |

The worst removal materializes at most7 Uniform or6 Adaptive items. At10000 items the Adaptive transition p99 is4.573334 ms; its allocation is about59203 B per transition versus17400 B Uniform. These are actual preparation/transition measurements, not a claim that large updates meet the unchanged2/4 ms and16 KiB steady-frame budgets. Full U02 remains **IN_PROGRESS**: one delta still rebuilds/revalidates the full collection. The next Runtime follow-up also checks rendering/accessibility against current collection state when a render-only update reuses an old layout; this checkpoint proves logical focus/anchor behavior and does not claim fresh visual acceptance. Runtime/visual and environment-specific checks are **NOT_APPLICABLE** to this host-free increment.

Historical failures retain FAIL: `run-dxvyqu8n`, `run-7pzf6mwc`, `run-yfwpub6m` stopped at new-fixture compilation errors (missing namespace, required selection argument and internal enum in a public theory signature respectively); each was fixed without changing behavior assertions. C `run-5k8onuey` reached240/246 Runtime passes, then all six new steady-render allocation assertions detected32 B/frame. The compositor enumerator was removed, the zero-allocation and no-layout-rebuild assertions were preserved, and final C executes them successfully.

## F17-a integration with atomic Parcel/Network

This task combines checked F17-a `4f38671c7b91afd28fe9d9eb8d6152a1e77de1ae` with published develop `c3ed27da6418372fc91ae1574d60fc9675019eed`. All16 incoming files other than this status document match the UI parent's bytes; all12 F17 source/harness/test files other than this document match the Flow handoff. The only conflict was append-only evidence, and both complete histories are preserved. Alpha.33 producer/package/API authorities, Core/Persistence and physical inventory effects are unchanged.

- `env DOTNET_gcConcurrent=0 ./tools/hatifect-check --platform`: **PASS1,409 .NET +330 Python**, `run-cw342dbb` (719 Flow,92 Stardew,90 CA).
- `env DOTNET_gcConcurrent=0 ./tools/hatifect-check`: **PASS1,212 .NET +330 Python**, `run-69vel7yd` (719 Flow and493 UI).
- Initial G `run-2a5r1t73` remains **FAIL build−9** with native sample. Three independent samples (`run-_k1bi_jb`, `run-dn8xac94`, `run-2a5r1t73`) show `SystemNative_ForkAndExecProcess` concurrent with GC `FlushProcessWriteBuffers`/`thread_get_register_pointer_values`, matching the reported [Rosetta process-spawn/GC hang](https://github.com/dotnet/runtime/issues/89272). This is a native-stack diagnosis; a bounded45-second attempt with local official dotnet-stack8.0.547301 produced no managed report. The [documented non-concurrent GC option](https://learn.microsoft.com/en-us/dotnet/core/runtime-config/garbage-collector#background-gc) is scoped to these validation commands and their child processes. No repository/runtime configuration or test requirement is changed. Actual game/performance processes use their ordinary GC environment.
- Bounded independent source/authority review confirmed the exact parent files, both evidence histories and unchanged F17 scenario/copy roles. Incoming source inventory is retained under `artifacts/flowline-f17-atomic-integration/`.

Runtime evidence remains attached to the original F17-a fingerprint. A new runtime/visual run is NOT_APPLICABLE to this source integration; the next F17 performance candidate will receive fresh deployment and actual game verification. The UI package producer and CA boundary are unchanged, so the existing isolated alpha.33 package evidence remains applicable to that boundary. Flow continues PERF; UI continues focus/scroll and owns the reserved alpha.34.


## Combined alpha.34 collection anchors and F17-a isolation

The candidate combines published UI `ed83393f63b51f48efcff50d03aaccef9ec05dae` (implementation `4482856`) with immutable Flow handoff `c9837fe68ea05d8190ec6c4f6de27862217ec42a`. All12 incoming source/harness/test files match the handoff; Runtime/Experience/CA, frozen API, all alpha.34 version authorities and Core/Persistence remain identical to the UI parent. The only conflict was appended evidence; both complete histories are retained. The exact private `flow.chest.isolation` handoff adds no general diagnostic or crash-continuation authority.

- `./tools/hatifect-check --platform`: **PASS**, `artifacts/validation/run-rfgmi5ja/summary.json`,1445 .NET +330 Python tests, zero failures/skips.
- `./tools/hatifect-check`: **PASS**, `artifacts/validation/run-yy0gokpi/summary.json`,1248 .NET +330 Python tests, zero failures/skips.
- The first G `run-372t4z9i` remains **FAIL**, build exit−9. Its process identity and two-second stack sample show the x64 Rosetta fork/background-GC stall; TERM was ineffective and only verified PID6336 was killed. Retry G and C use command-local `DOTNET_gcConcurrent=0`; no project setting, test assertion or timeout was changed. These retries do not establish default-GC runtime performance.
- Independent bounded integration source review found no open issue in exact parent bytes, derived A/B ownership, both initial save-tree baselines, private scenario delegation or evidence preservation. Independent evidence review confirmed both final summaries and every TRX counter, including zero failed/notExecuted/aborted/error results. Candidate file hashes are retained in `artifacts/u02-alpha34-f17-integration/candidate.json`.
- Actual F17 game evidence retains its original fingerprint; no fresh game/visual run is claimed for this source merge. The alpha.34 isolated package evidence above remains applicable to unchanged Runtime/CA/package sources. Runtime/visual and environment-specific checks are **NOT_APPLICABLE** to this integration.

Full U02 and F17 remain **IN_PROGRESS**. The next UI slice alpha.35 fixes collection rendering/accessibility against current captured selection and reconciled focus while reusing layout, then addresses the measured full-collection work for a single delta. Flow continues actual idle/paused/due-work performance independently.


## U02-d: current collection rendering and accessibility on retained geometry

Implementation commit: `56c9e7f`. Owner: UI Runtime, candidate alpha.35, following published integration `b94b262`. Render-only selection/focus updates correctly retained layout but the renderer and accessibility builder read the old window's visual/selected fields. The renderer now takes item geometry/clip/content from that immutable window and resolves state from the current captured collection plus the reconciled interaction snapshot. Accessibility reads selection from the same current capture. Reconciliation can therefore move focus to a surviving item or an empty selectable container in the first new frame. Refill moves the visible focus border to the first item.

The scene owns at most16 Selected/Hover/Focused/Pressed combinations, with normal/selected fast paths and lazy guarded resolution. Its policy captures concrete theme/profile/host/visual values, without capturing the mutable composer, publication or read scope. Existing render-only geometry guards remain in force. A transparent surface allows a focus border without selection/hover fill; the Stardew backend already draws that border after the fill. The old nine-argument collection constructor and prepared-state two-argument render planner retain their behavior; an explicit interaction snapshot, including an empty snapshot, is authoritative. Public API types and frozen source hashes are unchanged; all17 package/release authorities move together to alpha.35.

- Initial `./tools/hatifect-test ui`: **PASS541**, `artifacts/validation/run-sj20_xuq/summary.json`, including260 Runtime tests and the first12 new cases. The later compatibility/text-color tests are not certified by this earlier run.
- Final `./tools/hatifect-check`: **PASS1263 .NET +330 Python**, `artifacts/validation/run-6vllf2qy/summary.json`, including263 Runtime tests and all15 new cases; zero failures/skips.
- Final `./tools/hatifect-check --platform`: **PASS1460 .NET +330 Python**, `artifacts/validation/run-d043gfld/summary.json`, including90 CA,92 Flow Stardew and15 UI Stardew tests; zero failures/skips.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS**,44 projected files, eight exact `1.0.0-alpha.35` packages,90 CA tests and two CA DLLs with no UI sources. Full log: `artifacts/u02-current-rendering/isolated-ca.log`; retained workspace/TRX: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.bk3b5rdj`.
- Fifteen new cases exercise actual backend surfaces/text and accessibility for published and legacy collection selection, Uniform/Adaptive focus/hover/press, cancelled release, immediate removal fallback, empty/refill, theme capture and prepared-versus-explicit planner state. The owner-read guard throws on any post-Compose source/capture access during fallback update/render. Selection tests retain the identical layout/window, including its intentionally old Selected fields, while asserting the new rendered selection. Two tests verify600 steady Host.Render calls allocate0 bytes and rebuild neither frame nor layout.
- Independent source review found and closed the two-argument planner compatibility regression. The final source review has no open findings. Independent evidence review confirmed C/G summaries and all TRX counters, all15 new cases in both gates, exact44 projected files/eight package DLLs and isolated consumer references. Candidate hashes are retained in `artifacts/u02-current-rendering/candidate.json`.

Full U02 remains **IN_PROGRESS**: the next slice removes full projection/validation of unchanged collection items from a single typed Update. Actual game/visual acceptance and environment-specific checks are **NOT_APPLICABLE** to this bounded host-free Runtime change. Validation uses command-local `DOTNET_gcConcurrent=0` after the separately retained Rosetta SDK stall; no default-GC game performance claim is made.


## F17-b: measured production pause, bounded dispatch and idle

Owner: Flow game session counters and private `flow.chest.performance` driver/recorder, with a fixed Flow-only harness capture. The one existing production Tick is timed directly; counter reads, snapshots, sample storage, XML verification and JSON happen after that interval. Normal gameplay takes the existing unmeasured branch. Internal counters record actual Tick invocations, AdvanceTo operations, Tick refresh requests, successful BeginSave captures and physical Apply attempts. Public API, persistence formats, inventory effect semantics and the alpha.33 UI package producer are unchanged. The existing bounded save-tree fingerprint body is shared with isolation without changing its behavior.

The fixed workload creates80 distinct one-unit wine cargos across three one-hop routes/six ordinary chests. One real Saving/Saved provides a positive checkpoint control before admission. Paused and retained-history idle each execute120 warm-up plus600 measured owning ticks. The intervening backlog must reach64 operations in a tick, never exceed it, perform exactly240 transitions/160 physical effects and deliver every exact tagged item XML once. A Flow-specific strict manifest/report consumer checks request/session/thread/fingerprint, finite numeric types, clock/counter continuity, zero steady allocations/work and p95≤0.25ms/p99≤1ms. These Flow-only thresholds were fixed before the first actual capture; they do not describe total SMAPI frame cost or arbitrary graph scale.

Validation and actual candidate evidence:

- `env DOTNET_gcConcurrent=0 ./tools/hatifect-test flow --platform`: **PASS719 Flow +110 Stardew**, `run-ujsnzpcx`. All18 new recorder/counter cases executed: real effect/checkpoint positive controls, paused/saving/closed reset, warm-up exclusion, nearest-rank percentiles, retained allocation evidence, identity/thread/clock/tick continuity, incomplete/hidden work and cached projection replacement.
- `./tools/hatifect-test tools`: **PASS336 Python**, `run-3oa6e6hk`, before the final coordinated-counter regression. Independent review found that two claimed work ticks could be combined with240 operations despite max64; the validator now checks reachable operation totals including the final tick and observed maximum. The added regression rejects coordinated contradictory values. All seven new PERF Python cases and expanded canonical cleanup/crash-denial cases execute in final G/C (**337 Python**).
- Final `env DOTNET_gcConcurrent=0 ./tools/hatifect-check --platform`: **PASS1,427 .NET +337 Python**, `run-teyclwah`; final `env DOTNET_gcConcurrent=0 ./tools/hatifect-check`: **PASS1,212 .NET +337 Python**, `run-rn081282`. Both execute outside the filesystem sandbox to receive native file notifications; no source, test requirement or timeout was relaxed.
- Historical attempts remain **FAIL**. `run-il8ghyc3` stopped at restoreNU1301; NuGet index subsequently returned HTTP200 and unchanged restore passed. `run-ljtxsqyz` built successfully and passed Flow719/Stardew110/CA90, then the unchanged UI FileWatch test missed its rename event. Exact isolated repeats inside sandbox failed with both ordinary GC and gcConcurrent=0; the identical test outside sandbox passed1/1. Its three TRX artifacts are retained in `artifacts/flowline-f17-performance/file-watch-{diagnostic,ordinary-gc,host-check}/`. This differential points to the notification environment; no UI producer change was made. The build GC workaround remains command-local and is rejected by the actual PERF driver.
- Fresh isolated deployment `run-el2x0wmp`: **PASS selected build/package stages**, tests explicitly SKIPPED and no RC promotion.
- `./tools/hatifect-smoke flow.chest.performance`: **PASS9**, request `7a47bbe4-86de-4dfe-ab76-7c6f7aa3a0cd`,43,368ms,PID10336exit0,1,658 controller frames. Actual runtime .NET6.0.32/X64 on Darwin27, workstation GC with no override, Stopwatch1GHz. The C build ran concurrently; these are measurements of this machine/workload under that load, not a controlled general benchmark.
- Paused600 samples: **0 bytes total/max**, p50/p95/p99/max **0.002459/0.003042/0.003541/0.008792ms**. Now1→1, queue80 retained, zero processed/refresh/apply work. Idle600: **0 bytes total/max**, **0.003583/0.004708/0.005666/0.010458ms**; Now304→904, queue0. Both retained the same cached snapshot, three route searches and exactly one checkpoint capture.
- Due backlog:183 owning ticks, five non-empty work ticks, observed max64,240 operations,160 physical Apply calls and80 Delivered. Full source/destination XML and the entire last-saved tree remain valid. No extra save or title occurred.
- Same-candidate `./tools/hatifect-smoke flow.chest.isolation`: **PASS12**, request `8f13be30-aca0-431e-b6c5-05128c96a132`,29,538ms,PID11048exit0,528 frames,3 loads/3 Saving/Saved pairs/2 titles. Distinct worlds/sessions/parcels and exact cargo XML remain isolated after the shared hashing extraction.
- Both actual reports match deployed Flow fingerprint `7d955e9e6b6e18b1ac696d35e5d1e9df1527dfcb24c5b45e860cc1aa58c43ad2`. Errors, exceptions and teardown errors are empty. All three request-owned save copies are removed; golden0285ac28b8cd2b128ddebbdc validates unchanged. Generated report bytes matched the captured isolation artifact before removal. Clean archive/inventory/source attestation: `artifacts/flowline-f17-performance/`,21 files/14 DLLs.

F17's bounded production lifecycle/performance acceptance is complete on this checked Flow candidate. Supported F14–F16 consistency and failure-matrix evidence remains scoped as documented; interrupted game-save writes and arbitrary ambiguous inventory effects do not acquire a new guarantee. UI package-boundary/visual and environment-specific checks changes are NOT_APPLICABLE to this increment. The next ready Flow slice is F20 resource/receipt/checkpoint scale and D06; F18/F19 still depend on the specified UI stages. The UI task remains the sole develop integrator and owns alpha.34/alpha.35 integration; later combined candidates preserve their own gate evidence.

## F20-a: resource diagnostics and retention boundaries

Implementation: [6d0ac14](https://github.com/ihatectf/Hatifect/commit/6d0ac1407f70c70ccb03126d1ceea49f1caa6f58). Owner: Flow Core/Persistence and game-session host, based on published F17-b `ece74db`. Internal resource snapshots read counts directly from the owning collections; the host adds at most32 detached read-only port rows, bounded payload character totals and observed attempt limits. `hatifect_flow diagnostics` reports usage/limits/remaining without checkpoint capture, route search, inventory acquisition, physical transfer, XML disclosure or application notifications. Admission now distinguishes station, lifetime-link and retained-cargo exhaustion with typed internal errors and saturating counters scoped to the current load. Counters do not change revision or save data. Public application APIs and persistence formats remain unchanged; detailed UI refusal presentation still belongs to F18/F19.

- Scoped `env DOTNET_gcConcurrent=0 ./tools/hatifect-test flow --platform`: **PASS722 Flow +114 Stardew**, `artifacts/validation/run-d6yyk3h2/summary.json`. Four new host tests cover detached/no-effect diagnostics, thread/reentry/retirement fencing, cargo257 before lease/tag/quantity/revision changes and cargo retention after cancellation/reload, plus station33 rejection. The existing lifetime128-links test now verifies the specific error and remaining budget after removal/reload.
- Three new `ResourceRetentionTests` cases run32/128/256 parcels through16 explicit rejected-delivery attempts. At256: **4608 processed operations**, max64/advance,4352 physical callbacks/issued/retired capabilities,4096 destination receipts and256 source receipts. Attempt17 Retry/Return produces no work; replaying a known receipt at4096 produces no effect. Full encoded checkpoint round-trips byte-for-byte after restore, all identities/custody/attempts remain, and idle restored transport performs no physical callback.1000 Core diagnostic reads allocate zero bytes on the test thread.
- Full `env DOTNET_gcConcurrent=0 ./tools/hatifect-check --platform`: **PASS1434 .NET +337 Python**, `artifacts/validation/run-rq4b5dwy/summary.json`. Final `env DOTNET_gcConcurrent=0 ./tools/hatifect-check`: **PASS1215 .NET +337 Python**, `artifacts/validation/run-wg6hr6wl/summary.json`. Both execute outside the sandbox for the previously established file-notification environment requirement; the temporary build GC setting is not a production/runtime configuration.
- Independent source and scoped-TRX review found no actionable defect. No test is deleted or weakened; new exception assertions retain and extend the lifetime-retention behavior checks. UI package boundary and visual runtime changes are NOT_APPLICABLE to this bounded increment.

F20 remains **IN_PROGRESS**, and D06 is not yet closed. These are model and game-adapter tests, not actual SMAPI scale/performance evidence or a claim of unlimited save longevity. The next ready step is the fixed actual resources scenario: saved queues32/128/256, saturated receipts256/2048/4096, separate routing/dispatch/capture/write/restore costs, exact item custody and no-growth refusal/idle/reload checks. Receipt compaction is not introduced. New combined UI/develop candidates retain their own validation; alpha35/alpha36 and develop integration remain with the UI owner.

## Combined alpha.35 and F17-b performance

Integration commit: `f76ab21`. The integration combines UI `959c8cc` (alpha.35 implementation `56c9e7f`) with immutable Flow `ece74db16e6a25324af6c85bb83949c16e4c4996`, whose parent `c9837fe` was already integrated. All20 incoming non-status files initially matched that handoff. Both complete appended status histories survive the sole merge conflict. Runtime/Experience/CA, frozen API, all alpha.35 package/version authorities and Flow Core/Persistence retain the UI parent's bytes; only the exact private `flow.chest.performance` UI-controller delegation changes.

- `./tools/hatifect-check --platform`: **PASS1478 .NET +337 Python**, `artifacts/validation/run-5r32c_e4/summary.json`, zero failures/skips. This validates the combined compiled source; the subsequent correction changes only Python validator/tests.
- Independent review found a manifest gap: removing required host checks or the Flow mod could weaken acceptance despite retaining the fixed metric capture. The validator now requires all eight exact checks and `requiredMods=['Hatifect.Flow']`. A new behavior test reproduced22 previously accepted variants before the fix and covers omitted, removed and substituted checks/mods. `./tools/hatifect-test tools`: **PASS338**, `artifacts/validation/run-iembzk0h/summary.json`.
- Final `./tools/hatifect-check`: **PASS1263 .NET +338 Python**, `artifacts/validation/run-w4dwaafd/summary.json`, zero failures/skips. SDK gates use command-local `DOTNET_gcConcurrent=0` for the separately documented Rosetta fork stall; no persistent setting, assertion or timeout changed. Actual Flow PERF above used ordinary GC.
- Independent review read the original owner evidence in `${HOME}/Developer/Hatifect/artifacts/flowline-f17-performance/`: source attestation hashes match `ece74db`, archive/inventory21 files14 DLLs and computed Flow fingerprint match both actual reports. The stricter integration validator also accepts original PERF9 and isolation12 assertions. These reports retain their original request IDs and candidate fingerprint; no fresh game run is claimed for the combined UI source.
- Source and evidence review closed the manifest finding. Candidate file hashes and integration correction ownership are retained in `artifacts/u02-alpha35-f17-integration/candidate.json`. Unchanged package-boundary evidence remains the alpha.35 isolated CA run above. Runtime/visual and environment-specific checks are **NOT_APPLICABLE** to this integration.

F17 is complete for its documented provider/lifecycle scope. Full U02 remains **IN_PROGRESS**. Next: U02-e alpha.36 updates only changed collection payloads/projections while preserving immutable snapshots, stable indices, version history and atomic validation; Flow independently continues F20.

Before publication, the fresh remote `develop` contained `7a9e590` with nine runtime executor files. They merge without conflict or compiled-source changes. Final combined `./tools/hatifect-test tools` is **PASS342**, `run-q0jhgtwg`; `./tools/hatifect-check` is **PASS1263 .NET +342 Python**, `run-syq2_ud9`, zero failures/skips. The final source pass confirms Ready/transport validation remains intact; this merge changes no acceptance budgets. No executor or game was started for this tooling integration.


## U02-e: bounded Update preparation and U02 acceptance

Implementation commit: `356e52e`. Owner: UI Experience, alpha.36, based on published `7dec9c7`. A single typed Update previously copied and projected all N payloads, rebuilt the stable-ID dictionary and repeated full source validation. Update-only changes now validate every sequential operation's address/identity/nullability, retain final values by index and project them in index order. The registered immutable schema and exact captured source/base authorize a private prepared candidate; shared item validation retains the existing CLR/nullability semantics. Errors still reject the entire publication through `UIP010`. The proof is not stored in the committed snapshot or history.

Collection storage uses private leaves of at most128 elements. An updated capture clones the root array and touched leaves, retaining neither an older root nor an older snapshot. The stable-ID map is reused, and a supporting-item count avoids rescanning when the last supporting text disappears. Equivalent explicit updates still advance source version/history while retaining item/content revisions and immutable storage. Mixed equivalent/non-equivalent updates preserve the existing distinction between the supplied typed value and a reused equivalent semantic item. Empty explicit changes retain selection capability/membership validation. Shared formatter/locale changes use Reset/Replace to reproject unchanged payloads.

Projection/item validation is O(K); total preparation includes O(N/128 +128×touchedBlocks +K log K +history), with K≤128 and history≤64. Insert/Remove/Move/Reset retain full O(N) preparation. No public signature, persistence contract or dependency is added; all17 producer/consumer version authorities move together to alpha.36 and frozen public API source hashes remain unchanged.

Validation:

- Initial `./tools/hatifect-test ui`: **PASS559**, `artifacts/validation/run-ghfreyf6/summary.json`. This includes15 new Update cases; it precedes the final allocation regression assertion.
- Final `./tools/hatifect-check`: **PASS1278 .NET +342 Python**, `artifacts/validation/run-rhgz6ysz/summary.json`.
- Final `./tools/hatifect-check --platform`: **PASS1493 .NET +342 Python**, `artifacts/validation/run-htdyajte/summary.json`, including90 CA,110 Flow Stardew and15 UI Stardew tests.
- `env -u DOTNET_gcConcurrent -u COMPlus_gcConcurrent ... ./tools/hatifect-test ui --no-build --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`: **PASS263**, `artifacts/validation/run-t87jl1b3/summary.json`. Seven compiled DLL hashes match before/after. This uses the existing x64 .NET runner, SDK8.0.424 and net6.0 target on macOS27; environment and full measurement lines are retained in `artifacts/u02-update-preparation/performance-environment.json` and `performance.json`. No local build runs concurrently; this is not a globally idle/thermally controlled benchmark.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**,44 byte-identical projected files, eight exact alpha.36 packages matching current producer DLLs, two CA output/deploy DLLs and no UI sources. Full log: `artifacts/u02-update-preparation/isolated-ca.log`; retained workspace: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.ulga7jn8`.
- U/C/G/P and ordinary-GC source/evidence review passed with no open findings. All15 new cases execute in U/C/G. Current G also executes the earlier atomic, version/history, selection/focus/scroll and rendering acceptance:12 Publication,24 PublishedCollection,3 PublicationScene,30 CollectionPublicationInteraction,15 CollectionStateRendering,17 ParcelPublication,42 NetworkPublication and20 CA experience cases. All gate/TRX failure, skipped/notExecuted and error counters are zero.

Ordinary-GC preparation measurements,600 samples per case, whole BeginUpdate/Apply/Commit. Allocations are the measured mean, not a per-sample maximum:

| Items | Update p95/p99 ms | Update bytes/publication | Reset p95/p99 ms | Reset bytes/publication |
|---:|---:|---:|---:|---:|
|100|0.003375 /0.003791|4568.00|0.043000 /0.333583|34659.08|
|1000|0.003625 /0.017916|5129.48|0.792667 /1.046583|292606.00|
|10000|0.004500 /0.005458|6264.00|7.287292 /7.929292|2757495.01|

The three Update cases additionally enforce mean allocations≤16KiB/publication, rejecting a regression to full projection/validation. Existing UI time/frame budgets are unchanged. The large cold Reset measurements do not establish an overall steady-frame budget PASS. C/G/P use command-local `DOTNET_gcConcurrent=0` for the previously diagnosed SDK fork stall; the separate measurements above explicitly remove both GC override variables. Runtime/visual game acceptance and environment-specific checks are **NOT_APPLICABLE** to this host-free storage change.

Independent bounded acceptance review finds no remaining U02 exit criterion: source version/atomicity, all four deltas/reset, replay/gap handling, bounded history, stable selection/focus/scroll and coherent current rendering are covered by current tests. **U02 is DONE** with the explicit large structural/cold preparation limitation. Candidate source/package/DLL hashes are in `artifacts/u02-update-preparation/candidate.json`. Next UI ID is U03 typed request/result and asynchronous action lifecycle; U04 is independently ready after U01. The received Flow resource checkpoint is integrated separately after this source candidate.

## F20-a integration with alpha.35 and current executor tooling

Parents: published F20-a `6d0ac1407f70c70ccb03126d1ceea49f1caa6f58` and published develop `7dec9c7c4be4b9f318999658583a04f7160f59a1`. All43 incoming files outside the two roadmap documents retain the incoming parent's bytes; all14 F20 files outside Markdown and the Flow README retain the F20 parent's bytes. The single status-document conflict preserves both complete appended histories; the two roadmap documents now also link the actual F20-a commit. No additional compiled-source change is introduced by this integration.

- `env DOTNET_gcConcurrent=0 ./tools/hatifect-check --platform`: **PASS1485 .NET +342 Python**, `artifacts/validation/run-lgsxzus2/summary.json`.
- `env DOTNET_gcConcurrent=0 ./tools/hatifect-isolated-ui-ca --keep`: **PASS**,44 projected files,8 exact alpha.35 packages,90 executed CA tests and2 deployed CA DLLs with UI source absent. Retained workspace: `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.893zehr8`.
- `env DOTNET_gcConcurrent=0 ./tools/hatifect-check`: **PASS1266 .NET +342 Python**, `artifacts/validation/run-xx_1sd3l/summary.json`. All checks execute outside the sandbox; the temporary GC setting applies only to SDK processes.

The old executor's heartbeat was stale and its recorded PID no longer existed. The first canonical `serve` attempt stopped on an absent local SMAPI setting; configuring only the verified executable path in the ignored worktree `tools/hatifect.env` resolved that prerequisite. A single owned canonical supervisor then reached **Ready**, and `status` plus `doctor` passed. No game was launched for this merge; no new runtime/performance claim is attached to it. F20-b now proceeds with actual bounded resource measurements; F20 and D06 remain unfinished, while alpha.36 work and develop integration remain with the UI owner.

## Combined alpha.36 and F20-a diagnostics

The candidate combines published UI `b760efc` (U02 implementation `356e52e`, linked evidence `f15296f`) with checked Flow handoff `17a2e99d804c873bebece6aece1fb5a9ce67bf8f` (F20-a implementation `6d0ac14`). All14 incoming non-Markdown source/test files retain the exact Flow handoff bytes. UI/CA, frozen API, all alpha.36 package/version authorities and Flow manifest are unchanged. The only merge conflict was appended STATUS evidence; both full histories are preserved. A stale current U02 summary row and the equivalent SEMANTIC_V2 summary were synchronized with DONE; historical U02-a–d entries remain historical.

- `./tools/hatifect-check --platform`: **PASS1500 .NET +342 Python**, `artifacts/validation/run-43waze1u/summary.json`, including722 Flow,114 Flow Stardew,90 CA and559 host-free UI tests.
- Final `./tools/hatifect-check`: **PASS1281 .NET +342 Python**, `artifacts/validation/run-6rl0txqb/summary.json`. All executed/passed TRX counters agree and every failure/skip/notExecuted/error counter is zero. Three new retention cases and all15 Update cases execute in both applicable gates.
- Independent source/provenance review confirms14 incoming file identities,45 owner reviewed-source hashes and original G/C evidence on alpha.35. Independent combined G/C review and final source-hash/staging checks have no open finding. Candidate hashes: `artifacts/u02-alpha36-f20-integration/candidate.json`.
- The existing alpha.36 P/ordinary-GC UI measurements above remain attached to unchanged UI/package/CA sources. SDK gates use the previously documented command-local GC workaround. This merge starts no game or executor; runtime/visual and environment-specific checks are **NOT_APPLICABLE** to the integration. F20 actual resource measurements and D06 remain **IN_PROGRESS** with the Flow owner.

U02 remains DONE; Q01's published alpha.35 baseline and visual gaps remain unchanged. The next UI slice is U03 typed actions and asynchronous lifecycle, with alpha.37 reserved for its producer/consumer changes.

## U03-a typed action execution foundation

Status: IN_PROGRESS for full U03; alpha.37 candidate, base `cb037e0`. Owning contracts: Experience immutable action/request-result/availability/concurrency descriptions; Runtime owns per-host execution, dispatcher, cancellation resources and lifecycle fencing. Existing `UiActionDefinition` and frozen semantic surface API signatures remain unchanged in this foundation. Production host/input/legacy adaptation and RUNTIME acceptance remain U03-b.

The dispatcher carries explicit session/publication and generation identities. One action retains one active operation and a bounded FIFO or one latest waiting request. Queue capacity is explicit (1–128 waiting requests); bindings are bounded at4096 per host. RestartLatest is serialized: cancel active, cancel superseded UI submissions, retain latest, start only after active actually completes. Noncooperative domain work can delay latest. Cancel does not replace a later successful committed domain result. An async observer holds neither dispatcher nor completion subscriber; late faults remain observed after retirement. Cancellation callbacks run outside the completion lock; CTS disposal waits for an in-flight Cancel callback to exit.

| Requirement | Evidence in ActionExecutionTests |
|---|---|
| Shared immutable description; separate session/generation state | SharedDescriptionHasIndependentHostStateAndSynchronousTypedResults |
| Owning-thread completion and one delivery | BackgroundCompletionChangesStateAndCallsConsumerOnlyOnOwningPump |
| Reject without duplicate effect | RejectWhileRunningDoesNotInvokeAgainOrReplaceActiveState |
| Bounded latest and noncooperative active | RestartLatestCancelsWaitingAndKeepsOneNoncooperativeOperation |
| FIFO, explicit capacity, overflow and bounded per-pump work | QueueIsFifoWithCapacityAndAtMostOneQueuedStartPerPump |
| Availability/field reason rechecked before queued effects | AvailabilityIncludesFieldReasonAndIsRecheckedBeforeQueuedEffects |
| Cancellation cannot undo committed success; token provenance | CancellationDropsQueueButPreservesSuccessfulCommittedDomainResult; OnlyCancellationFromThisRequestedTokenBecomesCancelled |
| Late success/fault after retirement cannot affect successor | CloseOrReplacementFencesLateSuccessAndFault |
| Null/throw/fault/domain rejection/cancel result | SynchronousThrowFaultRejectionCancellationAndNullAreObserved |
| Reentry and failed callbacks preserve remaining work | CallbackFailureAndReentryDoNotLoseQueuedWork |
| Close during result or availability callback | CallbackCanCloseDispatcherWithoutStartingQueuedWork; AvailabilityFailureAndOwnerRetirementPreventExecutorEffects |
| Availability exceptions preserve diagnostics and do not leak into later domain rejection | QueuedAvailabilityFailurePreservesOriginalDiagnosticInCompletion; AvailabilityFailureDoesNotLeakIntoLaterDisabledOrSuccessfulAdmission |
| Custom ValueTaskSource errors and duplicate/inline/late callbacks produce one terminal result | ValueTaskSourceInspectionFailureCompletesAndAllowsNextInvocation; ValueTaskRegistrationAndLateDuplicateCallbacksHaveOneTerminalDelivery |
| Cancellation fault observation and no mailbox deadlock | CancellationCallbackFailureIsObservedAndDoesNotPreventRetirement; CancellationCallbackCanJoinWorkerCompletionWithoutHoldingMailboxLock |
| Closed owner and subscriber can be collected before task ends | RetiredPendingOperationDoesNotRetainHostOrCompletionSubscriber |
| Thread/registration guards and validated immutable contracts | WrongThreadAndDuplicateRegistrationHaveNoEffects; QueueCapacityAndResultFactoriesEnforceValidContracts |

Targeted command: `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, with explicit x64 SDK8/runtime6 paths and command-local `DOTNET_gcConcurrent=0`. Initial scoped evidence `run-rqmdvnwq`: PASS287/287,0 failed/skipped,24 new cases. Earlier `run-f43rfpb3` was a nullable build FAIL; `run-cgb0m0wf` executed285 cases with9 FAIL because tests assumed immediate mailbox delivery after TaskCompletionSource completion. Both failures were corrected and remain historical failures. Tests now wait boundedly for an actual owning-thread Pump delivery; no production timeout or acceptance assertion was weakened.

Final C/G/P passed; independent source review closed both P2 findings. RUNTIME/VISUAL: NOT_APPLICABLE to this private foundation-only implementation; mandatory full U03 RUNTIME remains outstanding and must run after host/consumer integration. Next: U03-b, bind per-presentation dispatcher to root/portal/Terminal hosts, retain it across ordinary recomposition, retire on close/successful reload/session replacement, adapt legacy synchronous actions, then execute isolated runtime acceptance.

U03-a second-pass corrections: availability exceptions now yield typed Failure with the original diagnostic for both rejected admission and already accepted queued completion. A fresh provider read clears the previous availability failure. Independent reviewer also found a custom ValueTaskSource status exception outside the observation boundary: `run-8gr14vxg` reproduced1 FAIL/291 cases (290 PASS). The direct configured awaiter now catches initial status, registration and result-reading failures, with one result read and first-terminal-result publication. Registration-then-throw still observes its late underlying fault; duplicate callbacks cannot re-read or replace a terminal result. This avoids unchecked custom-source paths in the .NET6 async builder/AsTask adapter. New narrow and final gates follow below; predecessor C `run-xheley1g` PASS1305+342 and G `run-xw7qboc9` PASS1525+342 do not cover this final observer correction.

Final U03-a source evidence: `run-obcov48y` PASS296 Runtime (33 action cases), C `run-f6ivev8y` PASS1314 .NET +342 Python, G `run-jk6ztvip` PASS1533 .NET +342 Python. Actual TRX counters agree with summaries and show no failures, skipped/not-executed/error/timeout/aborted/inconclusive cases. Source review closed both P2 findings; source remained unchanged throughout final gates. P passed against the same eight alpha.37 package producers:90 CA tests,44 exact projection files,3 assets files referencing isolated packages and2 CA DLLs without UI duplication. Final snapshot/evidence is retained in `artifacts/u03-action-foundation/candidate.json`.

P command: `./tools/hatifect-isolated-ui-ca --keep`, same SDK environment. Retained workspace `/private/var/folders/ly/sph907ln7bv1tc3dxmflc62h0000gn/T/hatifect-ui-ca-isolated.xqyabrfd`; log `artifacts/u03-action-foundation/isolated-ca.log`. All44 projection files match current sources,8 nupkg/DLLs match alpha.37 producers,3 assets use the isolated package cache,90/90 TRX counters pass, and both CA DLLs match the isolated deployment. The first supplemental attestation used an incorrect guessed semantic DLL filename and failed; actual output inventory corrected the check to `Hatifect.ChestsAnywhereOverlay.UI.Semantic.dll`. The canonical P command itself passed on its first run.

Remaining scope: foundation idle availability is refreshed only by admission/queued start; U03-b must compute availability before presenting input state. It must also preserve the committed Transient Terminal model during focus/text/profile recomposition: existing Shell.Recompose re-enters Activate and can replace the visible action while CurrentInvocation still points at the prior model. Host binding, legacy unit adapter, consumer migration, close/reload/save-switch wiring and isolated RUNTIME acceptance remain unimplemented; full U03 stays IN_PROGRESS.

## U03-a integration with published Q01 capture fixes

U03-a implementation is `320e5c9` (alpha.37); full U03 remains IN_PROGRESS. Integration preserves published `54e14e7`, including `85a786f` completed-frame capture and `37b735e` isolated HUD world/fade fixture. Their alpha.35/alpha.36 actual runtime evidence remains in Q01 documents, not attributed to this alpha.37 integration. Six incoming files merged without conflict; the controller and scenario manifest initially matched the published source byte-for-byte.

Independent review of the incoming controller found an error path: a failed diagnostics write was retried in the capture catch before exit was requested. The integration correction requests exit in finally and treats the repeated diagnostics write as best effort before marking missing checks. This preserves the existing diagnostic-before-failure-record ordering, even when that diagnostic cannot be written. The original capture failure remains the retained cause; a secondary diagnostics failure is logged. This bounded guard changes no capture timing, successful screenshot pixels, assertion thresholds or measured performance window. No synthetic test was added merely to repeat its try/catch; static source review and the current G/P check this correction, while actual runtime with unavailable diagnostics remains unperformed. Full Q01 remains IN_PROGRESS.

Pre-correction combined C `run-0mewubo0` PASS1314 .NET +342 Python, G `run-feubc2_3` PASS1533+342, P PASS90 (`hatifect-ui-ca-isolated.yydjl9v5`). Independent attestation verified all summaries/TRX,44projection files,8alpha.37 packages,3isolated assets and2CA DLLs. C remains applicable to unchanged host-free inputs; final G/P after the controller-only error guard follow separately. Evidence: `artifacts/u03-q01-integration/`.

The first guard validation `run-kzz2906k` failed 1/342 Python tests before .NET build: its reordered failure recording violated the existing diagnostic-before-check source contract. The correction preserves that order while catching secondary diagnostics I/O failure and retaining the finally exit request. The existing test is unchanged.

Final correction checks: tools `run-jfvm_a6p` PASS342 Python; G `run-ugtiegye` PASS1533 .NET +342 Python with actual TRX counters and individual results verified. P PASS90, retained `hatifect-ui-ca-isolated.xufaussq`, log `artifacts/u03-q01-integration/isolated-ca-final.log`; all44 projected files match sources,8 packaged DLLs match current producers, both CA deployment DLLs match and90 TRX results pass. Supplemental comparison with an older host-free feed failed on NuGet-generated metadata identities; direct packaged-DLL comparison passed and the actual eight package hashes are retained separately. Independent read-only controller review confirms the diagnostics error path closes without changing the required recording order. Actual failed-diagnostics runtime remains unperformed; next UI work is the U03-b Transient Terminal identity prerequisite.

## F20-b measured resources and D06 retention decision

Implementation: `a0520d20ea89422c06a929a6f3fca20557eb1bfe` ([commit](https://github.com/ihatectf/Hatifect/commit/a0520d20ea89422c06a929a6f3fca20557eb1bfe)). Together with F20-a `6d0ac14`, this completes F20 for the chosen bounded ordinary-chest single-player provider. [The resource profile](FLOW_RESOURCE_PROFILE.md) records the fixed workload, exact measurement scopes, limits, raw evidence locations and D06 decision. The public API and persistence format are unchanged.

- G `env DOTNET_gcConcurrent=0 ./tools/hatifect-check --platform`: **PASS1507 .NET +348 Python**, `artifacts/validation/run-qb_fc3jg/summary.json`. C `env DOTNET_gcConcurrent=0 ./tools/hatifect-check`: **PASS1269 .NET +348 Python**, `artifacts/validation/run-t8vqdf3n/summary.json`, zero failures/skips. Scoped Flow was **PASS725 +133**, `run-_7ay09me`. Build-only GC workaround remains command-local and absent from the game.
- Final ordinary-GC `flow.chest.resources` request `f68d1e6b-4737-4d6d-980c-3f3146f476c1`: **PASS12**, seven distinct loaded sessions, six real save/title cycles, queues32/128/256,75 measured routes and16 delivery-attempt waves. Total4608 operations/4352 physical calls; max64/tick. Per-station journals retain4096 destination and256 extraction receipts; attempted cargo257 and attempt17 refuse without changes to inventory, revision, queue or journal. Full XML and saved-tree identity checks pass. Resource idle p95/p99=0.003583/0.005875ms,0B over600 measured ticks after120 warm-up.
- Final `flow.chest.performance` regression `a488defd-3448-4a8d-93b1-abe2e0831a5c`: **PASS9** on the same deployed DLLs,80 deliveries/240 operations/160 physical calls, both600-frame paused/idle windows0B. Fingerprint: `b954c613428089e5cb84156dabd8742931997d99729bea15d6062ff86c52e44b`. No build overlaps either final measurement.
- Exact archive21 files/14 DLLs: `artifacts/flowline-f20-resource-profile/Hatifect-clean-baseline.zip`, SHA256`3d81d338cd32b82ce70cbff1eff809ffe5841558eb39506b67be87858bab48d4`. All entries match the tested deployment. Original reports, logs, process metadata, source hashes, archive inventory, metric extraction and unchanged golden validation are retained in the same owner worktree `${HOME}/Developer/Hatifect`; this does not claim Q03 release readiness.
- Independent source/actual-evidence review confirmed all new tests and counts. Three validator gaps were reproduced and closed: hidden work during queued saves, routing rows detached from their owner and receipts moved to another station. Exact roles/counters and regression mutations now reject them. The preliminary run `b120e372-200f-4e16-8154-19bdf1ca7690` passed its original validator but lacks final role metadata and is not final acceptance. Initial test failure `run-voa405c6` retained: corrected the test to reject old session capabilities after reload without changing production recovery.

D06 chooses bounded retention/backpressure, with256 lifetime cargo per save and no compaction. Final Core image is1,816,928 bytes plus406,572 separate item XML characters. Active work and cold persistence are allocation-heavy: first wave max24.735334ms/7,124,848B per tick, largest retry tick5.398ms, final capture65.037ms/29.9MB and construction/restore88.332ms/33.4MB. These measurements are not a60fps active-work guarantee, unlimited admissions or general hardware benchmark. Zero-allocation evidence is limited to idle/paused tick behavior. Any history reclamation needs a new recovery-horizon and migration proof.

F20 is **DONE** in this scope. Full roadmap remains incomplete: F12 requires U04, F13 requires F12/U03, and F18/F19 require U07 and the earlier Flow UI steps. Continue from the first completed UI contract dependency and coordinate integration through the UI owner; Q01 remains with its existing runtime-baseline task. No dependent stage is silently bypassed.


## Combined alpha.37 and completed F20 resource profile

Integration base `64cce4e` contains U03-a implementation `320e5c9` and Q01 through `54e14e7`. Incoming Flow handoff `09e3b4a` contains implementation `a0520d20`, evidence `db7acd69` and the F12 readiness assessment. Sixteen incoming non-Markdown files retain exact owner bytes; the shared UI controller only excludes the new Flow-owned scenario, and the manifest retains every existing scenario unchanged while adding `flow.chest.resources`. Both appended STATUS histories survive their sole merge conflict. Public UI signatures, alpha.37 package authorities, production domain rules and persistence format remain unchanged.

- C `./tools/hatifect-check`: PASS1317 .NET +348 Python, `run-zc0hvkty`.
- G `./tools/hatifect-check --platform`: PASS1555 .NET +348 Python, `run-af2q37sk`, including725 Flow,133 Flow Stardew,296 UI Runtime and90 CA. Actual TRX counters/individual results were verified, with no failures/skips. Commands use the documented explicit x64 SDK and command-local build GC workaround.
- P `./tools/hatifect-isolated-ui-ca --keep`: PASS90, eight alpha.37 packages,44 projected files and2 isolated deployment DLLs. Retained workspace `hatifect-ui-ca-isolated.rlvvd248`; source/package-DLL/TRX checks pass. Log and candidate hashes: `artifacts/u03-f20-integration/`.
- Independent F20 review verified the bounded fixture/validator,20 original source hashes/blob identities, original C/G and actual resources/PERF evidence. All29 retained runtime file hashes match. Those ordinary-GC game results belong to the owner alpha.35 candidate and Flow fingerprint `b954c613428089e5cb84156dabd8742931997d99729bea15d6062ff86c52e44b`; this integration does not claim new alpha.37 runtime/visual measurements. No game, normal Mods or save was changed by these integration gates.

F20 is DONE only in its documented bounded ordinary-chest single-player scope; active/cold cost limitations remain. Full U03 and Q01 stay IN_PROGRESS. The next UI implementation is the U03-b Transient Terminal model-identity prerequisite, then host action binding; F12 continues to await U04.


## U03-b Transient Terminal model identity

Base `4737d64`, alpha.38; full U03 remains IN_PROGRESS. Root cause: Shell.Recompose called Open/Activate; a Transient factory ran even for initial host focus, leaving visible scene and CurrentInvocation with different models. The shell now retains the committed Experience and uses internal InvocationService.ReplanKnownAvailable against its registered descriptor. Current assets, section availability, profile, locale and theme are evaluated again; only an explicit successful Open/route commits a new model. Failed host update preserves the prior owner, and Dispose clears the retained reference before cached-owner disposal. No new collection, traversal or history is introduced on input; the former activation/factory allocation is removed. Public Activate/Invoke lifetime policy, frozen surface API signatures and persistence remain unchanged. All17 UI version/package/consumer authorities move together to alpha.38.

Focused regressions extend the existing TerminalHostSessionTests fixture; no project-wide test scaffolding or new test dependency was introduced.

| Requirement | Evidence |
|---|---|
| Initial focus, actual text input and environment recompose keep model/action/draft/focus | `TransientHostRecompositionPreservesActivatedModelDraftAndAction` |
| Rejected route cannot replace committed model; explicit reopen creates fresh Transient model | `TransientRouteFailurePreservesModelAndExplicitReopenCreatesNewModel` |
| Availability changes still affect navigation/admission without activating sections | `TransientRecompositionRechecksAvailabilityWithoutActivatingSections` |
| Existing explicit transient activation and owned cached disposal remain valid | Existing `RegistryTests.TransientWindowCreatesEachTime` and Terminal lifecycle tests remain unchanged |

Reproduction `run-o33wsx8k`: FAIL3/299,296 existing cases PASS. Factories ran2/3/5 times instead of1; tests were preserved for the fix. Targeted `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`: `run-jdvxftmu` PASS299/299. Explicit x64 SDK and command-local build GC workaround as documented above. Final C/G/P and independent review follow on the frozen candidate. Runtime/visual is NOT_APPLICABLE to this internal identity prerequisite; full U03 host/consumer integration still requires isolated RUNTIME acceptance. Next: bind action execution to the stable host model and retire it on actual owner/generation replacement.


Independent review found one lifecycle regression before final gates: removing Open/Commit also removed its post-composition EnsureActive. `run-qxk8f5y0` reproduced FAIL3/302 (299 PASS): CanExecute disposed the host, opened another section, or explicitly reopened the same Cached definition while outer composition was still running. The shell now captures the committed model and an internal commit stamp before callbacks, then checks live/stamp after availability, asset resolution and composition. Every explicit Commit advances the stamp, including same-model reopen; ordinary Recompose does not commit. No new history or per-recompose allocation is added. Regressions are unchanged for the correction.

| Requirement | Evidence |
|---|---|
| A composition callback that closes the Terminal cannot validate/publish a new scene | `RecompositionCallbackCannotPublishAfterTerminalDisposal` |
| Nested committed Open wins over the old outer scene, including same Cached model | `RecompositionCallbackCannotOverwriteNestedCommittedOpen` (two cases) |

Final scoped correction `run-dfnhqwa0`: PASS302/302 Runtime cases, including all six new identity/callback cases. Independent source review closes the P2 at the shell composition return boundary. Validation/platform callbacks after shell return remain part of subsequent host action integration; this result does not claim that larger lifecycle surface is complete.


Final alpha.38 gates on unchanged compiled sources: C `./tools/hatifect-check`, `run-hsx41w29`, **PASS1323 .NET +348 Python**; G `./tools/hatifect-check --platform`, `run-lccboylt`, **PASS1561 .NET +348 Python**. Actual TRX counters and all individual outcomes agree, with no failures/skips. P `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**,44 exact projection files,8 alpha.38 package DLLs matching producers,3 isolated assets files and2 matching CA deployment DLLs without UI duplication. Retained workspace `hatifect-ui-ca-isolated.f1_qfmwf`; log and hashes under `artifacts/u03-terminal-identity/`. A supplemental audit initially assumed the TRX completed outcome equalled passed and that NuGet package paths ended in a slash; both audit assumptions were corrected against actual files, with the canonical successful P result unchanged. Source review is PASS at the documented shell boundary. Full U03 remains IN_PROGRESS; next is production host action binding and lifecycle fencing.

## Q01 first-frame fixes and fresh alpha.37 runtime

Integration `aa04181073ec66694ec6713aea7f7b5cf362b88c` combines published `4737d64` with `edbe9e8` (runtime help/argument validation before initialization) and `3f3029b` (synchronize Stardew menu viewport before Draw). All46 other incoming files retain exact bytes; the common controller preserves incoming Flow resource exclusion and capture-error shutdown. [The full Q01 report](Q01_WINDOW_VIEWPORT.md) separates before/after alpha.36 evidence, actual native window observations, retained failures and this new combined candidate. Evidence belongs to `${HOME}/Developer/Worktrees/hatifect/345f/Hatifect`.

- C `run-mo6_ehgs`: PASS1,317 .NET +351 Python. G `run-oxunrnku`: PASS1,555 .NET +351 Python. Actual TRX audits agree and have no failures/skips/error outcomes.
- P: PASS90 CA tests,44 exact projection files,8 alpha.37 packages,2 CA DLLs without UI duplication; retained `hatifect-ui-ca-isolated.8ou4iayd`. Logs and hashes are under `artifacts/q01-window-observation/`.
- Fresh standalone `6b68ae01-5911-41fc-9dc5-ce938696d34d`: PASS3, correct 1280×720 native menu bounds and inspected complete frame. UI/CA aggregate `ac90367e-7f6a-4aab-b92a-403eeb970efc`: PASS28, actual native window inspected,620 frames, p95/p99 0.055584/0.360876 ms,4572.052 B/frame, zero measure/arrange cache misses. UI fingerprint `4fc0b3c304ca342338cb28c32859439173ceea60928d6873c97753ffdc2ebab2`.
- Fresh fake Flow route `52984fa5-522e-417d-adea-ce9097420b81`: PASS6; save isolation `1baed3a9-7913-43da-8db5-df9ecea14bd0`: PASS7. Flow fingerprint `24750ff64fc5d223423bc246b13c8360aa1c3db96d8451be11520396582d2ec3`, with its own inventory algorithm. The mistyped isolation ID was rejected before game submission and is retained separately.

The two bounded fixes are DONE. Q01 remains IN_PROGRESS: native clipping is confirmed in this Mac's borderless mode and absent in isolated windowed1280×720, but no game-engine fix is claimed. Stable rendered EN/RU × scale/theme states and physical input remain unverified. These Q01 steps can continue independently of the UI owner's U03 host integration; F20 completion and its earlier measured source candidate are preserved.


## Combined alpha.38 Terminal identity and Q01 viewport fixes

UI checkpoint [`8a3545d`](https://github.com/ihatectf/Hatifect/commit/8a3545d) is integrated with published `d3e9092` (Q01 implementation `3f3029b` / `edbe9e8`, combined alpha.37 `aa04181`). All six incoming non-Markdown files retain exact owner bytes, all alpha.38 compiled UI/consumer changes remain unchanged, and both appended STATUS histories survive the sole merge conflict. The first-draw viewport synchronization and help-before-initialization fix are preserved alongside F20.

- C `./tools/hatifect-check`, `run-rgifh2v6`: **PASS1323 .NET +351 Python**.
- G `./tools/hatifect-check --platform`, `run-56ru38nr`: **PASS1561 .NET +351 Python**. Actual TRX counters and every individual outcome were verified for both gates; no failures/skips. The explicit x64 SDK and command-local build GC workaround remain as documented above.
- P `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**,44 exact projection files,8 alpha.38 packages whose DLLs match current producers,3 isolated assets files and2 exact CA deployment DLLs with no UI duplication. Retained workspace `hatifect-ui-ca-isolated.9qhv9tqo`; log and hashes: `artifacts/u03-alpha38-q01-integration/`.
- Independent bounded source review: **PASS**, including all32 candidate hashes. Changed viewport can recompose before Draw; no action Pump or Execute was added there. Future action completion dispatch must remain a separate Update operation. Existing shell return fencing is not a claim of transactional validation/platform callbacks.

This integration starts no game and makes no new runtime/visual claim. The Q01 native-window and runtime observations belong to the original alpha.36/37 candidates documented above. Full U03/Q01 remain IN_PROGRESS. Next UI step: guarded action request admission and independently retired binding, followed by production host/input/legacy binding and isolated U03 RUNTIME; U04 remains available by dependency but unimplemented.


## U03-b action admission and independent retirement

Implementation [`6444272`](https://github.com/ihatectf/Hatifect/commit/64442721bd724c2e7546bebf6568c829114690e8), base `44505c6`, alpha.39. Owning implementation is private Runtime/Actions/UiActionExecution; immutable public descriptions, existing legacy TryExecute and frozen surface signatures are unchanged. A binding can now retire independently before cancellation callbacks, clearing active/queued submissions and its result subscriber exactly once; its live siblings continue. Every admission, availability and Pump path checks both binding and dispatcher retirement. Retired entries remain bounded by the existing4096 dispatcher capacity until the host owner is disposed; no new history or registration mutation during Pump.

`InvokeCaptured` performs refusal guards before reading a request snapshot, captures once on the owning thread, checks retirement after the callback, then admits the immutable snapshot. Capture failure returns the original typed Failure without domain effects or a completion callback; it retains an earlier active operation and FIFO. The existing direct Invoke path allocates no capture closure. `RefreshAvailability` only reads eligibility and compares state/reason fields; it cannot deliver a completion or start queued work. CanInvoke uses captured domain availability plus actual concurrency/capacity. Equivalent newly allocated domain reasons do not falsely invalidate visuals. Recovery from disabled/availability failure restores Available, while ordinary domain terminal state remains observable.

Focused tests extend only ActionExecutionTests, using existing real dispatcher/task helpers. No new test dependency/scaffolding or weakened baseline assertions.

| Requirement | Evidence |
|---|---|
| One binding retires idempotently, refuses future effects and preserves sibling actions | `RetiringOneBindingBlocksItsEffectsWithoutRetiringOtherActions` |
| Presentation gets reason/code/field and disabled/fault recovery without effects | `AvailabilityRefreshReportsReasonsAndRecoversWithoutDomainEffects` |
| Availability refresh cannot deliver an already completed background task | `AvailabilityRefreshDoesNotDeliverBackgroundCompletion` |
| Unavailable, busy, full and retired admission never captures a request; all three concurrency policies | `RequestCaptureRunsOnlyForAdmittedInput` (three cases) |
| FIFO request uses the admitted draft, not a later form value | `QueuedRequestRetainsExactlyOneAdmissionSnapshot` |
| Capture error is observable and preserves an existing active operation | `RequestCaptureFailureIsObservedWithoutReplacingAnActiveOperation` (idle/running) |
| Capture callback cannot bypass retirement/reentry guards | `RequestCaptureCannotBypassRetirementOrReentry` (two cases) |
| Availability callback retiring binding or dispatcher cannot publish enabled state | `AvailabilityRefreshCannotPublishAfterBindingOrDispatcherRetirement` (two cases) |

Original existing-API regression `run-ivilw1u2`: **FAIL1/303**,302 baseline PASS; a separately retired binding accepted a new Invoke. Corrected scoped command `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, `run-j4ur_lil`: **PASS315/315**,13 new cases. Commands use the documented explicit x64 SDK and command-local build GC workaround. Final source review and C/G/P follow on the version-aligned candidate.

Full U03 remains IN_PROGRESS. Production scene/input/legacy binding, owning Update pump, per-publication retirement subscriptions, successful reload/explicit reopen/save-switch fencing and isolated RUNTIME acceptance remain next. Runtime/visual is NOT_APPLICABLE to this private execution seam; no alpha.39 game run or broad host lifecycle completion is claimed.


Final alpha.39 evidence on unchanged compiled sources: C `./tools/hatifect-check`, `run-_0iw7xvr`, **PASS1336 .NET +351 Python**; G `./tools/hatifect-check --platform`, `run-gmofyltu`, **PASS1574 .NET +351 Python**. All actual TRX counters and individual outcomes agree, with zero failures/skips. P `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**,44 exact projection files,8 alpha.39 producer DLLs,3 isolated assets files and2 exact CA deployment DLLs without UI duplication. Retained workspace `hatifect-ui-ca-isolated.p2zhc5rp`; log and hashes: `artifacts/u03-action-admission/`. Independent source/actual narrow TRX/22-file hash review is **PASS**, no open finding. The newly added guards do not allocate on the stable path by source inspection; no new allocation benchmark is claimed. Next: prepare/accept host scene state before committing action ownership, then production binding and isolated U03 RUNTIME.


## U03-b host scene acceptance

Base `3fb56b9`, alpha.40. Owning layer is private Runtime. Previously Update assigned layout before interaction callbacks and scene/frame before accessibility callbacks; an exception left a partially published host. It now prepares layout, a shallow interaction candidate, frame, accessibility and synchronized collection requests locally, then copies accepted state without external callbacks between assignments. The original input session object remains stable, including its offscreen logical focus hint. No public action/surface signatures or persistence format change; all 17 package/version/consumer authorities advance together.

During preparation, nested Update/refresh/scroll/focus and direct input mutations refuse before effects. A failed layout attempt restores only the virtualizer's logical predecessor references from the accepted scene, including a collection removed by the candidate. Keyed measurements may become cold after failure. Successful Update adds one constant-size interaction copy and a small active-collection request map when layout changes; it does not clone the size-N adaptive height index or measurement cache. Existing steady Render allocation and layout-reuse regressions remain unchanged.

| Requirement | Evidence |
|---|---|
| Early and late callback failure retain exact scene/layout/frame/accessibility/input/snapshot/LastUpdate; retry succeeds | `FailedCandidateRetainsTheAcceptedSceneAndInteraction` (two cases) |
| Callbacks see accepted snapshots; nested Update/refresh, direct focus/text/Submit/Reconcile refuse before effects; later input works | `CandidateCallbacksSeeAcceptedStateAndCannotReenterMutation` (six cases) |
| Rejected reorder or structural removal cannot replace the accepted predecessor order; focus/selection and repeated refresh remain correct | `RejectedCollectionCandidateCannotReplaceAcceptedPredecessorOrder` (Uniform/Adaptive × reorder/removal, four cases) |
| Render-only geometry reuse and bounded steady rendering remain valid | Existing `RenderOnlyVisualStateReusesLayoutButRebuildsFrame` and `ColdPredecessorSearchReportsTheWorstRemovalTransitionAndLeavesSteadyRenderingBounded` |

Original reproduction `run-8zhv4pf4`: **FAIL 2/317**, 315 baseline PASS; early failure exposed changed layout, late failure changed scene. Those two tests remain unchanged. First correction `run-asd6ttzk`: PASS 317. Additional test setup mistakes are retained in `run-jm8n369j` (FAIL 5/327) and `run-rgselaxx` (FAIL 10/327): action insertion changed focus order and an overscan item was outside clip. Tests now select explicit scene control IDs and navigate to a bounded logical collection target. Production was not changed to satisfy those setup assumptions. Final scoped command `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, `run-rfis2wif`: **PASS 327/327**, 12 new cases. Explicit x64 SDK and command-local build GC workaround apply as documented above.

Static source pairing identifies all four production files in existing Runtime suites. The task-local parser setup initially failed on a personal cache location; the corrected artifacts cache still recorded one unrelated UiSlotProjection download timeout. That file's heuristic result is inconclusive; this is not executed coverage. Test research/plan and parser evidence remain in ignored task artifacts. Independent review and final C/G/P follow on the version-aligned candidate.

Full U03 remains **IN_PROGRESS**. This prerequisite covers Runtime.Update scene acceptance, not all scroll/refresh transactions or host liveness/generations. Terminal validation callbacks, disposal/reopen fences, production action binding, owning Update pump and isolated U03 RUNTIME remain subsequent work. Runtime/visual is NOT_APPLICABLE to this private prerequisite; no alpha.40 game result is claimed.


Pre-integration C `./tools/hatifect-check`, `run-fw16etnx`: **PASS 1,348 .NET + 351 Python**, all actual TRX outcomes/counters verified. Independent bounded source/12 new cases/original reproduction/26 hashes review is **PASS** with no open defect. Platform measurement exceptions were reviewed through the common preparation catch, without a dedicated throw regression; no new candidate-Update allocation benchmark is claimed. Incoming published Q01 `1872c7a` will be integrated before final C/G/P; these gates will attest the combined alpha.40 sources.


## Q01 rendered matrix and alpha.39 integration

The bounded [rendered locale/scale checkpoint](Q01_RENDERED_MATRIX.md) is **DONE**. Implementation [`ae2f829`](https://github.com/ihatectf/Hatifect/commit/ae2f829ec0be02f302df522247d20dd8e557f825) replaces same-tick settings readback with two consistent completed frames per EN/RU × 75/100/125/150% × Dark state, eight screenshots and verified restoration before aggregate continuation. It also prevents the full-viewport native menu resize formula from producing `int.MinValue`. The initial real matrix FAIL and subsequent successes remain attached to their actual candidates.

Integration [`b3265fa`](https://github.com/ihatectf/Hatifect/commit/b3265fa3b7f23b2794588f56afcc51d59148859f) preserves published alpha.39 `3fb56b9`, including Terminal model identity and private action admission/retirement. All 25 incoming files matched that exact parent before these Q01 documentation additions. All 30 compiled/tool inputs remain unchanged through final checks. No public API or persistence change is introduced by Q01.

- C `run-p1av7g44`: **PASS 1,350 .NET +351 Python**. G `run-4zcdaasw`: **PASS 1,588 .NET +351 Python**. Actual counters and individual outcomes in all 17 TRX files agree, with no failures/skips. Runtime suite includes 329 cases and 14 new rendered-state regressions.
- P: **PASS 90**, 44 exact projected files, 8 alpha.39 packages matching producer DLLs, 3 isolated assets files and 2 exact CA DLLs without UI duplication. Retained workspace `hatifect-ui-ca-isolated.lifvyszj`; source/package/test audit is under `artifacts/q01-rendered-matrix/`.
- Fresh matrix `bd277a1d-2e9c-4e09-87f1-cb36fc304417`: **PASS 8**, all eight completed-frame PNGs inspected, Cyrillic and controls visible, menu bounds match scaled viewport. Aggregate `d5ee5e15-7a23-4762-8446-9fadfa587bf0`: **PASS 28**, matrix restoration precedes CA and 620 measured performance frames; p95/p99=0.041501/0.285708 ms, 4541.458 B/frame, zero measure/arrange misses. UI fingerprint `b5353b9f1ddf22c86606a49a9a523792e2ac701c520802577abec83f4d5b3098`.
- Actual I/O fault `aec3a3bc-015b-411b-a73a-14b8a0b8d778`: expected **FAIL 8** in 9.536 s. The original capture error survives the secondary diagnostic-write error; game exits 0 with no teardown errors and executor returns Ready. The unavailable final diagnostic JSON is not claimed valid; its temporary file and logs retain the cause.
- Fresh fake Flow route `5e1c0a65-06bd-48df-b7eb-f13af7dfbbfd`: **PASS 6**; save isolation `f143d416-1db1-4508-928e-ffe4f5283ba2`: **PASS 7**. Their separate Flow fingerprint is `4540912142242796c446029ca28537340391fda9d38cf04ed4701d89998dfb88`. Earlier real-chest F20 evidence keeps its owner/source identity.

C/G/P/prepare use only the documented command-local build GC setting; executor/game use ordinary GC. Normal Mods, real saves and golden assets are unchanged. Full Q01 stays **IN_PROGRESS**: native/physical input is still separate, and the Mac borderless clipping limitation remains. This fixture does not certify complete product localization or every UI/Flow/CA state. Next ready Q01 work is native-input observation with OS-injected and physical hardware events distinguished; full U03 host integration remains with its existing owner.


## Alpha.40 integration and Q01 final diagnostic guard

UI scene acceptance implementation is [`22935f3`](https://github.com/ihatectf/Hatifect/commit/22935f3800a039dde4cae646e3d5c1d126e2c7f2). The merge includes published Q01 `1872c7a`; all eight incoming source/tool files initially matched their owner exactly, all 23 non-Markdown alpha.40 inputs remained unchanged, and both appended STATUS histories were retained. The initial combined C `run-wxltx7bi` passed 1,362 .NET + 351 Python; G `run-387k7wl1` passed 1,600 .NET + 351 Python, including 341 Runtime cases. These static results preceded the following validator correction and are not final evidence for it.

Independent review found a P2 in Q01 finalization: the controller could record all eight matrix checks PASS, then fail to publish diagnostics/runtime.json; the first-verdict ledger correctly kept the earlier observations, but finalization accepted the report without checking the terminal diagnostic outcome. The retained first-PNG fault from alpha.39 does not cover this late path. An actual temporary-directory reproduction (`late-write-reproduction.log`) returned 0/PASS instead of required 1/FAIL. That test remains unchanged.

The correction belongs to tools/live-harness/validate.py. UI finalization now requires published diagnostics matching the request, scenario, protocol, timestamp and process identity, with an explicitly null terminal error. Standalone and aggregate matrix finalization additionally require all eight ordered states, restored settings, coherent locale/scale/theme/geometry/flags and their contained PNG files. Missing, partial, malformed or foreign diagnostics produce a separate structured HARNESS-RUNTIME-DIAGNOSTICS failure before overall PASS. Prior check verdicts and the report remain intact. Non-matrix scenarios may validly finish with closed menus; smoke/Flow finalization is unchanged. PNG signature/presence checks do not replace visual inspection. No compiled UI/consumer source or public signature changes are needed for this correction.

| Requirement | Evidence |
|---|---|
| All matrix checks PASS plus late final JSON write failure must still fail the run | `test_late_diagnostic_write_failure_cannot_finalize_passed_matrix` |
| Missing/temporary/malformed/foreign/stale/error diagnostics cannot finalize PASS | `test_ui_finalization_rejects_invalid_or_foreign_diagnostics` (15 subcases) |
| Matrix completeness, observations, geometry and PNG output apply to standalone and all | `test_ui_finalization_requires_complete_rendered_matrix_including_aggregate` (23 defects × 2 scenarios) |
| Correct matrix/all and closed non-matrix UI complete successfully; fallback request identity works | `test_ui_finalization_accepts_current_diagnostics_and_restored_matrix` (3 subcases) |

Focused `python3 -m unittest tools.tests.test_live_harness`: **PASS 50 tests**, log `artifacts/u03-alpha40-q01-integration/finalization-focused.log`. Canonical `./tools/hatifect-test tools`, `run-bi0w010k`: **PASS 355 Python**, zero failures/skips. Stock source pairing parsed 16 Python production / 22 test files without warnings; validate.py pairs with the existing test file. Original valid alpha.39 matrix and aggregate diagnostics also pass the new guard read-only; their source and runtime identity remain alpha.39. Final C/G/P, correction review and a fresh isolated matrix/late-only I/O fault follow before publication.


The correction review also reproduced an uncaught RecursionError from a 200 KB deeply nested diagnostic JSON. The actual command_finalize reproduction is retained in `deep-json-reproduction.log`; the helper now converts that parser failure into the same structured diagnostic FAIL. The added raw-file subcase passes; final focused log `finalization-focused-final.log` is **PASS 50 tests**. Independent source/exact patch and hash/evidence review closes both P2 findings. Initial correction C `run-dvjtqmxa` passed 1,362 .NET +355 Python before this two-line parser-boundary correction; final gates below attest the corrected files.


Final combined alpha.40 static evidence: C `./tools/hatifect-check`, `run-hv4jm5o1`, **PASS 1,362 .NET +355 Python**; G `./tools/hatifect-check --platform`, `run-yqtccs1t`, **PASS 1,600 .NET +355 Python**. All 17 actual TRX files have matching total/executed/passed and individual successful outcomes, zero failures/skips. P `./tools/hatifect-isolated-ui-ca --keep`: **PASS 90**, 44 exact projection files, 8 alpha.40 packages whose DLLs match the current producers, 3 isolated assets files and 2 exact CA deployment DLLs without UI duplication; retained workspace `hatifect-ui-ca-isolated.gsbwsxh8`. Full source/contract diff and 37 candidate hashes agree with the independently reviewed files. Evidence is under `artifacts/u03-alpha40-q01-integration/`. The combined implementation is committed locally before fresh runtime so requests have a concrete source HEAD; runtime acceptance remains pending here.


Fresh runtime now attests exact integration [`9756790`](https://github.com/ihatectf/Hatifect/commit/9756790604bfc85039d05f86903e781d85661f6a), alpha.40, fingerprint `3904aef41f4edc2e18fc834b39993a23bda01a5feea2f05b4775da3ae7b58320` (`sha256-runtime-v2`), Stardew1.6.15 build24356 / SMAPI4.5.2. Raw artifacts are under `/private/tmp/hatifect-ui-next-ten-slices/artifacts/runtime/`; the request/source/process/file-hash index is `artifacts/u03-alpha40-q01-integration/fresh-runtime-evidence.json`.

| Scenario | Request | Actual outcome |
|---|---|---|
| `./tools/hatifect-ui-test semantic.locale-scale-theme` | `a88ff5f5-1dbd-4261-9f73-67e60538890b` | **PASS 8**, 14.542 s, eight states, sixteen matrix PNGs, restored settings and null terminal error |
| `./tools/hatifect-live-runner ui semantic.locale-scale-theme` with directory only at final diagnostic path | `12077b0c-9b88-4a3a-8cfa-12072851bc8b` | Expected **FAIL / HARNESS-RUNTIME-DIAGNOSTICS**, 12.133 s; all eight earlier host checks remain PASS, every matrix PNG exists and restoration completed |
| `./tools/hatifect-live-runner ui all` | `66428992-7d9f-4517-b410-77ed3fe2d451` | **PASS 28**, 22.529 s; restoration precedes CA and performance |

The late-only fault reaches `WriteDiagnostics` from `CaptureCompletedFrame`, after all matrix checks. Its `runtime.json.tmp` retains the original `System.IO.IOException: Is a directory`; the unavailable final JSON is not claimed valid. All three games exited0 with empty teardownErrors; executor returned Ready and the executor started by this task was then stopped normally through its own PTY. The initial aggregate `ea7784ad-b491-4fd1-93d9-bebdf997b5fe` is retained as **BLOCKED / HARNESS-ENV-MODS**, before game launch: this worktree lacked Pathoschild.ChestsAnywhere. Eighteen distribution files of verified CA1.30.1 were copied read-only from the Q01 isolated fixture into this worktree's isolated Mods, without copying its config; a fresh request above passed. File hashes and paths are in `ca-fixture-copy.json`.

All eight standalone completed-frame images were inspected: EN/RU probe text, navigation and focus border are visible, including Cyrillic and the bottom line. Backbuffer is1280×720, viewports are1707×960 /1280×720 /1024×576 /854×480 and native menu origin is0,0. Accepted frames are3,5,7,9,11,13,15,17. This is the bounded diagnostic probe, not complete product localization or physical input. Only own isolated startup/default options were changed to the documented windowed1280×720 configuration; original bytes and before/after hashes are retained under `original-isolated-window-config/` and `isolated-window-config.json`. The known macOS borderless limitation remains.

Aggregate measured620 frames: p95/p99 UI-thread cost0.045625/0.300335ms, steady allocation4545.045B/frame, measure/arrange miss ratios0. No build or external screenshot capture overlapped this sample; a small read-only audit of the already completed preceding requests ran while aggregate was active. This is an observed budget result, not a new controlled benchmark of candidate Update. Check/prepare commands used only the documented command-local GC workaround; executor/game used ordinary GC.

The bounded alpha.40 scene-acceptance prerequisite and Q01 final-diagnostic correction are complete, with C/G/P/source review and the fresh outcomes above. Full U03/Q01 remain IN_PROGRESS. Next UI step is private root/portal retirement and Terminal validation callback fencing, then typed production binding and owning Update pump. Original Q01 alpha.39 and real Flow F20 evidence retain their source identities; no new full U03, physical-input or Flow runtime acceptance is claimed here.

Independent fresh-evidence review: **PASS**, no findings. Reviewer checked all three source identities, 99 indexed artifact hashes, actual late-only failure/first verdicts, aggregate27 host checks plus the performance budget, separate prelaunch BLOCKED and all18 CA distribution hashes. All32 non-Markdown candidate inputs remain unchanged after the final checks.


## U03-b host retirement and validation fences

Base `c1de257`, alpha.41. Runtime owns the new private active flag and accepted scene version. Retirement is idempotent, may occur during Update preparation and makes that candidate uncommittable. Layout/frame/accessibility phase boundaries check liveness; future retained root input/update/refresh refuses before effects and retired Render performs no platform work. Accepted snapshots remain inspectable. Portal Deactivate retires root and all children before clearing its stack; Close retires exact descendants, and an action closing its parent does not trigger post-action composition on a removed child. Present checks the original owner instance/version/node and duplicate registration after construction and initial focus callbacks; a successful nested registration is retained.

Terminal captures the root's accepted version before composition and checks liveness/version before and after validation. Successful nested Open, same Cached reopen or Recompose wins over its outer stale candidate. Recompose/OpenSection/interaction/route paths use this fence. Construction failure deactivates a created host before releasing the shell. The existing Stardew host Retire path now calls Session.Deactivate before disposing its owner, matching its Dispose path. Public surface signatures and persistence remain unchanged; all17 package/version/consumer authorities advance together.

Host input uses a private UiActionDefinition overload to check its owner before availability and again before Execute. This closes the reproduced case where CanExecute disposed the host but returned true. Public direct TryExecute and headless input retain their original behavior; this is not the typed legacy adapter or production async binding yet. Ordinary admitted actions may still close their host and return their domain result.

| Requirement | Evidence |
|---|---|
| Validation disposal retains accepted scene/layout/frame/accessibility and retires retained root input | `ValidationDisposalCannotPublishThePreparedTerminalFrame` |
| Nested same Cached / other section Open wins over outer validation | `ValidationCannotOverwriteANestedAcceptedTerminalFrame` (2 cases) |
| Open, interaction refresh and route validation cannot publish after disposal | `ValidationRetirementFencesEveryTerminalEntryPoint` (3 cases) |
| Nested Recompose remains accepted on those three entry paths; later retry works | `NestedRecompositionDuringValidationKeepsItsAcceptedFrame` (3 cases) |
| Disposal during actual platform measurement prevents validated Update acceptance | `MeasurementDisposalCannotAcceptTheValidatedTerminalCandidate` |
| CanExecute disposal prevents the legacy domain effect | `AvailabilityDisposalPreventsTheLegacyActionEffect` |
| Idempotent retirement blocks retained root Submit without effects/snapshot replacement | `DeactivatedHostRefusesRetainedRootInputBeforeEffects` |
| Present cannot register after owner retirement in preparation | `PresentCannotRegisterAfterOwnerRetirementDuringPreparation` |
| Closing the exact portal during candidate measurement prevents Update commit | `ClosedPortalCannotCommitItsPreparedUpdate` |
| Retired root update/refresh/scroll/Present/unhandled input refuse and Render is inert | `RetiredRootCannotUpdateRefreshOrDrawThroughRetainedReferences` |
| Root Update, parent close/reopen or duplicate registration during Present preserves nested state and supports retry | `PresentCannotOverwriteAnOwnerOrRegistrationChangedByItsCallback` (3 cases) |
| Nested action closes its parent/descendants, skips retired recomposition and preserves root input | `ClosingAParentFromANestedActionSkipsRetiredCompositionAndPreservesRootInput` |

Original existing-API reproduction `run-okppoput`: **FAIL 6/347**,341 baseline PASS. Those six cases remain unchanged. First fix `run-rymyihaw`: **PASS 347**. Expanded regression `run-7gmz0_hf`: **FAIL 1/360**, exposing CanExecute-disposal followed by Execute. Final scoped command `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, `run-uj1x9r24`: **PASS 360**,19 new cases and no failures/skips; actual counters and individual outcomes retained in `artifacts/u03-host-retirement/narrow-trx.json`. This scoped run precedes only the17 version authority updates. Full C/G/P and independent final review follow on alpha.41.

Existing stock source pairing was reused for the five Experience/Runtime targets and established suites; it is a static heuristic, not executed coverage. The one-line Stardew retirement wiring is additionally checked by the full platform build and source review. Research/plan and the assertion/gap review are recorded in ignored `.testagent/`. Guard calls/version increments allocate no new steady input/Update storage; Present validation adds bounded cold checks and retirement traverses only its existing portal stack. The existing render-allocation regression is retained; no new allocation benchmark is claimed.

Full U03 remains IN_PROGRESS. This slice fences retirement and Terminal validation; it does not make every input/scroll/draw callback a transaction, provide thread/generation ownership for all hosts, or wire typed actions, Update pump, publication/reload/save-switch lifecycle. Those remain subsequent U03/R01 work. Runtime/visual is NOT_APPLICABLE to this private prerequisite; the alpha.40 Q01 game runs retain their exact source/fingerprint and are not alpha.41 acceptance. Next ready UI step is per-host typed/legacy action binding with owning Update pump and retirement subscriptions.


Final alpha.41 gates on unchanged implementation: C `./tools/hatifect-check`, `run-17yde5x5`, **PASS 1,381 .NET +355 Python**; G `./tools/hatifect-check --platform`, `run-bdp06td1`, **PASS 1,619 .NET +355 Python**. All17 actual TRX suites have consistent total/executed/passed and individually Passed outcomes with no failures/skips. P `./tools/hatifect-isolated-ui-ca --keep`, retained workspace `hatifect-ui-ca-isolated.m182hsyb`: **PASS 90**,44 exact projected files,8 alpha.41 package DLLs matching current producers,3 isolated consumer assets files and2 exact CA deployment DLLs without duplicate UI. Evidence/hashes are under `artifacts/u03-host-retirement/`; the documented explicit x64 SDK and command-local build GC workaround apply.

Independent bounded review is **PASS**, no open findings: all28 candidate file hashes and patch postimages, the four actual scoped TRX runs and every new case were verified. This completes the bounded retirement/validation prerequisite; full U03 stays IN_PROGRESS. Runtime/visual and локальная среда host audit are NOT_APPLICABLE to this slice. Next is the prepared per-host action registration map, typed description binding, renderer/input/accessibility execution-state consumption, then Update/lifecycle wiring and isolated U03 acceptance.

Implementation commit: [`8a45917`](https://github.com/ihatectf/Hatifect/commit/8a45917a040813a508ac70d4a3d71a73261468e8). Source/test/package authority bytes are the checked alpha.41 candidate; this subsequent change adds only its stable roadmap reference.

## U03-b legacy admission scene version repair

Implementation [`ae8ef7d`](https://github.com/ihatectf/Hatifect/commit/ae8ef7d14a15f614bb704d1fc89bb62ae3199fd7), base `6ef1b12`. A subsequent independent review found a remaining P2 in alpha.41: legacy CanExecute could successfully open another section, reopen the same Cached model, or Recompose; the old button then executed against an already replaced scene. The prior retirement guard checked active/preparing state but did not retain the accepted scene version across availability.

The private Experience admission overload now captures the Runtime owner's AcceptedVersion before availability and checks it again before the domain effect. Capture retains the existing active/preparing/disposal guards. A replacement is explicitly rejected while keeping its newly accepted scene intact. Public direct TryExecute and headless input preserve their existing behavior. The guard delegate is still created once per host; the dispatch path uses value-type version snapshots with no additional allocation by source inspection. This is not a new allocation measurement.

| Requirement | Evidence |
|---|---|
| CanExecute opening another section cannot execute the previous button | `AvailabilitySceneReplacementPreventsTheLegacyActionEffect("other-section")` |
| Reopening the same Cached model invalidates the previous admission | `AvailabilitySceneReplacementPreventsTheLegacyActionEffect("same-section")` |
| Recompose also invalidates admission while preserving the new scene | `AvailabilitySceneReplacementPreventsTheLegacyActionEffect("recompose")` |
| All three replacements retain the accepted scene, refuse the old effect, and permit a later current action exactly once | Assertions in all three cases above |
| Existing disposal and preparation guards remain effective | `AvailabilityDisposalPreventsTheLegacyActionEffect` and the existing six preparation-reentry cases passed in the same suite |

Original `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, `before/run-gqlikicg`: **363 executed,360 PASS,3 FAIL**,0 skipped; all three new cases observed expected effects0 versus actual1. The unchanged test cases after the fix, `after/run-fl2meypr`: **363/363 PASS**,0 failed/skipped. C `./tools/hatifect-check`, `c/run-vjgbyj_l`: **1,384 .NET +355 Python PASS**. Independent review verified the four-file diff and actual before/after TRX, with no remaining finding in this bounded repair. Evidence is retained in `/private/tmp/hatifect-flow-alpha41-action-fence/artifacts/flowline-action-fence/`.

G `./tools/hatifect-check --platform`, `g/run-f04xy10t`: **1,622 .NET +355 Python PASS**. All actual C/G TRX counters and individual outcomes agree, with no failures/skips. P `./tools/hatifect-isolated-ui-ca --keep`, retained workspace `hatifect-ui-ca-isolated.25thclnj`: **90/90 PASS**,44 byte-exact projected source files,8 packages whose DLLs match the current producer outputs,3 assets files resolving UI exclusively from the new isolated consumer cache and2 exact CA deployment DLLs. Source hashes, actual TRX hashes and the package/deployment audit are recorded in `candidate.json`; the before/after logs and reproducing test patch remain alongside it. Commands use explicit x64 .NET 8 SDK/.NET 6 runtime and command-local `DOTNET_gcConcurrent=0` for build/test processes.

Package version authorities are unchanged in this repair branch; its private feed is a source-validation candidate, not a new published alpha.41 package release. UI owns integration with the next aligned package candidate and has accepted the three unchanged regression cases. Runtime/visual and локальная среда host audit are NOT_APPLICABLE to this private synchronous repair. Full U03 remains IN_PROGRESS and still requires production binding, Update/lifecycle wiring and isolated runtime acceptance. F12 remains dependent on U04; this fix does not change that dependency.


## U03-b production action binding and captured presentation

Alpha.42 implementation [`790288b`](https://github.com/ihatectf/Hatifect/commit/790288b1e1f680e264437037ca52a915b7764ce1), base `6ef1b12`. Experience adds `UiAction<TRequest,TResult>.Bind(capture, completed, owner)` returning an immutable existing action definition. The generic internal factory retains request/result types without reflection, dynamic, boxing request payloads or an Experience dependency on Runtime. The same exact definition/scene can serve two hosts with independent execution state. Consumers retain the definition across ordinary composition; a fresh definition with the same stable ID intentionally replaces the previous execution.

Runtime prepares a map of at most4096 current registrations beside the candidate scene. New registrations are detached, retained exact definitions reuse execution, and a failed candidate retires only newly prepared bindings. Scene commit installs the whole map before removed executions are cancelled. Registered membership immediately fences every removed sibling, even when the first cancellation callback uses a retained old resolver. The map may be replaced from a completion while Pump enumerates its previous immutable map. No retired-registration history grows with repeated replacements. Publication retirement sets its common disposed fence before callbacks; subscriptions detach on discard, replacement and owner disposal. Wrong-thread retirement/input refuses before mutation.

Admission checks the accepted frame version after availability, request capture and RestartLatest cancellation callbacks. This incorporates FLOWLINE's three unchanged Terminal cases from `ae8ef7d` for other-section/same-section/Recompose during legacy availability. UI input preserves the private Func<long> owner seam and explicit stale-frame error. Root binding uses the stronger guard before effects; failures from a legacy admitted effect become observed unit results while public ownerless TryExecute retains direct synchronous exceptions. Already committed domain effects are never rolled back by UI cancellation.

Input, rendering, accessibility and explicit diagnostics consume captured host eligibility. Legacy CanExecute remains in the existing candidate focus/accessibility validation phases, staged separately from retained execution, and is read once on admission. Typed availability failures are captured as action failures. Root PumpActions refreshes state on its owning thread, even without a dirty model; rendering/composition/diagnostics never pump completions. The root seam is implemented and behaviorally tested here; actual Stardew Update ownership is the next integration step.

Disabled opacity comes from the shared theme token. Scene reconciliation compares the actual host button state, so a changed Disabled-only recipe updates a running action without new measurement. A staged legacy eligibility change also invalidates the frame with the same scene identity. Typed dynamic button states reject geometry recipes during composition. Existing legacy geometry recipes continue through composition/layout; between compositions only render properties change against accepted geometry. Policy caches retain at most16 state combinations plus16 legacy render projections and capture policy values, not a mutable composer or source.

| Requirement | Evidence |
|---|---|
| Shared description has independent hosts and callbacks return on the owner thread; Draw/diagnostics/Update cannot deliver completion | `SharedDescriptionHasIndependentHostStateAndOwningThreadCompletions` |
| Typed direct invocation requires a host and performs no request/effect | `TypedDescriptionRequiresAHostWithoutCapturingOrExecutingDirectly` |
| Failed scene preparation preserves exact accepted snapshots and old pending work; retry accepts the new action | `FailedScenePreparationKeepsPendingWorkAndRetiresOnlyTheCandidate` |
| Successful replacement/removal cancels old work without updating successor | `AcceptedReplacementOrRemovalCancelsOldWorkBeforeItCanAffectSuccessor` (2 cases) |
| Publication retirement fences siblings before cancellation reentry and preserves independent owners | `PublicationRetirementFencesAllBindingsBeforeCancellationCallbacksReenter` |
| Host retirement blocks retained bindings/input and late callbacks | `HostRetirementCancelsRetainedBindingsAndRefusesNewEffects` |
| Queue retains one admission snapshot in FIFO order across composition and rejects capacity overflow before capture | `QueuedInputCapturesDraftOnceAndPreservesAdmissionOrderAcrossRecomposition` |
| Availability reason/field, provider failure, capture failure and recovery remain observable without unauthorized effects | `AvailabilityAndRequestFailuresAreObservedWithoutDomainEffects` |
| Legacy host admission reads availability once, observes effect errors and preserves ownerless direct errors | `LegacyAdmissionReadsAvailabilityOnceAndObservesEffectFailure` |
| Completion may accept a replacement map safely during Pump | `CompletionCanAcceptAReplacementWhileDispatcherEnumeratesItsPreviousMap` |
| Availability/capture frame replacement prevents the old typed effect | `TypedAdmissionCannotExecuteAfterACallbackAcceptsAnotherFrame` (2 cases) |
| Running button uses disabled primitives/accessibility/diagnostics and keeps measured geometry | `RunningPresentationUsesHostEligibilityAndRetainsMeasuredGeometry` |
|4095/4096/4097 boundary, full-set replacement and removal/reuse do not leak registration slots | `RegistrationCapacityIsCurrentSetBoundedAndFailedCandidatesCannotLeakSlots` |
| Failed initial preparation cannot reactivate a disposed publication | `FailedInitialHostCannotReactivateDisposedPublication` |
| Idle/unfinished action pumps retain frame/layout and do not allocate per tick | `UnchangedActionPumpsDoNotAllocatePerFrameAndKeepAcceptedFrame` (4 cases: idle/running × legacy enabled/disabled, at most64B across1000 measured ticks after warmup) |
| Wrong-thread retirement/invoke/input leaves accepted state usable | `WrongThreadRetirementOrInputCannotMutateTheAcceptedHost` |
| Restart cancellation replacing a frame cannot enqueue the stale request | `RestartCancellationCannotQueueInputFromTheFrameItsCallbackReplaced` |
| Removed siblings are fenced before the first cancellation callback | `ReplacingAllActionsFencesRemovedSiblingsBeforeAnyCancellationCallback` |
| Disabled-only recipe change updates running primitives without measurement | `ChangedDisabledRecipeUpdatesRunningPrimitivesWithoutMeasuringAgain` |
| Legacy staged true/false eligibility invalidates primitives with the same scene instance | `StagedLegacyEligibilityRebuildsTheFrameWhenTheSceneIdentityIsUnchanged` |
| Invalid typed Disabled/Focused geometry retains the accepted scene/pending action | `TypedButtonGeometryRecipeIsRejectedBeforeReplacingTheAcceptedScene` (2 cases) |
| Legacy Disabled/Focused geometry still applies through composition/layout | `LegacyGeometryStatesStillApplyThroughSceneComposition` (2 cases) |
| Live publication cannot retain a discarded/replaced/disposed binding | `RetiredBindingsReleaseTheirPublicationSubscription` (3 weak-reference cases) |
| Legacy Terminal other-section/same-section/Recompose during availability prevents effects | `AvailabilitySceneReplacementPreventsTheLegacyActionEffect` (3 unchanged FLOWLINE cases) |

Final scoped command so far: `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`, `run-iqs3dniu`: **PASS395**, zero failures/skips, on alpha.42. The32 new binding cases and3 imported Terminal cases pass along with the unchanged360 baseline. Actual TRX counters, individual outcomes and file hashes are indexed in `artifacts/u03-production-binding/narrow-trx.json`. Earlier red runs are retained:12 legacy preparation compatibility failures, new test-fixture/compiler/analyzer corrections, the missing common Disabled opacity and explicit focus setup for async completion. No old assertions were removed or weakened. One early idle sample allocated48B total across1000 ticks; the test checks absence of per-tick allocation with64B total allowance, not a claim of zero runtime overhead or full game-frame performance.

Review identified RestartLatest cancellation reentry, retired sibling membership and two state-render invalidation cases; corrections and targeted assertions are present. Final independent source review is PASS: all39 candidate hashes/postimages match and all395 actual TRX results passed. Pre-integration C `run-_65w6mb3` is PASS1416 .NET +355 Python; all7 actual TRX files were audited. Final C/G/P will be repeated after integration with current develop and FLOWLINE ancestry. All17 version/package/consumer authorities align to alpha.42. The stock source/test pairing was rerun with the existing task-only parser cache:146 sources,72 test files,116 paired and30 reported unpaired; no parse failures. UiHostActionBindings is paired with ActionBindingTests. UiActionBinding is heuristically paired by shared names with InspectorTests, PublishedCollectionTests and ScreenOwnershipTests; this does not prove its generic dispatch paths were exercised. UiButtonStateVisuals remains statically unpaired (suggestion: Hatifect.UI.Runtime/Scene/UiButtonStateVisualsTests.cs), although ActionBindingTests exercises its observable state/geometry behavior through the host. This is a static heuristic, not line/branch coverage. The initial default-cache run could not parse files and is retained as failed evidence. Research, plan and assertion/gap evidence live in ignored `.testagent/`.

The reviewer also found allocation on the disabled legacy polling branch. New false-legacy cases first reproduced80,000/80,048B across1000 ticks (`run-a5pt8973`, FAIL2/395); the same assertions pass after sharing an immutable disabled availability value. Full C `run-gpjzw3n9` was intentionally interrupted with exit130 before that fix and is not PASS.

Combined integration [`9b8f5e6`](https://github.com/ihatectf/Hatifect/commit/9b8f5e6beea9691c8d8644230a4075c8b57c4e24) preserves exact FLOWLINE `ae8ef7d`/`60c82ce` ancestry and Q01 `ba0db13`. FLOWLINE merge adds only its22 documentation lines to the reviewed implementation. Sixteen incoming Q01 postimages are exact; the compositor combines the independent empty-input hint with action-state rendering. Independent integration review is PASS; source identity is indexed in `artifacts/u03-production-binding/integrated-candidate.json`. Final C `run-vokns0vr` is **PASS1431 .NET +362 Python**; final G `run-darycblx` is **PASS1672 .NET +362 Python**. All17 actual TRX files have matching counters and individual Passed outcomes, zero failures/skips; integrated Runtime is409/409 including all32 binding cases. Final repeated P `./tools/hatifect-isolated-ui-ca --keep` is **PASS90**, retained workspace `hatifect-ui-ca-isolated.lndyn8g4`:44 exact projected files,8 alpha.42 package DLLs equal to the final producer outputs,3 assets files using the new isolated consumer cache and2 exact deployed CA DLLs. All90 actual TRX outcomes passed; `artifacts/u03-production-binding/final-p-audit.json` records the byte audit. The first P workspace `8spb85hh` passed90 tests but failed the additional DLL audit for Language/Semantics/Experience: those packaged assemblies included +9b8f5e6 in informational version while the final producer outputs used plain alpha.42. Its mixed metadata evidence is preserved in `package-mismatch.json`/`package-informational-versions.json` and is not promoted. The unchanged-source canonical repeat supplies the final consistent set. All build/check commands use explicit x64 .NET8 SDK/.NET6 runtime with command-local `DOTNET_gcConcurrent=0`; no game-frame PERF claim follows from the headless allocation tests.

Final pre-publication harness integration `9c2435d` additionally retains `74f8010`/`2e19ab9`, which reject linked runtime-evidence directories before copying isolated options. Both incoming Python postimages are exact; UI/package inputs remain identical to9b8f5e6. C `run-c_kg9d9v` is **PASS1431 .NET +363 Python**, all7 actual TRX audited. The earlier G/P above remain attributed to their tested UI candidate; this pre-launch Python guard introduces no platform or package change. Its original two failed subtests and separate owner review remain in the Q01 evidence.

Full U03 remains IN_PROGRESS. Next: actual root/portal owning Update pump in Stardew, ordinary and occluded hosts, explicit Open/Hide/successful reload/save-switch generation retirement, concrete typed consumer and fresh isolated RUNTIME acceptance. Failed reload must keep old pending work. Existing Hide is terminal for the surface; reopening creates a new owner, without adding reusable Show-after-Hide semantics. Runtime/visual game acceptance and host локальная среда audit are NOT_APPLICABLE to this private binding checkpoint; earlier alpha.40 game artifacts are not relabelled. The acceptance remains mandatory before full U03 DONE.


## Q01 native input and user-reported Search/Backspace defects

Implementation [`14db772`](https://github.com/ihatectf/Hatifect/commit/14db7729ff0f28147d9ac6c6d1e5a7db97defba6), alpha.40 integration `fe5b10b`, and native empty-result correction [`edad4b1`](https://github.com/ihatectf/Hatifect/commit/edad4b1e89c11860290b21ad74b5f07314e892a8) are documented in [Q01_NATIVE_INPUT.md](Q01_NATIVE_INPUT.md). GQ owns common integration, harness and acceptance; subsequent product UI/Flowline slices stay with their respective tasks. These bounded UI fixes were requested directly during runtime observation.

Inspector entries now have distinct stable IDs, so Tab focuses the real Search field. Empty fields render their semantic name without changing editable/accessibility value. The shared Stardew keyboard lease handles Back/Tab/Enter command characters once and rejects input from a lost screen/subscriber. No public signature or persistence change. The optional native fixture requires fresh correlated events, visible focused targets, exact request text and stable completed frames. Its geometry correction allows legitimate empty sibling lists while retaining root/field visibility and viewport checks; diagnostic snapshots remain bounded.

The original Search/hint/command regressions fail before their fixes. Final scoped Inspector PASS7, ScreenOwnership PASS18 and native gate PASS13 include empty-result editing, precise deletion, clipped/empty/overflowing targets and stale/duplicate/foreign input refusal. C `run-tqxp9awg` is PASS1,377 .NET +355 Python; G `run-ft4t9bl2` is PASS1,618 .NET +355 Python. All17 TRX files have matching actual successful outcomes and totals, with zero failures/skips. The exact changed-source hash index and logs are under `artifacts/q01-native-input/`.

Fresh native request `753737ca-a8b1-4e50-ade5-9f6d9e3619f0` confirms pointer, Tab and exact text against UI fingerprint `6843303da6f70fd35156a2218f26035d22af937082c6eb3a812fa145968984ca`. Their three completed-frame PNGs were inspected. CUA BackSpace visibly removes one final character but did not increment the required SMAPI key counter; after restoring the probe, no qualifying physical event was received before timeout. The overall request is therefore FAIL on `semantic.input.native.backspace`, 387.487s; PID85484 exits0 without teardown errors. Earlier incomplete requests retain FAIL. OS-injected results do not certify physical hardware. Full Q01 stays IN_PROGRESS; the next acceptance step is a complete physical Backspace observation with retained capture/provenance. Final P is PASS90 (`hatifect-ui-ca-isolated.naugiqjf`), with 44 exact projection files, eight alpha.40 packages matching producer DLLs, three isolated assets files and two exact CA DLLs. Fresh aggregate `43a63e05-9399-4f98-a8b6-e331685e0e70` is PASS28 on the same fingerprint, 32.134s; settings restored, terminal error null, owned PID88887 exits0 with no teardown errors. Its620 frames measure p95/p99 0.037958/0.271708ms and4356.981B/frame with zero measure/arrange misses. No build/CUA/visual audit overlaps the sample. All12 changed source/test/tool hashes still match `edad4b1` after final validation.


Q01 integration of independently published alpha.41: exact UI owner `6ef1b12` is merged as `9006131`, retaining26 incoming file postimages and both roadmap histories. Combined C `run-ha3cenlb` PASS1,396 .NET +355 Python; G `run-remfjoux` PASS1,637 .NET +355 Python; P `hatifect-ui-ca-isolated.mjczvzxq` PASS90 on eight exact alpha.41 packages. Fresh UI/CA aggregate `291142fa-49ac-4ce5-97e6-da7c6086ec1b` PASS28, fake Flow `d406faee-9c96-407b-85a1-6e0a1229224e` PASS6 and save isolation `b0a216f2-8275-4db3-ae95-95b534f638ab` PASS7. UI/Flow fingerprints, exact commands, source hashes and process results are in [Q01_NATIVE_INPUT.md](Q01_NATIVE_INPUT.md). The initial aggregate `bea6c556` remains FAIL because isolated pauseWhenOutOfFocus stalled load fade. Temporarily disabling that option only in the isolated fixture allowed the same DLLs to pass; both original config files were restored exactly. The passing620-frame sample records p95/p99 0.035916/0.274375ms and4349.884B/frame with zero measure/arrange misses and no concurrent build/CUA. All37 non-Markdown inputs match the combined source. Native physical Backspace remains incomplete; full Q01 is IN_PROGRESS, and no product UI/Flow roadmap ownership changed.

## Q01 — фоновые runtime-запуски

Ограниченный harness fix [`4c860dd`](https://github.com/ihatectf/Hatifect/commit/4c860dd53c1be9cfc53b670c6f3f81d4a65ff2c1) — DONE. Общий runner временно задаёт pauseWhenOutOfFocus=false только в двух изолированных документах и восстанавливает исходные байты/права после запроса. Семь новых tests и дополненная проверка фактического запуска: scoped PASS36; финальный C `run-pxp3dxim` — PASS1 396 .NET +362 Python. Fresh aggregate `7ed3036b-0af5-49e5-966d-1e4c8777b98b` — PASS28, boot без preferences `746aa695-01d3-4d50-8222-07204100bb27` — PASS1, Flow crash-after-save `7a08e72f-e342-4b87-879d-d71c39583992` — PASS9. Исходные файлы и настройки восстановлены; runtime evidence относится к source4c860dd. [Полный отчёт](Q01_BACKGROUND_RUNTIME.md) содержит причины, тесты, fingerprints, измерения и ограничения. Q01 сохраняет IN_PROGRESS: завершённое наблюдение физического Backspace остаётся следующим шагом. UI/Flow product implementation остаётся у профильных задач.

Follow-up [`74f8010`](https://github.com/ihatectf/Hatifect/commit/74f8010ad937fba376e30bca703c23478f92f097) закрывает P2 linked evidence directories до первой записи исходных настроек. Два реальных failing subtests выявили внешние backup/diagnostic files; после fix direct-runtime PASS37, tools `run-uh0zxyot` PASS363, C `run-46f1cxu2` PASS1 396 .NET +363 Python. Независимое review двухфайлового fix и actual regressions — PASS. [Полная трассировка требования и evidence](Q01_BACKGROUND_RUNTIME.md#проверка-каталогов-резервных-копий). Это проверка pre-launch filesystem boundary на временных fixtures; новые игровые PASS не заявляются, предыдущие runtime source/fingerprints сохранены. Интеграция идёт отдельным проверенным commit; общий Q01 остаётся IN_PROGRESS.

## Q01 — общая регрессия alpha.42

UI source [`9b8f5e6`](https://github.com/ihatectf/Hatifect/commit/9b8f5e6beea9691c8d8644230a4075c8b57c4e24) объединяет action binding, FLOWLINE admission repair и Q01. Published [`db9332c`](https://github.com/ihatectf/Hatifect/commit/db9332c3deede5458e42c58498d644ea094aca88) добавляет evidence-directory guard без изменения UI/Flow/CA/package inputs. GQ проверил owner C1431 .NET +363 Python и G1672 .NET +362 Python по actual TRX и source identities. Собственный P `hatifect-ui-ca-isolated.gk0wtdmj` — PASS90; все восемь package DLL равны producer и игровым DLL этого worktree. CI exact db9332c — SUCCESS, все10 jobs.

Fresh aggregate `6c0fecbc-cc1f-4b40-b6ed-22fd0cdde244` — PASS28; fake Flow `2d183d83-71f7-4863-a3b2-a473d1ee3606` — PASS6; isolation `00f184be-dc0b-4722-845b-4d34f1ab26d3` — PASS7. Отдельный PERF `25070893-f4a6-484b-b9b5-75f8c9873133` после harness integration — PASS2 на прежних восьми DLL:620frames, p95/p99 0.048083/0.366583ms,4605.484B/frame, zero layout cache misses. Все game processes exit0, teardownErrors пусты, исходные options восстановлены. Прежний aggregate sample не доказывает отсутствие concurrent owner build; отдельный повтор включает11 наблюдений без dotnet/MSBuild. Его первоначальная pre-launch observation попытка сохранена как BLOCKED, не PASS.

[Полный отчёт alpha.42](Q01_ALPHA42_INTEGRATION.md) сохраняет commands, fingerprints, source attribution, package audits, фактически просмотренные captures и ограничения. Визуальный проход завершён для всех восьми composed PNG EN/RU ×75/100/125/150% ×Dark на том же request6c0fecbc; diagnostic controls, focus outline и полный locale probe находятся внутри окна. SHA-256/наблюдения сохранены в visual-matrix-audit.json; полная product localization этим не сертифицируется. Полные U03/Q01 остаются IN_PROGRESS. Следующий общий шаг — приёмка готового product candidate либо завершённое физическое Backspace при доступности пользователя; GQ не начинает следующие продуктовые UI/Flowline срезы вместо их владельцев.

## Q01 — идентичность пакетных DLL

Ограниченное исправление ложного PASS [`c36a0d1`](https://github.com/ihatectf/Hatifect/commit/c36a0d133d847f2a9a8bd25fe1478d13963e1c5a) — DONE. Verifier завершённого feed сравнивает каждую DLL с итоговым normal Release producer и отклоняет отсутствие или различие байтов, сохраняя оба артефакта. RED2/7 → PASS7; tools `run-wvh66k1h` PASS365; C на хосте `run-bx1yb1kh` PASS1 431 .NET +365 Python с семью audited TRX. Fresh P `hatifect-ui-ca-isolated.5mec2l0l` на c36a0d1 — PASS90,44 точных projection inputs,8 byte-matched producer DLL,3 isolated assets и2 CA DLL. Все UI packages остаются alpha.42; публичный API, compiler profile и runtime не менялись.

[Отчёт](Q01_PACKAGE_IDENTITY.md) сохраняет replay первого mixed feed (прежний verifier принимает, исправленный отклоняет), первоначальные file-watch FAIL внутри sandbox и успешный неизменённый host repeat. Причина исторической смены Git metadata не объявляется исправленной. Этот Python fix не получает новых игровых PASS; предыдущая alpha.42 runtime-база сохраняет свои fingerprints. Точный commit передан задаче UI для alpha.43 P. Следующий общий шаг — приёмка готового U03 candidate; полный Q01 и физический Backspace остаются IN_PROGRESS.

## U03-b owning Update pump and portal retirement

Implementation [`8987a83`](https://github.com/ihatectf/Hatifect/commit/8987a83d9d9df4df99db64171b70e4e083e49a35), FLOWLINE ancestry merge [`6138bdc`](https://github.com/ihatectf/Hatifect/commit/6138bdc27e5e2578756866704f16f028b6fa26a4). The merge has an identical tree to the tested implementation. Final independent V2 evidence review is **PASS**:30 source postimages,10 G TRX, native20 artifact hashes/8 probes/restored settings and P44/8/3/90/2 all verified. Final P DLLs also equal the native V2 module byte-for-byte. Common integration/publication is handed to GQ; full U03 remains IN_PROGRESS.

Alpha.43 completes the bounded production Pump/close prerequisite. Full U03 remains **IN_PROGRESS**. The owning implementation is UI Runtime plus its Stardew menu/overlay adapter; Experience descriptors, public surface signatures and Flow persistence do not change. All17 existing producer/package/consumer version authorities advance together. GQ verifier [`c36a0d1`](https://github.com/ihatectf/Hatifect/commit/c36a0d133d847f2a9a8bd25fe1478d13963e1c5a) and its preceding alpha.42 documentation are retained by fast-forward, with all30 UI candidate postimages unchanged.

The existing root dispatcher had no production game Update caller. Root and child results now progress once through native `menu.update` or `overlay.OnUpdateTicked`; an occluded HUD still progresses, while an active-menu overlay loses ownership before Pump. A captured portal snapshot excludes newly presented entries until the next tick and skips retired entries. Draw, diagnostics and ordinary synchronization do not Pump. Closing first fences the complete retiring set and removes membership, then cancels operations before releasing keyboard/event/host resources. Wrong-thread retirement refuses before changing flags or consuming a handle. Retryable cleanup remains possible after a terminal Hide.

Independent review exposed two additional cancellation paths. Explicit owner Update originally cancelled a removed action before retiring its portals. Ordinary MovePointer recomposition bypassed the first repair. The final private `IUiHostSceneOwner` transaction runs inside every Runtime.Update: Begin precedes preparation, a callback-free fence follows accepted scene assignments, and End cancels the retained detached set before releasing guards. All sibling orphans and descendants are removed before the first callback. Rejected preparation preserves their pending work. Retirement drops the graph reference; a local owner reference guarantees End when a cancellation callback closes the runtime itself.

FLOWLINE's two unchanged presentation regressions are included from `a7be29938bbf8a34db0c70a74c9f969018cbaff1`. Pump now stages input/frame/accessibility together and retains presentation dirty state after a failed measurement, so the next Pump or ordinary accepted Update retries without another availability change. Preparation rejects nested scene/input mutation, preventing a text-metrics callback from overwriting a newly accepted scene.

| Observable requirement | Behavioral evidence |
|---|---|
| Whole-tree retirement before cancellation; wrong-thread handle remains closable | Four original PortalAction cases: `run-6tdx4btx` FAIL4/413 → unchanged `run-slc1fjdw` PASS413 |
| Root/portal owning completion, ready/pending retirement, callback replacement and new entries next tick | PortalAction Pump cases, worker threads and effect counters; `run-2um80605` PASS425 |
| All orphan siblings/descendants detached before removed owner or child cancellation | Four root/portal × running/idle cases: `run-fjwlrihr` FAIL4/429 → unchanged `run-jv9mno_a` PASS429 |
| Detached cleanup survives nested Close/Deactivate; rejected owner Update keeps pending work and allows retry | Four additional cases, `run-cj5jr9at` PASS433 |
| Standard interaction recomposition uses the same owner fence | Exact MovePointer regression: `run-lkwx_12e` FAIL1/434 → unchanged `run-kxprojde` PASS434 |
| Retained retired runtime releases its graph | Actual `RetainedRetiredRuntimeDoesNotKeepItsPortalGraphAlive`, final Runtime435 PASS |
| Presentation failure retains accepted input/frame/accessibility and recovers | Two unchanged FLOWLINE cases plus two Pump/Update recovery cases in ActionBindingTests |

The published409 Runtime cases remain intact; alpha.43 adds26 cases. Idle/pending root+portal tests retain frames/layout/counters and allow at most64B total over1000 warmed ticks; no new snapshot is allocated on each Pump. Scene-update orphan discovery still traverses the existing portal/owner graph; no claim of constant-time cold recomposition or whole-game frame performance is made. Initial `run-o1cmqszh` FAIL3/421 is retained: new fixture assertions wrongly required cancellation after an executor had already completed and disposed its token source. The corrected ready/pending cases require cancellation only for pending work and suppress observer delivery in both cases. No existing assertion was weakened.

Completed commands, with explicit x64 .NET8 SDK/.NET6 runtime and command-local `DOTNET_gcConcurrent=0` for build/check preparation:

- `./tools/hatifect-check`: `run-s889fhde`, **PASS1457 .NET +363 Python**, seven actual TRX.
- `./tools/hatifect-check --platform`: `run-eg0veryf`, **PASS1698 .NET +365 Python**, ten actual TRX, including Runtime435. `final-c-trx.json` and `final-g-v2-trx.json` verify counters and every individual Passed outcome, zero failures/skips.
- After incoming verifier integration, `./tools/hatifect-test tools`: `run-0rdce49n`, **PASS365**, zero failures/skips. The host-free C#/version postimages tested by C are unchanged. Final G includes both new verifier cases and the final native fixture. Earlier G attempts retain their failures: one pre-build Python source-contract failure, then three compiler diagnostics in the new test-only native fixture. Existing Hide assertion was preserved by separate equivalent early returns; the fixture now derives its empty cover menu from the abstract game base and checks nullable access explicitly.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**, retained `hatifect-ui-ca-isolated.m7giwmqf`;44 exact projection files,8 alpha.43 DLLs equal to final producer outputs,3 isolated assets files,2 exact deployed CA DLLs and no UI source in the consumer projection. The new feed verifier performs the producer comparison itself; an additional audit is retained in `final-p-v3-audit.json`.

Fresh `./tools/hatifect-ui-test semantic.actions.pump`, request `61e1e874-e390-430d-ab87-1e3c2aaca309`, is **PASS4**. The canonical executor was started directly in this worktree and confirmed Ready; SMAPI used ordinary GC and only isolated data. The test-only controller never calls Pump or native Update itself. Eight non-cooperative ValueTask probes record the framework's actual GetResult and observer delivery. Five completions carry captured request7/result42 on owning thread1; workers use other threads. Two cancelled menu/HUD-portal operations observe one late fault each with zero callbacks. The active-menu probe publishes its completion mailbox before the native owner changes, using a context-only bounded5s worker handshake; the first mismatched Update retires it with zero callbacks. The scenario completes in15 ticks; process14010 exits0 with no teardown errors, terminalError is null, and both isolated option files are byte-restored.

Native report identity is alpha.43, fingerprint `0502d9a683a9fabb4d1e7f486a2deb78a6bc43e2a3084e5df802da892d1fd8ad`. `native-v2-audit.json` independently binds20 artifacts and the eight DLLs plus manifest in the isolated UI module. It belongs to the frozen alpha.43 working candidate over base `c36a0d1`, not to the base alone and not to later repacked DLL metadata. `candidate-v2-source.json` binds30 final source postimages. P retains its separate final package identity. Independent source review is **PASS**, including the final fixture memory-order correction; final evidence review is recorded with the immutable checkpoint below. FLOWLINE separately verified the earlier C/G/native/P evidence within those exact source identities.

The earlier V1 candidate remains documented by `final-g-trx.json` (`run-x_jr9feo`,1698 .NET+363 Python), `native-audit.json` (request `7b0b4902-d747-4258-b2b7-e94c48b6dfde`, fingerprint `0442bc56152f6cb797a0558e628d5dce27d73cec89528ad5f65aa5049cca540f`) and `final-p-audit.json` (`itrwqwun`). The second source pass found a test-only observation race: GetResult published its read count before its late-fault flag. V2 increments the count in finally after the fault flag, retaining all assertions and production source. Fresh G/native/P above cover V2. An initial V2 P run `ovm9olie` passed90 but its default cleanup removed the projection before the separate byte audit; the retained `m7giwmqf` repeat supplies the final audit. None of these historical identities is relabelled as a later build.

Evidence lives under `artifacts/u03-owning-pump/`; research and assertion review are in ignored `.testagent/`. The stock static pairing scan reports149 source files,74 tests,117 paired and32 unpaired with no parser errors. The new native controller partial is heuristically unpaired; its executed game scenario is documented separately and is not called static coverage. Pairing also reports the pre-existing EditorOutlineTests as an orphan; no tests were deleted to change that classification.

Next ready U03 work is explicit action-generation acceptance for successful Terminal Open/Follow (including the same Cached model) and successful active asset reload. Ordinary focus/text/profile/theme/locale recomposition and failed reload must retain the current generation and pending operations. A concrete typed consumer with user messages and complete reload/save-switch runtime acceptance remain mandatory. Existing Hide is terminal; reopening creates a new owner. This checkpoint does not certify full R01 multi-host reload, physical input, visual localization or a new game PERF budget.


## Q01 — общая приёмка alpha.43

Corrected integration [`6c30713`](https://github.com/ihatectf/Hatifect/commit/6c307137287a9a5634c4383d9773cc873bf1a068) — **DONE** в ограниченном объёме общей приёмки. Все31 source postimages совпадают с owning UI candidate; f2f5a03/a7be299/53e85ff/32c936b и GQ a85c8ce ancestry сохранены. C `run-nhoswj2z` — PASS1457 .NET +365 Python на неизменном host-free C#; final G `run-10wrvzrr` — PASS1698 .NET +365 Python с десятью audited TRX. P `hatifect-ui-ca-isolated.x30nbzbc` — PASS90,44 projected files,8 package DLL равны producer и final game,3 isolated assets,2 CA DLL.

Дополнительный P2 от FLOWLINE независимо подтверждён GQ: failed event removal при Hide оставлял Visible=true, и продолжившийся input callback повторно захватывал keyboard lease. Owner correction32c936b проверяет current screen/retirement до Acquire. Реальный native subscriber regression на9485895 дал ожидаемый RED `c0e7c6a5-3afc-4ddc-b8b5-a4d1bd530710`; на6c30713 — GREEN `e6a80234-2501-489b-95ae-35289b32180a`, PASS2 при тех же preconditions. Retry cleanup прошёл в обоих случаях; исходный FAIL сохранён. Полные commands, causal observations, hashes и source-review boundaries — в [отчёте alpha.43](Q01_ALPHA43_INTEGRATION.md).

Fresh action Pump, UI/CA aggregate, fake Flow и isolation — PASS4/28/6/7. Отдельный PERF — PASS2,620frames,p95/p99 0.048542/0.518625ms,4609.006B/frame,layout misses0;11 process observations не обнаружили dotnet/MSBuild. Все game processes exit0 без teardown errors, исходные options bytes/modes восстановлены. Все8 composed EN/RU×75/100/125/150%×Dark PNG фактически просмотрены. UI fingerprint fdebf9cb24d282a4c3409b18daa59028e6bc1c7e3c5b308f636469a846c84c25, Flow2f7a0c223f4941373ade08b25f33ec16ee0b3553081e488b53c4dfe55c58731f относятся только к corrected6c30713.

Полные U03/Q01 остаются IN_PROGRESS. Следующий общий шаг — проверка переданного alpha.44 Open/Follow generation checkpoint; его новые source/runtime fingerprints ещё требуют отдельной приёмки. Physical Backspace, полный reload/save-switch и concrete typed consumer не объявляются завершёнными. UI и FLOWLINE сохраняют согласованное владение U03/U04; GQ продолжает общую интеграцию и публикацию.

### Q01 — исправление CI race после публикации alpha.43

Exact CI `34048617203` на `dc79fc7` — **FAIL**: быстрый child сохранил marker до поздней startedAtUtc родителя. Общий harness fix [`3043c67`](https://github.com/ihatectf/Hatifect/commit/3043c67) фиксирует timestamp перед Popen; transport/CLI и продуктовые контракты не меняются. Детерминированный real-process regression RED→GREEN, tools `run-dyy_2ve1` **PASS366**, host C `run-mktjcq83` **PASS1457 .NET +366 Python**, все семь TRX audited. Fresh `flow.chest.crash-after-return`, request `8d376841-169b-4a7a-86fa-29577f58f57b`, — **PASS15**, ожидаемый SIGKILL и distinct resume0, options restored и owned copy cleanup. [Причина, точные tests и evidence](Q01_ALPHA43_INTEGRATION.md#ci--временная-граница-запуска-процесса). Exact CI исправленной публикации `f035f4c`, [run34049870307](https://github.com/ihatectf/Hatifect/actions/runs/34049870307), — **SUCCESS**, все10 jobs. Следующий шаг — общая приёмка alpha.44. Полные U03/Q01 IN_PROGRESS.

## U03-b explicit Terminal action generations

Implementation [`ed44663`](https://github.com/ihatectf/Hatifect/commit/ed44663b1dda1819ea95ed5c3f9a526dda1cf91b). All22 source postimages in the immutable commit equal the C/G/P-reviewed candidate. GQ owns common integration/publication.

Alpha.44 completes the bounded explicit Terminal Open/Follow prerequisite. Full U03 remains **IN_PROGRESS**. Owning Runtime changes are private: UiHostActionBindings, UiHostRuntimeSession, UiPortalHostSession and UiTerminalHostSession. Public Experience/surface signatures and Flow persistence remain compatible. All17 version authorities advance together. The source base is `32c936ba72ebeb9f472a261a95b255d4ceafc1ce`, including the separate alpha.43 retired-overlay input correction; that correction's exact native RED/GREEN and common integration evidence belong to GQ.

A successful explicit OpenSection or route Follow now prepares a new dispatcher with the same SessionId and a fresh GenerationId, even when a Cached Experience returns the identical model and action descriptors. Ordinary focus/text/profile/theme/locale recomposition keeps the accepted dispatcher and its pending operations. Preparation installs no candidate state and cancels no accepted work. Acceptance installs the new map, fences the old dispatcher, publishes the accepted scene and detaches the old owned portal tree, then publishes callback-free Terminal metadata before any cancellation callback. Cancellation can close the Terminal without a later shell/current-invocation assignment resurrecting disposed owner state. An Open inside one completion prevents other ready results or queued requests from the old generation from being delivered or started.

The new TerminalActionGenerationTests adds nine actual cases:

- Three accepted transitions: same Cached section, another section, and pointer-followed route. Root and portal cancellation observe the accepted section/invocation and zero old portals; old actions/input are refused, late results have no effects, and the new action delivers42.
- Two rejected transitions preserve pending root/portal operations, accepted invocation and section; their completions still deliver21/22, and retry accepts the new section.
- Ordinary theme/profile/locale/pointer recomposition retains model and pending operations.
- Cancellation can dispose the owner without publishing metadata after disposal.
- Opening from completion suppresses another old ready callback while the new generation remains usable.
- Reopening the same Cached section cancels the pending operation and drops waiting requests without starting them.

The controlled ValueTask source uses inline ManualResetValueTaskSourceCore continuations so completion returns only after the framework mailbox is ready. These deterministic ownership cases do not substitute for separate worker-thread/native Update evidence. The published435 Runtime cases remain intact. The first fixture compile attempts (`run-btf7dm8k`, `run-iw87enuf`) and an initial thread-pool-sensitive TCS fixture attempt (`run-acpk4prv`) are retained. Final behavioral reproduction `run-wiaemfrg` has8 FAIL/1 PASS in the new nine cases. After the four production changes, `run-jh2e8knf` has9/9 new cases PASS, but the complete suite is443/444 because an unchanged FileWatchLoadsReplacementRetainsInvalidLastGoodAndStopsOnDispose wait fails in the restricted execution context. Exact same source under host-context execution gives `run-tpgza_ok` **PASS444/444**. No watcher source, wait threshold or assertion was changed; the evidence establishes execution-context dependence without claiming a broader OS root cause.

Completed commands use the explicit x64 .NET8 SDK/.NET6 runtime with command-local DOTNET_gcConcurrent=0 for build/check:

- `./tools/hatifect-test ui --project 'Hatifect UI/tests/Hatifect.UI.Runtime.Tests/Hatifect.UI.Runtime.Tests.csproj'`: `run-tpgza_ok`, **PASS444**, actual counters and every outcome audited in `runtime-trx.json`.
- `./tools/hatifect-check`: `run-1s3sgwvd`, **PASS1466 .NET +365 Python**, seven actual TRX, zero failures/skips.
- `./tools/hatifect-check --platform`: `run-1h63c5rf`, **PASS1707 .NET +365 Python**, ten actual TRX, including Runtime444, zero failures/skips.
- `./tools/hatifect-isolated-ui-ca --keep`: **PASS90**, retained `hatifect-ui-ca-isolated.vukebou9`. Independent root audit verifies44 exact projection files,8 alpha.44 package DLLs equal to final producer outputs,3 assets files resolving solely from the isolated cache,90 actual Passed results and2 exact CA deployed DLLs with no UI copies.

Evidence is under `artifacts/u03-terminal-generation/`: `candidate.json` freezes22 source postimages over32c936b; `final-c-trx.json`, `final-g-trx.json` and `final-p-audit.json` bind actual outputs. The stock static pairing result is150 sources/75 tests/117 paired/33 unpaired with no parser errors; the existing EditorOutlineTests orphan remains. This heuristic is not executable coverage. Independent bounded source/test and final evidence/hash review are **PASS** with no actionable finding. The reviewer independently verified all22 source hashes, all17 C/G TRX, the separate Runtime444 host-context result, and P projection/package/cache/deploy bytes; alpha.43 native evidence is explicitly excluded from this alpha.44 verdict.

There is no extra per-frame dispatcher allocation or retained generation history: allocation occurs only for explicit accepted/candidate transitions, and the existing4096-action bound is retained. Old registrations and publication subscriptions retire after the complete fence. This checkpoint does not claim a new game performance budget. Fresh alpha.44 reload/save-switch runtime acceptance is still pending; alpha.43 native evidence is not relabelled as alpha.44. Full U03 requires successful active reload generations, failed reload preservation, a concrete typed consumer and user messages. The next implementation must commit asset metadata together with the accepted host scene before old-action cancellation; the current staged apply/rollback callback cannot safely rotate generations on both paths.

### U03 / Q01 — общая интеграция alpha.44

Локальный merge [`e954c6d`](https://github.com/ihatectf/Hatifect/commit/e954c6db21e26e1a9546f3bd23fe77c1fa25f329) сохраняет immutable e3c9dbf/ed44663 и опубликованный GQ f035f4c с исправлением harness3043c67. Все22 source postimages проверены; только append-конфликт документации разрешён сохранением обеих частей. C `run-m57db06u` — **PASS1466 .NET +366 Python**; G `run-ik_madoq` — **PASS1707 .NET +366 Python**; все17 TRX audited. P `hatifect-ui-ca-isolated.ooc7yrq9` — **PASS90**,44 projected files,8 package DLL равны producer этой сборки,3 isolated assets,2 deployed CA DLL. [Отчёт и границы](Q01_ALPHA44_INTEGRATION.md). Это первоначальная проверка production baseline; итоговая приёмка исправленной поставки приведена ниже. UI reload/U04 WIP не импортирован.

Native merge `4f9b388` принимает ровно3postimages `0e6f6358`: full source/staged review, idle sibling retirement до capture доказан. G `run-6x8z89bn` — **PASS1707 .NET +366 Python**,10actualTRX; P `hatifect-ui-ca-isolated.xtrbljni` — **PASS90**,44/8/3/90/2 с package DLL, равными игровой поставке prepare `run-h62059vp`. Fresh Terminal/keyboard/pump/Flow/isolation — **PASS8/2/4/6/7**, отдельные raw reports и options/process audits. Aggregate `a3d96a64-b156-4657-92fc-8d8f4d1ce5c8` — **FAIL** на LoadFadeFinished после13passed host checks; ни один matrix PNG не получен. Diagnostic-only `b381eae`, scoped18PASS и probe `1d4f9506-c438-49fc-b4a4-6dc5d25cd445` **FAIL14**, подтвердил actual pauseWhenOutOfFocus=true при unfocused single-player после загрузки golden. SaveGame заменяет runtime options значениями сейва; config-file lease перед запуском недостаточен. Этот FAIL стал основанием для общей post-load correction; её итоговая приёмка приведена ниже, исходный FAIL/golden/criteria сохранены. Alpha45 frozen foundation/capture f4b7386 согласован отдельно,17version authorities принадлежат GQ; alpha46 host wiring/reload остаётся у UI. Полные U03/Q01/U04 не закрыты.


Итоговый source [`fe51271`](https://github.com/ihatectf/Hatifect/commit/fe51271c86c40198a431703ed248da91028da765) устраняет post-load pause общей policy, включаемой canonical runner и обязательным UI adapter при трёх явных flags. Current Options проверяется перед Update, subscription снимается при disposal; public API/Flow persistence/criteria неизменны. Три existing launcher tests RED3→GREEN3, tools run-l0eetq_e PASS366; финальные C run-2n1xvy1g PASS1466 .NET +366 Python, G run-xnir8zxt PASS1707 .NET +366 Python,17actualTRX; P hatifect-ui-ca-isolated.rxhxmv45 PASS90 и44/8/3/90/2, package DLL равны game prepare run-h4un1s5n. Bounded independent source review не сообщил открытых findings.

Corrected aggregate bcc44b11-0b67-49ea-bafe-e9d42fc4376b — **PASS28** при actual unfocused/pause=false/fade=-0.0304 и visualMatrixRestored=true. Fresh Terminal/keyboard/pump/Flow/isolation — **PASS8/2/4/6/7**; crash-return09170474 — **PASS15** через distinct owned processes и пять saves. Отдельный PERF8a42d13b — **PASS2**,620frames,p95/p99 0.052125/0.395959ms,4446.477B/frame,layout misses0;14наблюдений без dotnet/MSBuild/vstest. Все8 EN/RU×75/100/125/150%×Dark composed PNG фактически просмотрены. Config bytes/modes восстановлены, owned copy удалена, golden hashes неизменны. Полные runtime/crash/visual source-and-artifact audits перечислены в [отчёте alpha.44](Q01_ALPHA44_INTEGRATION.md#исправленная-поставка-fe51271).

Ограниченная общая приёмка alpha.44 — **DONE**, обязательные gates **PASS**; опубликовано в develop как bea00adf9c409421415a8b370c29fd3c7ac656b4. Exact [CI34056007173](https://github.com/ihatectf/Hatifect/actions/runs/34056007173) — **SUCCESS**, все10jobs. Следом GQ интегрирует frozen alpha45 foundation/capture f4b7386 и обновляет все17version authorities. Полные U03/Q01/U04, concrete typed consumer, complete cargo failure matrix и physical Backspace остаются IN_PROGRESS; native host wiring/reload alpha46 ведёт UI.

## U04-a: environment planning and invocation foundation

Implementation: `b89c792fd623bd58ac07c95337ff7602de33928a`, branch `feature/u04-environment-planning`, immutable base `f2f5a03`. Evidence worktree: `/private/tmp/hatifect-flow-u04-environment`. Full U04 is **IN_PROGRESS**. The coordinated scope contains six owning implementation files and three new behavioral test files. UI retains existing Terminal/host/reload implementation and package version authority; local alpha.43 packages here identify only this tested producer, not a newly published version or the later combined candidate.

`UiEnvironment` is a validated immutable semantic snapshot: logical viewport, finite positive scale, defined input mode, locale, stable theme identity, reduced-motion/high-contrast preferences and six nonempty facet origins. It retains no platform object. Named `UiHostContext.InEnvironment` and `UiInvocationService.InvokeInEnvironment` preserve existing constructors and `Invoke(id, profile, presentation)` signatures, including uncast null source calls. Internal known-available/replan paths optionally accept the same environment. Invalid environment/profile combinations reject before consumer callbacks; replan preserves the active Experience and registry ownership.

Framework profile selection retains existing boundaries: Controller input first, otherwise logical width below720 Compact, below1100 Medium, otherwise Wide. Scale is not applied twice. The compiler's LUI2009 now requires **all** declared capabilities instead of any overlap. The planner independently enforces this for direct IR and rejects mismatched presentation name/identity. Generated defaults can select the first fully covering alternative in a fixed12-presentation order. Invalid explicit view/prefer/fallback assignments reject; an impossible combination raises an addressable `UiPlanningException` with readonly rejection reasons and exposes no partial plan. Trace contains only the current plan's six facet values/origins, profile rule and bounded element decisions, with invariant numeric formatting.

The intended compatibility change is stricter rejection of partially covering explicit presentations, which previously lost declared semantics. Supported existing consumers pass the complete graph and isolated package gate. The frozen `IUiSemanticSurfaceApi` v1, persistence formats and version authorities are unchanged. Environment-aware native sessions are still pending.

| Verification | Actual result and scope |
|---|---|
| Coverage regression before implementation | `artifacts/u04/before/run-21cqs5yq`:94 executed,90PASS/4FAIL; three missing LUI2009 diagnostics and wrong generated Side choice |
| Same four regressions after implementation | `coverage-green/run-urgxk000`:94/94PASS |
| Expanded environment/trace/boundary cases | `environment/run-rd3606tl`:114/114PASS |
| Final scoped Planning before last identity-only case | `planning-final/run-eff039by`:117/117PASS, ordinary GC, includes record-copy guard and two performance cases |
| `./tools/hatifect-check` | `artifacts/validation/run-ctzcq2ih`:1489 .NET and365 Python PASS; seven actual TRX, Runtime440 and Planning117 |
| `./tools/hatifect-check --platform` | `run-28xoii09`:1731 .NET and365 Python PASS; ten actual TRX, Runtime440 and Planning118 including final `DirectIrCannotBorrowCoverageFromAnotherPresentationName` |
| `./tools/hatifect-isolated-ui-ca --keep` | `hatifect-ui-ca-isolated.8exc9zrj`:90 actual CA tests PASS,44 exact projected files,8 exact producer package DLLs,3 fresh-cache assets files,2 deployed CA DLLs; UI source absent |

Commands used x64 SDK8.0.424 with the .NET6 target/runtime; G/P use the configured Stardew references without deploying into ordinary Mods or running the game. Final C/G used task-local `NUGET_HTTP_CACHE_PATH=.../artifacts/nuget-http` and `RestoreForce=true` to refresh a cached NuGet audit failure; audit was not disabled. C/G/P preparation used command-local `DOTNET_gcConcurrent=0`. The separate117 Planning run used ordinary GC. Earlier C `run-ne8y5kwm` failed NU1301 restore and `run-g_ubdyuy` failed cached NU1900 audit before tests. Earlier sandboxed Runtime `run-hpur3on3` passed all five new cases but failed the existing bounded FileWatch wait (439/440); final host-permitted C/G passed that unchanged case. Initial authoring compile errors remain in their artifacts and are not claimed as production RED evidence.

The two performance cases warm100 calls and measure600 alternating Wide/Controller plans of one four-element Experience. Ordinary case: p95 **0.018666ms**, p99 **0.020125ms**, **8716.71B/plan**; capability fallback: **0.032000ms**, **0.081375ms**, **11521.41B/plan**. Existing2ms/4ms/16384B thresholds remain unchanged; trace sizes stay bounded. These measurements cover planner work plus a small result traversal with prepared environments, not environment capture, full invocation, native rendering or arbitrary model sizes. Full U04 runtime PERF/matrix remains required.

Independent read-only review found no remaining actionable issue in the bounded source diff and independently confirmed actual C,117 Planning PERF, and the five invocation outcomes. Final G confirms the later identity case. `artifacts/u04/candidate-source.json` binds all nine source postimages; `final-c-audit.json` and `final-g-audit.json` bind summaries/TRX and every Passed outcome with zero skips; `final-p-manifest.json` and `final-p-audit.json` bind package/archive hashes, actual producer DLL equality, fresh cache assets and CA deployment. The44 projection files equal immutable base `f2f5a03`; the U04 producer delta has its separate nine-file manifest. Final staged bytes matched that source manifest before commit. Static source-pairing tool could not run because `tree-sitter-language-pack` is absent; no pairing coverage is inferred. Assertion/gap review is recorded in ignored `.testagent/`.

Next ready step: add the Stardew environment capture adapter and wire it through existing hosts/Terminal while preserving reload ownership, then execute native environment acceptance on one combined package candidate. Flow F12 remains dependent on completed U04; live locale and meaningful empty/faulted consumer states are tracked in [FLOW_UI_READINESS.md](FLOW_UI_READINESS.md). Runtime/visual status for this foundation is NOT_APPLICABLE; it does not discharge the full U04 runtime requirement.

## U04-b: Stardew environment capture

Implementation `29373a249ae72fadee435d0ca790e90183db2c75`, over U04 foundation/docs `b89c792`/`9a85940` in `/private/tmp/hatifect-flow-u04-environment`. Exactly two new files: `UiSemanticStardewEnvironmentCapture.cs` and `EnvironmentCaptureTests.cs`. No existing host/Terminal/reload file or package version authority changed. Full U04 remains **IN_PROGRESS**.

The adapter captures the host's logical viewport and applied `Game1.options.uiScale` without another geometry transform, gamepad input, actual locale, existing theme ID and explicit accessibility policy. HighContrast follows the selected `UiSemanticTheme`; ReducedMotion=false is described as the current framework default, not an observed OS preference. Every facet records its source. One previous immutable snapshot is reused only when every value, accessibility preference and origin agrees. Invalid data rejects without mutating the previous capture; the adapter retains no platform object or growing cache. Hosts must invoke it after their owning-thread/screen/visibility guards and commit the candidate environment together with accepted scene preparation.

Independent review found and closed a locale P2 before this checkpoint. `CurrentLanguageCode.ToString()` collapses every custom language to `mod`, so a mod-language A→B switch would incorrectly reuse the previous environment. The installed game XML and local decompilation confirm the API behavior. Its cached `CurrentLanguageString` can also remain stale when `SetModLanguage` retains the same enum. The production resolver therefore uses uncached `LanguageCodeString(current)`, preserves regional/custom codes and maps the actual empty English asset suffix to `en`. Missing non-English locale codes reject instead of falling back to English.

| Command/evidence | Actual result |
|---|---|
| `./tools/hatifect-test ui --platform --project 'Hatifect UI/tests/Hatifect.UI.Stardew.Tests/Hatifect.UI.Stardew.Tests.csproj'` | Final `artifacts/u04/capture-locale/run-x7gau2hi`:50/50PASS,32 new cases; ordinary GC |
| `./tools/hatifect-check` | `artifacts/validation/run-rp3v0ud0`:1490 .NET+365 Python PASS, seven actual TRX, Runtime440/Planning118 |
| `./tools/hatifect-check --platform` | `run-_b3y7j8s`:1763 .NET+365 Python PASS, ten actual TRX, Stardew50/Runtime440/Planning118 |
| `./tools/hatifect-isolated-ui-ca --keep` | Retained `hatifect-ui-ca-isolated.0az60ey5`:90/90PASS,44 source projection files equal immutable `9a85940`,8 exact producer package DLLs,3 isolated cache assets,2 exact CA deployment DLLs; UI sources absent |

SDK/runtime, game-reference configuration and fresh audited restore use the same command-local setup as U04-a. C/G/P preparation uses `DOTNET_gcConcurrent=0`; the scoped50 run uses ordinary GC. Final `source-manifest.json`, `final-c-audit.json`, `final-g-audit.json`, `final-p-manifest.json` and `final-p-audit.json` live under `artifacts/u04/capture-locale/`. Every .NET outcome was individually Passed, with nonzero counters and no skips; staged source bytes matched the two-file manifest before commit. These local alpha.43 archives belong to this exact producer and do not certify a later combined/versioned build.

The tests cover all theme variants, each changed environment facet, prior-snapshot immutability, foreign origins/preferences, invalid geometry/scale/theme/locale, actual game EN/RU/PT mapping and custom A→B normalization. One allocation assertion permits at most64B across1000 warmed `FromSnapshot` calls and confirms the same retained reference. It does not measure the Game1-reading wrapper or native frame cost. Custom A→B is tested through the pure mapping seam, not by switching a running game's language. The earlier V1 scoped43 and G1756+365 remain under `capture/` and `run-60q30qam`, with source copies/hashes; those outcomes did not detect the reviewed locale error and are not final candidate acceptance. Initial `run-zwvruhpa` failed a missing System import in the new test fixture; no existing assertion was changed.

Source/scoped independent re-review closed the P2 and found no further actionable issue. Final independent evidence review also verified both immutable Git blobs, actual C1490/G1763+365 and every retained package/cache/TRX/deployment identity above. The ready handoff includes `artifacts/u04/environment-wiring.md` for UI-owned existing callsites. Native host propagation, coherent plan/compositor/consumer locale, retained Experience/action generation across environment-only refresh and the actual environment matrix remain required. In particular, current Flow parcel labels and `_russian` are constructor-bound; updating only measurement locale does not satisfy F12 live localization. No new native/visual result is claimed by this adapter checkpoint.

## U04 — общая интеграция alpha.45

GQ интегрировал frozen f4b7386 foundation/capture поверх опубликованной alpha.44 bea00ad как [`92f4890`](https://github.com/ihatectf/Hatifect/commit/92f48904e766f1c3fac479d84c85a57502d41b75), сохранив background policy fe51271 и поколения действий. Все11 source/test postimages совпадают с проверенным владельцем; единственный append conflict журнала разрешён сохранением обеих частей. Все17version authorities переведены GQ на alpha.45.

Собственные C `run-g3mz915j` — **PASS1499 .NET +366 Python**, G `run-qia6ig5p` — **PASS1772 +366**,17actualTRX. P `hatifect-ui-ca-isolated.61463sz9` — **PASS90**,44/8/3/2 с exact producer/package/consumer identities и8 DLL, равными prepare `run-u3zuo5s9`. Ordinary-GC Planning `run-2qizpggb` — **PASS118**; по600 alternating plans после100warmups: no-fallback p95/p99 0.015000/0.015583ms,8716.71B/plan; fallback 0.017959/0.022583ms,11520.71B/plan. Бюджеты и bounded trace сохранены.

Fresh UI/CA aggregate5adb1d67/Flow04cc2bce/isolationb7e08ac4 — **PASS28/6/7**; native PERF55c6c0cd — **PASS2**,620frames,p95/p99 0.049166/0.367626ms,4604.155B/frame,layout misses0,10наблюдений без .NET builds. Все8 EN/RU×scale PNG фактически просмотрены. Options bytes/modes восстановлены; все4request-owned save copies удалены, включая companion isolation; golden hashes неизменны. PERF по контракту выполняется без сейва. `final-audit.json` связывает все34source hashes с Git и фактическими артефактами.

Ограниченная интеграция alpha.45 — **DONE**, приёмка **PASS**; опубликовано в `develop` как [`12e7aab`](https://github.com/ihatectf/Hatifect/commit/12e7aabf816a8e67a4f64846d2fb90231e1d76b1). Exact [CI34058056333](https://github.com/ihatectf/Hatifect/actions/runs/34058056333) — **SUCCESS**, все 10 jobs. [Команды, полные request IDs, source identities и ограничения](U04_ALPHA45_INTEGRATION.md). Следующий готовый шаг — приёмка frozen alpha.46 host wiring/reload от UI, затем согласованная live text/Flow Parcel projection. Полные U04/F12/U03 и physical Backspace остаются незавершёнными.


## U03/U04 — общая интеграция alpha.46

GQ принимает frozen handoff `b6f60b49af5c7f94469bab4021ab12f2b61acd88`, source `0cd356f0fd252a0744fe6a871e46ba631193f876`, поверх опубликованной alpha.45 `12e7aab`. Все 48 source/version hashes владельца совпали с immutable Git и общим index; 17 version changes принадлежат UI. Независимая проверка GQ повторно прочитала 20 изменённых source/test files и вычислила owner C/G/P и native audits: PASS1528/1801 .NET +366 Python, 17 TRX, P90/44/8/3/2, reload18/environment25. Все старые scenario objects и исходные U03/U04 criteria сохранены. Merge прошёл без конфликтов; чужая следующая работа не импортирована.

Общая alpha.46: приёмка исправленного source [`61e8a94`](https://github.com/ihatectf/Hatifect/commit/61e8a941bbb2936e2b41f029e931b2ee8d30fe8e) — **PASS**; ограниченная интеграция **DONE**, опубликована в `develop` как [eb193b7](https://github.com/ihatectf/Hatifect/commit/eb193b7b98b35e8a446d1fef683c032588ece30a), exact [CI34062754345](https://github.com/ihatectf/Hatifect/actions/runs/34062754345) — **SUCCESS**, все 10 jobs. Финальные C `run-856rkloj` — PASS1528 .NET +366 Python, G `run-k8rpgi68` — PASS1801 +366; 17 actual TRX, P90/44/8/3/2, package/producer/game DLL совпадают. Fresh reload/environment/aggregate/Flow/isolation/PERF — **PASS18/25/28/6/7/2**; PERF620, p95/p99 0.051792/0.417460 ms, 4451.265 B/frame, layout misses 0, 15 наблюдений без .NET builds. Восемь EN/RU × scale PNG просмотрены, options bytes/modes восстановлены, шесть request-owned save copies очищены, golden hashes неизменны. Исходный environment FAIL на `fc83019`, диагностический `a456eee`, input correction `7d68b6a` и custom-locale restoration `61e8a94` сохраняют отдельные evidence; native custom-start — NOT_RUN. Итоговый `artifacts/alpha46-release-preflight/final-audit.json` — PASS, 53 source/doc hashes связаны с Git. [Команды, source identities, RED→GREEN и ограничения](U03_U04_ALPHA46_INTEGRATION.md). Следующий шаг GQ — интеграция готового UI save-switch `889f9b1` / `d044f22` и завершение review FLOWLINE text/Parcel `485c815`; новый WIP владельцев не импортирован. Полные U03/U04/F12 остаются незавершёнными, общий alpha.47 bump принадлежит GQ.

## U04-c: retained Experience text projection

Combined source checkpoint [`905acf7fc33fcbe752ef4eba003723c8efbc831a`](https://github.com/ihatectf/Hatifect/commit/905acf7fc33fcbe752ef4eba003723c8efbc831a), branch `feature/u04-text-projection`, base `f4b7386`; evidence in `/private/tmp/hatifect-flow-u04-environment/artifacts/u04/text-projection/`. The common API is `736368ba3216a12005611d46fd06cebf651f5714`; `27c590e` updates only its builder checksum in the API baseline; `aed436b` adopts UI owner's isolated host correction `ec933730e13dca0bbe0674289f969ddefc93f49e`; `905acf7` adds the formatter integration regression. Eleven immutable source, metadata and test postimages are bound in `source-manifest.json`. Full U04/F12 remains **IN_PROGRESS**. Existing Terminal/reload/Flow source and all package version values are unchanged by this chain.

`UiLocalizedText` copies an exact ordinal locale map and retains a mandatory authoring fallback. Named builder declarations attach display names, element labels and action titles to stable IDs. `FormatText<T>` accepts a captured typed value and captured locale for read-only displays. It rejects wrong types, auxiliary/editable/form/collection/action-group value formatters, missing IDs and duplicate declarations. Empty results remain valid; null/exception rejects scene preparation with element and locale context. Graph fallback labels/aliases and the original source/action objects remain intact. Scene values, layout and accessibility consume materialized text, and drawing does not call the formatter. Frozen surface API v1 and persistence formats are unchanged. [Authoring and compatibility details](UI_TEXT_PROJECTION.md).

| Scoped evidence | Actual outcome |
|---|---|
| Before locale resolution implementation, `red/run-o31zvy_6` | 442 executed,439PASS/3FAIL: two new environment-locale regressions plus the existing restricted FileWatch timeout |
| Initial implementation, `runtime/run-leazafvp` | 447/448PASS; the new test's total formatter-call expectation omitted the legitimate focus-triggered composition. The corrected assertion measures the exact locale-transition delta and still forbids formatting in Draw |
| Planning authoring checks, `planning/run-1b7d4lqa` | 133/133PASS,15 new cases |
| Runtime before final invariant case, `runtime-final/run-j8dmbe15` | 449/449PASS,9 new cases including retained pending typed action and owner-pump completion |
| Reviewer invariant-locale regression before correction, `invariant-red/run-rf9x68fl` | 449/450PASS; captured empty locale incorrectly changed to ambient fr-FR during publication recomposition |
| After private ComposeCaptured correction, `invariant-green/run-eqs4ljub` | All10 locale cases Passed; whole run remained FAIL449/450 because unchanged Adaptive collection rendering allocated5416B against its zero-allocation assertion |
| Same built source, isolated ordinary-GC locale plus Uniform/Adaptive allocation checks | `invariant-isolated/tests.trx`:12/12PASS, no skips, no rebuild or GC override; non-reproduction in isolation does not erase the preceding failure or establish its cause |
| Combined final source, `combined-runtime/run-ooa9md8c` | 455/455PASS, including all10 locale cases, the formatter reentry regression and four adopted host cases; command-local `DOTNET_gcConcurrent=0`, every actual TRX row Passed |
| `./tools/hatifect-check` | `artifacts/validation/run-mt5zq4ak`:1520 .NET+365 Python PASS, seven actual TRX, all30 new authoring/locale/host cases, no skips |
| `./tools/hatifect-check --platform` | `run-y52tgcit`:1793 .NET+365 Python PASS, ten actual TRX, Runtime455/Planning133/Stardew50, all30 new cases, no skips |
| `./tools/hatifect-isolated-ui-ca --keep` | Retained `hatifect-ui-ca-isolated.v4wi_cav`:90/90PASS,44 projected files equal Git905acf7,8 package DLLs equal actual producer outputs,3 isolated cache assets,2 exact deployed CA DLLs; UI source absent |

Independent reviewer confirmed the actual scoped TRX and closed the invariant-locale P2. `Compose` resolves initial explicit/environment/ambient locale once; publication callbacks use private `ComposeCaptured`, preserving even the invariant empty locale. A new independent composition still uses the current ambient culture for a legacy invocation.

The review also identified an inherited host refresh reentry gap: composition occurs before the host update transaction, so a formatter could accept a nested scene which the outer stale composition then overwrote. The isolated UI-owned correction captures `AcceptedVersion` before composing, rechecks host lifetime/preparation afterward and rejects a stale outer result if a nested scene was accepted. The six-line successful path adds no closure allocation. Existing Terminal behavior intentionally permits nested acceptance; UI rejected an attempted blanket reentry prohibition after its unchanged Terminal regression failed. Four adopted host cases cover root/portal replacement, compose failure retry and retirement during compose. Our formatter-specific integration regression accepts a nested replacement, observes the outer `InvalidOperationException` and verifies that exact replacement remains current.

Independent source review closed both P2 findings on this combined base, verified all11 source hashes and actual455 TRX rows, and found no further actionable issue. UI's separate development-tree472 result is not relabelled as this older base's455 result. Initial C `run-_s0lv1ix` failed before build because the FormAuthoring baseline fingerprints the entire builder file. Canonical baseline regeneration changed only `FormAuthoring.Files[1].Sha256`; `SemanticSurface`, `ContractVersion` and all version values remained identical. The canonical verifier then passed.

Final `source-manifest.json`, `combined-runtime-audit.json`, `final-c-audit.json`, `final-g-audit.json`, `final-p-manifest.json` and `final-p-audit.json` bind immutable Git905acf7 postimages to the actual results above. Every TRX outcome is Passed, every executed test count is nonzero, and there are no failed or skipped cases. C/G include15 ExperienceText,10 LocaleComposition,4 InteractionPreparation and1 formatter reentry case. C/G restore retains NuGet auditing with a task-local fresh HTTP cache and `RestoreForce=true`; P retains its canonical isolated offline feed policy. Commands use the same explicit x64 SDK8.0.424/.NET6 target and command-local `DOTNET_gcConcurrent=0` as preceding U04 preparation. The earlier ordinary-GC allocation failure remains retained; the final full green checks do not establish its cause or a new frame budget. Local alpha.43 package archives identify this exact producer only; the combined integration version is assigned separately after alpha.46, and owner correction ec933730 must not be duplicated if already present through that host chain.

Next work is the Flow parcel migration onto these captured text contracts and UI-owned native environment propagation. Actual game item localization, empty/faulted surface semantics, retained native environment updates and the complete F12 lifecycle/input/scale matrix remain required. Native/visual status is NOT_APPLICABLE to this common host-free prerequisite; the mandatory native U04/F12 acceptance remains outstanding. There is no new numerical frame-PERF claim in this text projection checkpoint.

## F12-a: retained Parcel text

Consumer source checkpoint [`6e75c5c7cc2ec31b2b0ae5b5a58c7a590fbc5551`](https://github.com/ihatectf/Hatifect/commit/6e75c5c7cc2ec31b2b0ae5b5a58c7a590fbc5551) is on `feature/flow-parcel-text` over published U04 docs `6552514` and source `905acf7`. Full F12 remains **IN_PROGRESS**. Two production source files own the migration: `ParcelExperience` and `ParcelTextValue`. No Core/Persistence/application/native host behavior, UI production source, persistence schema or version value changes. Nine source/test/project postimages are tracked in `artifacts/f12/parcel-locale/source-manifest.json` in `/private/tmp/hatifect-flow-u04-environment`.

Five stable published sources now contain typed immutable facts over one captured snapshot, parcel, names and original last `FlowCommandResult`. Labels, diagnostic title and the five original action titles use U04 locale metadata. `FormatText<ParcelTextValue>` consumes only the captured payload and locale; it performs no application/name-adapter reads. The surrounding composition continues normal legacy CanExecute checks, which intentionally read the current cached application snapshot. Ordinary availability/revision/session fences, source/action identities, atomic publication, failed preparation retry and retirement remain intact.

The constructor language controls authoring fallback only. Explicit ordinal keys are en/en-US and ru/ru-RU; unknown and invariant locales use that fallback. Quantities format invariantly. Graph IDs/aliases/fallback labels remain stable; internal presented payloads change from string to `ParcelTextValue`, whose ToString retains fallback text for source inspection. The optional exclusive `localizedItemName` captures an immutable item catalog before publication. The current game callback still uses the legacy single-string path and is not claimed to support live item locale changes. No formatter performs native lookups or global language changes.

Model preparation retains one latest fact record, five fixed value records and the existing bounded publication, without an added event queue/cache. Every scene composition can format the five values and the fixed five-action availability description; Draw still consumes materialized UI text. These checks introduce no numerical frame-PERF guarantee. U04 source and the consumer must be integrated together on the version assigned after alpha.46.

| Evidence | Actual result and boundary |
|---|---|
| Initial locale regression `red/run-yu5v5oxr` | 455PASS/2FAIL: retained title stayed in the constructor language for either original fallback |
| First correction `green/run-6ml2929f` | 457/457PASS, including both initial locale scenarios |
| Initial Flow helper migration `flow/run-lq_dsmzd` | 720PASS/5FAIL: shared subscription test helper still cast Parcel payloads to string; corrected to assert the typed Parcel source while preserving every old behavioral assertion |
| Expanded earlier placement | `flow-final/run-7rsgf1bo`730/730 and `runtime-final/run-29v99lbj`466/466; this temporary UI-tests→Flow graph failed the unchanged architecture rule and is not final integration acceptance |
| Corrected test graph | Consumer scenarios moved to Flow.Tests; pure `ExperienceTextProbe` remains in UI.Runtime.Tests with no consumer dependency. Temporary production internals grants were removed. Architecture PASS26 projects/5 rule groups |
| Corrected graph initial build | `final-graph/run-hlm4c3ue` failed one nullable Name diagnostic in the new probe; its observation now preserves nullable accessibility names rather than inventing a fallback |
| Corrected graph scoped run | `final-graph/run-0i2grkck`:741/741PASS |
| Final probe with actual retained scene/accessibility references | `final-probe/run-sfn6uot_`:741/741PASS, all11 ParcelLocale and5 ParcelTextCapture cases; every actual TRX row Passed, no skips |
| `./tools/hatifect-check` | `artifacts/validation/run-bpfplhig`:1536 .NET+365 Python PASS, seven actual TRX; all16 new cases Passed, no skips |
| `./tools/hatifect-check --platform` | `run-lkwi6jj1`:1809 .NET+365 Python PASS, ten actual TRX; Flow741/Runtime455/Planning133/Stardew50, all16 new cases Passed, no skips |
| `./tools/hatifect-isolated-ui-ca --keep` | Retained `hatifect-ui-ca-isolated.7eo2gau9`:90/90PASS,44 exact Git6e75c5c projection files,8 package DLLs equal actual producer outputs,3 isolated cache assets,2 exact CA deployment DLLs; UI source absent |

`ExperienceTextSnapshot` reads the actual retained UiScene/accessibility objects when assertions access its properties; earlier-frame checks do not merely inspect strings copied by the test helper. Scenarios cover same-instance EN→RU→EN, all labels/titles/values, accessibility text, source/action identity, domain/publication revision stability, actual rejected-command reason without another provider attempt, immutable captured item catalogs, old publication values, name-capture failure/retry and retirement before remaining callbacks. Existing Flow assertions remain, with only typed-source reading helpers adapted. Independent bounded source/scoped review closed without actionable findings and verified the actual741 TRX rows plus all9 source postimages. Final C `artifacts/validation/run-bpfplhig` passes1536 .NET+365 Python, seven actual TRX including all16 new cases; `final-c-audit.json` matches immutable Git6e75c5c and confirms every outcome Passed with no skips. The same audit verifies G1809+365. Final P manifest/audit verifies the retained package-only consumer. Independent C/G evidence review confirms all counts, actual outcomes and immutable source hashes; independent final P review also confirms all44 Git projection files, eight actual producer DLL byte comparisons, three cache assets, every90 Passed outcome and both exact deployment DLLs, without findings.

| Bounded requirement | Exact test evidence |
|---|---|
| Same retained Experience changes locale without command replay or a publication/domain revision | `RetainedParcelReformatsLabelsAndCommittedResultWithoutPublishingOrRepeatingCommand` (both authoring fallbacks) |
| Explicit aliases and unknown-locale fallback remain coherent | `ExplicitLocaleAliasesAndUnknownLocaleFallbackApplyToTheWholeParcel` |
| Item translations are captured and external mutation cannot alter accepted names | `LocaleChangesUseTheCapturedItemTranslationsWithoutCallingTheNameAdapterAgain` |
| Rejected command retains its reason without another provider attempt | `RejectedCommandReformatsItsOriginalReasonWithoutAnotherProviderAttempt` |
| Old scenes keep their captured publication | `CapturedParcelScenesKeepTheirOwnCompletePublicationAfterALaterCommand` |
| Failed preparation retries the pending result and retirement stops remaining callbacks | `FailedLocalizedNameCaptureRetainsTheWholePublicationAndRawCommandResultForRetry`; `RetirementDuringLocalizedNameCaptureStopsTheRemainingPreparation` |

Commands used the explicit x64 SDK8.0.424 and .NET6 target/runtime. C/G retained audited restore with task-local `NUGET_HTTP_CACHE_PATH` and `RestoreForce=true`; P used the canonical isolated offline feed policy. Preparation used command-local `DOTNET_gcConcurrent=0`. The earlier ordinary-GC UI allocation failure belongs to U04-c and is retained there; this consumer checkpoint introduces no new frame-budget claim. Local alpha.43 package names identify this exact producer only and do not certify the future combined release after alpha.46. No ordinary Mods or real save was modified.

Next: implement meaningful current-session empty/faulted projections, then finish locale-explicit native item capture and the full native F12 environment/open/close/A→B→A acceptance. Native/visual is NOT_APPLICABLE to this host-free consumer prerequisite; no full U04 or F12 native result is inferred.

## F12-b: visible empty and faulted Parcel states

Source checkpoint [`8639b46400aa89a7bacf66b0178972ff19fb7f77`](https://github.com/ihatectf/Hatifect/commit/8639b46400aa89a7bacf66b0178972ff19fb7f77) on `feature/flow-parcel-states`, base published F12-a source6e75c5c/docs9b4169b. Full F12 remains **IN_PROGRESS**. Scope is FlowApplication lifecycle, two Flow.UI.Semantic files and two new Flow test files. Public signatures, domain/persistence state and formats, native host, UI framework, project metadata and version values remain unchanged. The observable application lifecycle now includes Faulted → Closed on owner disposal.

The defect was the use of one IsActive condition for both lifetime and data availability. Missing/faulted snapshots caused ParcelSurface to dispose before Refresh, and the formatter mapped both to Session closed. IsActive now retains these current-session views until Closed or explicit disposal. CurrentAllows independently requires Active plus a present parcel before inspecting availability, then preserves the live session/revision/notification fence. Text uses ParcelNotFound or the owner's fault reason; unavailable cargo/route remain blank. When an empty snapshot is also paused/recovering, availability retains the owning reason. No queue/cache, allocation or new collection walk is added to the guard/formatter paths.

| Evidence | Actual result |
|---|---|
| Initial test fixture build `red/run-lqz99fyq` | FAIL before tests: missing Dispatch namespace in new fixture, corrected without changing production |
| Actual regression `red/run-m2gh6tma` | 745 executed,741PASS/4FAIL: premature surface closure for EN/RU empty, disappearance and actual executor fault |
| Owning correction `green/run-2nqudkxb` | 745/745PASS, all four regressions closed |
| Expanded scoped `final/run-79a4p98k` | 747/747PASS, all six new cases including empty+pause/recovery and resume; every actual TRX row Passed, no skips |
| Reviewer owner-close regression `owner-close-red/run-k9rbd7ng` | 746PASS/1FAIL: faulted surface remains visible after owner.Dispose |
| Final scoped `owner-close-green/run-orkwjayl` | 749/749PASS, all eight ParcelState/Lifecycle cases, no skips |
| `./tools/hatifect-check` | `artifacts/validation/run-matposm6`:1544 .NET+365 Python PASS, seven actual TRX, all eight new cases Passed |
| `./tools/hatifect-check --platform` | `run-1ggprez5`:1817 .NET+365 Python PASS, ten actual TRX, Flow749/FlowStardew133/UIRuntime455, all eight new cases Passed |

Evidence is under `artifacts/f12/parcel-states/` in `/private/tmp/hatifect-flow-u04-environment`. `history.json` retains the compile failure and actual RED; `source-manifest.json` binds the five current postimages and `scoped-audit.json` binds the actual final TRX. The initial unsupported CLI option failed before build/tests and is not production regression evidence. Independent re-review closed the owner-lifetime P2, found no further actionable defect and checked the actual749 outcomes plus all five source hashes. The scoped and final C/G audits bind immutable Git8639b46 and every actual test outcome; every executed test count is nonzero and no test was skipped. Independent final C/G evidence review confirms the actual outcomes, every summary/TRX hash and all five immutable source postimages, without new findings. The initial three-file scoped result did not cover owner disposal after a fault.

| Bounded requirement | Exact test |
|---|---|
| Empty view stays visible with EN/RU reason, no name callbacks or commands, then closes on owner close | `EmptySessionRemainsVisibleWithLocalizedReasonUntilItsOwnerCloses` |
| Empty pause/recovery keeps the owner reason and resuming cannot enable a missing-parcel action | `EmptyParcelRetainsTheOwningPauseOrRecoveryReasonUntilTheOwnerResumes` |
| Disappearance clears current data; refill retains identities and old frames; disposal releases subscription | `MissingParcelClearsCurrentFactsAndRefillRetainsIdentityWithoutReplayingActions` |
| Actual executor fault remains visible and translates its original result without another command/report, then closes on owner disposal | `FaultedCommandRemainsVisibleUntilOwnerDisposalAndTranslatesWithoutAnotherAttempt` |
| Final owner close reaches current/late observers exactly once without reopening or replay | `FaultedApplicationPublishesClosedToCurrentAndLateObserversExactlyOnce` |
| Observer failure and reentrant disposal cannot suppress final close | `FaultObserverFailureAndReentrantDisposalCannotSuppressTheFinalOwnerClose` |

The surface fixture invokes the actual compositor/host through ExperienceTextProbe, but stands in for the native session. Real application owners cover empty and command fault; a same-session immutable snapshot fixture supplies disappearance/refill. Independent review found that retaining Faulted exposed an existing lifetime gap: FlowApplication.Dispose ignored an already faulted snapshot and fault handling had already dropped its observers. Strengthened owner-close regression `owner-close-red/run-k9rbd7ng` failed as expected (746PASS/1FAIL). The owning correction retains observers while faulted and allows Dispose to publish one final Closed revision before clearing them. Operations/Refresh/SetAvailability remain blocked after a fault, and the prior immutable fault snapshot is unchanged. Later commands return SessionClosed after owner disposal; no transport effect is replayed and this transition does not create recovery or reopen the domain session. `owner-close-green/run-orkwjayl` passes749/749, including six ParcelState plus two FlowApplicationLifecycle cases. The latter prove current/late observer notification, idempotence, stable identity, observer failure and reentrant disposal rejection. Existing native saving/title cleanup explicitly disposes surfaces, including after Faulted, and all prior cleanup/reentry/publication tests remain unchanged.

C/G used explicit x64 SDK8.0.424/.NET6, task-local audited restore with `NUGET_HTTP_CACHE_PATH` and `RestoreForce=true`, and command-local `DOTNET_gcConcurrent=0`. The source change adds no numerical frame-PERF claim.

Native/visual is NOT_APPLICABLE to this host-free source prerequisite. UI/package boundaries are unchanged; no new P result is claimed by this slice. F12-a's separately bound P90 remains its own package evidence. The current native show entry still rejects missing IDs before model construction; native opening, locale-explicit item names and the complete F12 environment/open/close/A→B→A matrix remain required. Next ready step is to implement that native opening path against the completed UI environment host.


## F12-c: opening without a selected Parcel

Source [`b06805d4a17a6530ff4fe618c2df835502f50b66`](https://github.com/ihatectf/Hatifect/commit/b06805d4a17a6530ff4fe618c2df835502f50b66), branch `feature/flow-parcel-opening`, base F12-b docs `5e234030`. Full F12 and this native opening acceptance remain **IN_PROGRESS**. The source prerequisite changes only the internal Parcel constructor/captured facts, the native `show` branch and one test file. Core/Persistence, public UI contracts, package/project metadata and version values are unchanged.

An omitted ID uses the remembered sent parcel, otherwise the first actual snapshot parcel, otherwise null. A remembered missing ID is retained; an explicit nonempty missing ID opens its diagnostic state. Explicit malformed/empty GUID rejects before retiring the old surface. Null is a stable no-selection state: EN/RU guidance, blank cargo/route, no name capture, no parcel search and no command admission or later automatic selection. Pause/recovery availability and fault/Closed reasons retain owner precedence. Five sources/actions keep their original identities; captured earlier scenes remain unchanged.

The native adapter constructs the Flow experience before retiring its prior surface. If prior cleanup fails, the already-retired old handle remains owned for retry and the candidate experience is disposed. Native surface creation/Show still follows old-surface retirement; there is no promise of full opening rollback after any native Show failure. Those native paths received source review, not execution through the host-free fixture. No new event queue/cache or per-frame collection walk is added; no numerical PERF claim is made.

| Requirement | Exact evidence |
|---|---|
| Empty initial no-selection view gives EN/RU guidance without names, cargo or effects; owner closure retires actions and old scene stays captured | `EmptySessionCanPresentAnUnselectedScreenWithoutInventingCargoOrRunningCommands` (two locales) |
| Later application revision cannot implicitly select a parcel or execute its command | `UnselectedViewNeverAdoptsAnUnrequestedParcelAcrossOwnerRevisions` |
| Explicit empty identity remains invalid before callbacks | `ExplicitEmptyIdentityStillRejectsBeforeNameCapture` |
| Paused/recovery owner reason survives missing selection, resumes to selection guidance, then shows external fault and final close | `UnselectedViewPreservesOwnerAvailabilityAndTerminalReasons` (both owner states) |

`./tools/hatifect-test flow --project 'Hatifect Flow/tests/Hatifect.Flow.Tests/Hatifect.Flow.Tests.csproj' --results-directory artifacts/f12/parcel-opening/scoped` passed **755/755**, including all six new cases, in `run-4fwp868l`. This feature adds a constructor option; no baseline failure against a nonexistent constructor is claimed as a behavioral RED. `./tools/hatifect-check` passed **1550 .NET +365 Python**, seven TRX, in `artifacts/validation/run-0qi13fxt`. `./tools/hatifect-check --platform` passed **1823 .NET +365 Python**, ten TRX, in `artifacts/validation/run-4gk7xio4`. Actual TRX counts/outcomes and all four postimages are bound to immutable Git b06805d in `artifacts/f12/parcel-opening/{source-manifest,scoped-audit,final-c-audit,final-g-audit}.json`. No failed/skipped/NotExecuted cases. Independent source/scoped review found no actionable defect and confirmed the native failure boundary above; independent final C/G evidence review verified the actual summaries/TRX, all six new cases and all four immutable source postimages without findings.

Commands use the existing explicit x64 SDK8.0.424/.NET6 target/runtime, command-local `DOTNET_gcConcurrent=0`, and audited C/G restore with task-local HTTP cache/RestoreForce. New P is not applicable to this bounded diff because UI producer/package/CA files are unchanged; F12-a P90 retains its own source attribution. Full F12 combined package acceptance still remains mandatory. **RUNTIME/VISUAL: BLOCKED on the pending combined environment-enabled UI candidate after alpha.46**; these native opening changes cannot be labelled runtime NOT_APPLICABLE or full F12 DONE. Native item capture, actual opening/replacement/fault rendering, input/scale and A→B→A retirement/subscription checks remain required. Continue with the locale-explicit native item adapter and a bounded canonical Flow UI driver while the combined candidate is prepared.


## F12-d: exact native item name capture

Source [`3810b1014eb1cc7d8a2d26fc21b43441fbb85cef`](https://github.com/ihatectf/Hatifect/commit/3810b1014eb1cc7d8a2d26fc21b43441fbb85cef) on `feature/flow-item-locales`, base F12-c docs `9ad7f0c`. The UI owner's exact `flow.ui.names` registration `a93c993` was adopted as [`d7047ac184c002746367cb8bed29be0bc60cfe2e`](https://github.com/ihatectf/Hatifect/commit/d7047ac184c002746367cb8bed29be0bc60cfe2e): only the UI acceptance bypass and fixed Flow report route. Companion-save work is excluded. This bounded prerequisite is verified; **full F12 remains IN_PROGRESS**.

The native Parcel callsite previously captured one ambient-language string. `FlowItemNames` now materializes an immutable `UiLocalizedText` during publication preparation. It recognizes a complete, parameter-free ordinary ObjectData `[LocalizedText asset:key]`, loads the English base asset and exact `.ru-RU` asset through the installed `LocalizedContentManager.LoadImpl` contract, selects `.desktop` before the base key and applies native preprocessing plus final plain-text escapes. It supplies four exact ordinal aliases: en/en-US and ru/ru-RU. Missing Russian content falls back to captured English; unavailable English does not discard valid Russian text. Unsupported literal, custom, nested or parameterized names retain the captured native fallback. This is ordinary item-type naming, not new per-instance wine/flavor translation or a general mod token parser.

The returned catalog retains no content manager/table and later formatting performs no game content read. Unexpected content failures still enter the existing publication retry path. Only exact EN/RU resolution bypasses the ambient localized map: an outer ItemRegistry cache miss may legitimately populate ordinary fallback caches. The adapter never assigns the current language or global maps. Core/Persistence, UI public signatures, formats, project/package metadata and version values are unchanged. The production update path adds one nullable acceptance-driver branch; name capture occurs during model preparation, and native diagnostic snapshots are limited to two captures. No new numerical frame-PERF claim is made.

| Verification | Actual result |
|---|---|
| `./tools/hatifect-test flow --platform --project 'Hatifect Flow/tests/Hatifect.Flow.Stardew.Tests/Hatifect.Flow.Stardew.Tests.csproj' --results-directory artifacts/f12/item-locales/native-driver-scoped` | `run-6kan4l9u`:152/152 Passed,19 new cases; before the subsequent driver-only custom-locale restoration correction |
| Final `./tools/hatifect-check` | `artifacts/validation/run-nfqije0k`:1550 .NET+367 Python PASS, seven actual TRX, exact committed source |
| Final `./tools/hatifect-check --platform` | `run-zgecf7y7`:1842 .NET+367 Python PASS, ten actual TRX, all19 native-name cases Passed, no skips |
| `./tools/hatifect-test tools` | `run-mhrm47qh`:367/367 Python PASS |
| `./tools/hatifect-live-prepare` | `run-ufo4n1p8`: isolated build/deployment PASS; its package assembly deliberately skips tests and is not RC test-completion evidence |
| `./tools/hatifect-smoke save.bootstrap` | `552bd5b0-a034-40df-b004-c41a889cc274`: PASS1/process exit0; generated a new compatible isolated fixture |
| `./tools/hatifect-smoke flow.ui.names` | `8915aae8-f838-4f6c-8d59-91a07d25834c`: PASS7/process exit0,37 driver frames,10.691 seconds |

The 19 game-reference cases exercise exact asset/language calls, `.desktop` and preprocessing, immutable retained catalogs, all four aliases, unknown/invariant fallback, missing tables/keys/blank values, unsupported tokens, unexpected failure propagation, plain escapes and Russian-only availability. Python tests require exact report ownership, exclusion from `all`, the one-copy cleanup after launch failure, and rejection when any individual one of the seven declared native checks fails. No old assertion or guard was weakened. `registration-history.json` retains the initial new-test fixture error (the normalized scenario omits `includeInAll`) and its correction to assert actual aggregate exclusion; the corrected targeted run passes3/3. Initial prepare rejected the configured `/private/tmp` root before deployment because it did not match shell TMPDIR policy. Configuration was corrected to the accepted worktree `.smapi-test/item-locales` root; no path guard or managed policy was changed.

The opt-in driver loads the request-owned single-player world, verifies its identity and unchanged files, and bypasses production transport/surface creation. Unexpected saving or title lifecycle fails acceptance. Driver-owned EN/RU setup happens between completed updates; after warming the ordinary ItemRegistry fallback, each real capture checks current language and the public game's `localizedAssetNames` contents. Both actual captures returned Copper Ore / Медная руда; public map counts were0/0. The first catalog remained stable across the second capture. All seven source checks passed: loaded, english, russian, capture-state, retained, restored, read-only.

Second-pass source inspection found that restoring only the language enum loses a custom-language descriptor: the installed game setter clears it when switching away from `mod`. The driver now captures enum, descriptor and uncached locale identity immediately before its first change, restores custom languages through public `SetModLanguage`, and verifies the same descriptor reference and locale string before releasing the restoration lease. Final G compiles this correction; independent source review closes it. The actual native run started in English (empty native asset suffix) and restored English. **A custom starting locale was not executed**, and the public-map check does not inspect SMAPI's private coordinator cache.

Evidence lives under `/private/tmp/hatifect-flow-u04-environment/artifacts/f12/item-locales/`. `source-manifest.json`, `final-c-audit.json` and `final-g-audit.json` bind all11 source postimages to immutable Git3810b10 and actual summary/TRX/Python outcomes. `native-audit.json` binds the actual request/repository SHA, report freshness, process71404/exit0, seven checks and diagnostics. Flow fingerprint is `fc26939c432e48c44807ef9e31582b4ff374223fe7f34c8112951f985bf84f65`; all four deployed Flow and eight deployed UI DLLs equal their actual Release producer outputs. The one copied save was removed; native read-only hash was `b9444679ae9612301be5f5d91d8ef141c1ebb66feea36b518a6d5db5ae30ed76`. Canonical fixture validation after cleanup retains checksum `e24ecd5f6492644f9506d2775e56562d831f26b962bca37206b77ad843505046`. Both temporary isolated option files were restored (removed because originally absent), without teardown errors. Game1.6.15 build24356 / SMAPI4.5.2 were the actual installed runtime.

C/G use the explicit x64 SDK8.0.424/.NET6, task-local audited restore and command-local `DOTNET_gcConcurrent=0`; native serve/game did not receive that override. Independent source/adapter/restoration/G reviews closed without remaining findings. Independent final C/native evidence review confirms all actual outcomes,11 immutable source postimages,15 runtime artifact hashes,12 producer/deployed DLL matches, the Flow fingerprint, option restoration and unchanged fixture. Public map0→0 does not establish behavior for a nonempty or private SMAPI map. New P is not applicable to this bounded registration/adapter diff: producer package/API and CA are unchanged; F12-a retains its own P90 attribution. This read-only names smoke does not render a Flow screen, inject input or switch worlds, so it cannot close F12 UI/visual acceptance or the combined release's package/runtime gate. Next: Flow's actual opening/update/empty/unavailable/faulted/close and A→B→A driver against the combined environment-enabled UI candidate and the owner's verified companion-save protocol.
