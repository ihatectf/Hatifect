# Flowline D03/D04: game-save coordinated chest provider

Date: 2026-09-06. Implements the provider decision required by F14; implementation and acceptance progress are tracked in ROADMAP-STATUS.md. Core/Persistence remain independent of Stardew/SMAPI and game item types.

## Supported boundary

The current production subset is one authoritative single-player session, stationary ordinary player Chest/Big Chest containers, and exact ordinary Stardew Object source stacks up to 999 units. A command selects 1..source quantity; the legacy command without a quantity selects the whole stack. XML serialization must roundtrip before admission and preserves quality, preserve type/parent and modData. Fridge, mini shipping bin, shared/special chests, held objects and third-party item subclasses are rejected by capability checks. No arbitrary modded-inventory or multiplayer write guarantee is claimed. Partial-stack unit evidence and fresh runtime acceptance are recorded separately below; neither closes the full process-crash matrix.

Station binding combines an opaque GUID in chest modData with saved location/tile and the known binding. Container replacement or a conflicting identity fails access. Explicit rebind requires an available destination and rejects relocating an origin that still owns queued active cargo. Snapshot metadata contains detached descriptions/IDs; game objects and item XML do not enter the public application projection.

## Commit timeline and custody

1. Admission takes the source inventory lease and verifies the selected complete item fingerprint. Detached XML clones validate both the selected quantity and reconstruction of the complete expected source. Only after the cargo/shipment/parcel are admitted does the physical source receive its cargo tag; its quantity remains unchanged. A tagging observer failure preserves the admitted evidence and recovery fence. Without a transfer receipt that fence cannot be automatically reconciled; restore a known-good complete game save.
2. The Core schedules a bounded operation and records its retained transfer intent. The save-bound port uses the transfer identity to retain Applied/Rejected results. Extraction rechecks the unique tagged source, its full original quantity and XML metadata, prepares an untagged remainder, then replaces exactly one source slot with that remainder or null. The selected quantity moves to parcel custody. A thrown callback is settled Applied only if the exact expected slot/content remains; changed contents retain uncertainty. Delivery and return materialize only the selected payload quantity into a free slot, without merging an unrelated stack.
3. Flow's payloads, station bindings, checkpoint, journal and recovery fence are written under SMAPI `flowline-v1` during Saving. Physical inventories are serialized by the same game save. BeginSave prevents new host mutations; Saved resumes. A new session token fences stale UI on reload/title.
4. A complete game-save rollback restores both the physical inventories and their Flow state. Reload never reconstructs a historical Delivered/Returned physical item from checkpoint payloads. A player taking delivery therefore cannot cause its recreation by reloading that saved state.
5. If the physical inventory callback makes the result ambiguous, the retained intent/payload remain and transport is fenced for recovery. Automatic replay is forbidden. Only a retained settled Applied/Rejected receipt can authorize reconciliation; Missing does not authorize a guess based on an empty slot.

Game-save aggregate version 2 adds `CargoPayload.SourceQuantity`; selected `Xml` plus that quantity must reconstruct the complete expected source. A v1 aggregate without this field migrates explicitly as whole-stack cargo from its validated manifest, and the next save writes v2. Missing/out-of-range v2 source quantity is rejected before inventory access. The SMAPI save key remains `flowline-v1`; the aggregate version is independent of that stable key. Older code rejects v2 rather than interpreting partial custody as a whole stack. Core checkpoint and save-bound receipt formats do not change.

The chosen boundary is the complete game save. It is not an independent durable commit spanning an arbitrary external database and game inventory. Replacing only one side of the saved aggregate, editing saves or arbitrary external observers that mutate/throw mid-effect are outside the automatic-recovery guarantee; ambiguity is preserved and stopped, not repaired by creating items. Restoring a known-good complete save is the user-visible fallback when no settled result exists.

## Failure matrix and actual evidence

| Boundary | Required behavior | Evidence currently available |
|---|---|---|
| Stale slot before/while lease acquisition | Conflict before cargo tagging/admission | ShipmentAuthoringTests: quantity/metadata/replacement and ItemChangedWhileAcquiringMutexRejectsOldFingerprintUnderTheLease |
| Queued operation waits for mutex | Keep reservation, intent and custody; do not consume delivery attempt | PortReadinessTests + InventoryLockTests; late grant never owns cargo authority |
| Source changes after admission | Reject extraction/cancel queued parcel, retain actual source quantity | FlowGameSessionTests.ChangedOrAlreadyAssignedSource_CannotLoseOrDuplicateThePhysicalStack |
| Destination disappears/replaced/full | Retain parcel custody and bounded retry; no write to replacement identity | FlowGameSessionTests and ChestInventoryAccessTests |
| Physical callback throws after mutation | Persist uncertainty and recovery fence; do not replay automatically | AmbiguousInventoryCallback_IsFencedAcrossSaveReload and RecoveryGameSessionTests |
| In-flight whole-game save/reload | Resume sole parcel custody and deliver once with exact item fidelity | Unit roundtrip plus actual SMAPI request c64b077c-986a-450e-87a8-8e57a7d0c9f6 on prior candidate |
| Delivery taken before save | Historical checkpoint never recreates taken item | TakenDelivery_IsNeverRecreatedFromHistoricalCheckpoint |
| Whole-save rollback before delivery | Restore source and queue together, then deliver once | RollingBackWholeGameSave_RestoresSourceAndQueuedJourneyTogether |
| Partial stack and subsequent remainder shipment | Conserve quantity and separate custody across reject/cancel/return/reload | PartialStackTests: queued/in-flight/delivered reload, stale source, cancellation, full destination/return source, exact post-write proof, v1 migration and invalid v2. Actual whole+partial roundtrip d60b0e0c-6d5f-4560-b07c-37bf28b1c906 PASS8 |
| Process crash after confirmed InTransit save | Reload the same saved custody in a distinct process, then deliver whole/partial cargo once | Actual SIGKILL/restart request 628c1799-df43-4c51-8b82-b8d6c4432979 PASS9; marker and raw -9 evidence recorded separately from unit fault injection |
| Process crash after confirmed Delivered save | Retain physical whole/partial cargo and historical delivery; reject repeat materialization | Actual request db552b12-1f5a-4313-a3c8-95fe71728eae PASS9 with second-Saved marker, owned raw-9, new process and 120 verification ticks |
| Process crash after unsaved extraction from a Reserved save | Restore full tagged source stacks and Reserved queue together, then deliver once | Actual request 6c8b70e3-20f9-4f63-9301-1d1c76f177a3 PASS10; unchanged confirmed hash, owned raw-9, new process, full source XML8/13 plus CargoId and explicit unsaved-rollback check |
| Remaining process-crash boundaries | No mismatch within the supported complete-game-save recovery model | Other confirmed states and interrupted/unsaved effects still require their own matrix evidence |
| Multiplayer contention / arbitrary external inventory | No production writes under an unproven authority contract | Explicitly disabled in ModEntry; exploratory peer code is not an advertised capability |

F16-a source gates: `./tools/hatifect-check --platform` run-o7zrqs5e PASS1,107 .NET +297 Python (659 Flow +92 Stardew), and `./tools/hatifect-check` run-ogcutlsc PASS929 .NET +297 Python. Fresh isolated deployment run-lj6ytpwy; actual `flow.chest.roundtrip` request d60b0e0c-6d5f-4560-b07c-37bf28b1c906 PASS8, 347 frames, 3 loads, 2 Saving/Saved pairs and process exit0. Whole8 and partial13→5+8 retained total21 and item fidelity. Flow runtime fingerprint: `4b56cbc49bfcdfae5332e67e0db9818aa0707a64fdb4dafc177603a843d356c9`. This is normal lifecycle evidence, not a process-crash/restart result; the crash matrix and production A→B→A remain open.

F16-b adds one actual process-crash boundary on the combined UI alpha.31 candidate. Final G `run-t3t1h105` PASS1,203 .NET +315 Python; C `run-pht4qkv7` PASS1,016 .NET +315 Python. Fresh deployment `run-uwtjbc_f`; `flow.chest.crash-after-save` request `628c1799-df43-4c51-8b82-b8d6c4432979` PASS9. PID68184 emitted its bounded Saved/InTransit marker, the executor observed SIGKILL/raw-9, and PID68206 resumed the same canonical save and exited0 with no teardown errors. The marker's save hash was rechecked after death and before the new load. Across both processes: 3 loads, 2 real Saving/Saved pairs, whole8 plus partial13→5+8, no duplicate delivery. Fingerprint: `67df64afd3e51c398bb4ba8fb59708d3e55c6305c565289909e8028e23c36243`. This evidence does not certify crash-during-write, unsaved effect rollback, other confirmed custody states or production A→B→A.

F16-c adds the second fixed boundary, `flow.chest.crash-after-delivery`: marker `saved-delivered` requires exactly two loads and two Saving/Saved pairs before SIGKILL. Fresh deployment `run-3o3qkopf`; actual request `db552b12-1f5a-4313-a3c8-95fe71728eae` PASS9, PID70870 raw-9 → PID71073 exit0. The third load retained both Delivered parcels, destination quantities8+5 and source remainder8, then rejected repeat delivery. Same-candidate InTransit crash `e6e12955-dbb0-4df8-95a4-1a863c4096b9` also PASS9. Both reports use fingerprint `f7fc744de7f9b6004f5fea897e71221d71923600c5c7e07cb22ee180b4408bca`; final G `run-xetndbxq` PASS1,203 .NET +317 Python, C `run-yw5xa814` PASS1,016 .NET +317 Python. The remaining matrix includes unsaved effects, interrupted writes, other confirmed custody states and production A→B→A.

F16-d adds `flow.chest.crash-after-unsaved-extraction`. The initial SaveLoaded freezes the production session before its first transport tick; both real Saving/Saved events must retain Reserved parcels, full tagged source XML8/13 and an empty destination. After unsaved extraction, marker `saved-reserved-unsaved-extraction` requires the unchanged confirmed save hash and counters1/1/1. Actual request `6c8b70e3-20f9-4f63-9301-1d1c76f177a3` PASS10: PID76164 raw-9 → PID76269 exit0, restored Reserved/source21, then single whole8/partial5 delivery with remainder8. Final3 loads/2 Saving/Saved pairs/341 resume frames; no errors. Same-candidate InTransit regression `afa6d7fb-6893-41b9-aca6-6087b8052d0a` PASS9. Fingerprint `2d2b18916f389f42f3ae2e150185d1c8eefd27dac63e2e56021b631a9e09d07b`; deployment `run-dcakgrid`, G `run-84z67xfh` PASS1,203 .NET +319 Python, C `run-g3xbx1ju` PASS1,016 .NET +319 Python. This proves complete-save rollback of unsaved extraction; unsaved delivery, interrupted writes, other custody boundaries and production A→B→A remain separate acceptance.
