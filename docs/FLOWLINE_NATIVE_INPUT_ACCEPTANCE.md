# Flowline «Первая игровая альфа» — ordinary entry and native input acceptance

Status: prepared operator/runner sequence, NOT_RUN. This supplements `flow.ui.player`; it does not relabel that prepared-form scenario as ordinary entry. GQ owns the fixed combined candidate and runtime slot. UI owns focus, geometry, text routing and the adapter for existing native-input diagnostics. No new public input API is required by this plan.

## Evidence and start conditions

Use one canonical request-owned working save, matching DLL/package/source fingerprints, with the normal Flow entry enabled. Provision two reachable ordinary supported chests in the isolated world near the player, without registering stations or creating routes. The source contains the existing roundtrip fixture: marked quality4 wine stacks8 and13; destination empty. Record their exact locations, original item XML and total21 during Update. Confirm zero Flow stations/links/parcels. Fixture placement and player positioning are preparation, never input evidence. Do not modify golden files, ordinary Mods or real user saves.

Start only when an operator or an explicit OS-input controller is ready. Record input origin per segment: `human-physical`, `os-injected`, or `normalized`. SMAPI events alone cannot distinguish the first two. Normalized API actions are forbidden as a substitute inside a segment claimed as OS/human. If an input source changes, end the segment and record a new baseline.

Reuse the evidence format and bounded principles of `semantic.input.native` protocol1 (Ready/Pointer/Keyboard/Text/Backspace/Complete): foreground, a bounded wait of18000ticks per phase, new MouseLeft press/release/Tab/Back counters after a baseline, exact request-derived text, and two stable fresh completed frames. No-event timeout is FAIL with the stopped phase and counters, never PASS on clean process exit. The earlier zero-event native-input run is not reusable as successful evidence.

The current `UiNativeInputGate` cannot run unchanged on Flow: its Ready phase requires no text fields, its pointer phase opens Inspector, and its producer observes only `_dogfood.AutomationMenu` and the first TextField. Ordinary Flow Window already contains fields and is deliberately bypassed by that UI controller. UI owns the next bounded observer/phase adapter for a real ordinary Window; a legacy gate PASS must not be claimed before that exists.

The existing public surface snapshot exposes semantic/action IDs, role/name/value/enabled and scene/render identity. It does NOT expose FocusedId, TextFieldFocused or bounds. Those native gate fields must come from the UI-owned inspection adapter; Flow must not invent them, cast to private UI objects or calculate layout. Until that adapter is ready, composed-frame focus review is explicitly manual evidence, not an automated native-gate PASS.

Flow's supplement correlates each gesture to one surface instance, Flow session/revision, command result and exact inventory/custody delta. Capture domain/inventory evidence during Update; Draw may capture already prepared immutable observations/pixels only. The public observation must match the committed publication and a later completed render. Preserve composed and UI-layer PNG hashes. Readonly observation does not prove unclipped pixels; inspect the actual composed capture.

## Concrete run card

All semantic suffixes below are under `Hatifect.Flow/network/`. Obtain the actual formatted IDs from accepted observations; do not infer private runtime node IDs. Labels are the current EN/RU consumer labels. Text fields may be reached with a real pointer gesture or Tab; do not write their semantic sources from this input run.

| Step | Real gesture and target | Required postcondition and evidence |
| --- | --- | --- |
| 1. Busy menu guard | Open a normal game menu, then press the configured Flow binding (default K). | Existing native menu remains; no Flow Window, target capability, station or parcel is created. Record input event and admission context. Close the native menu normally. |
| 2. Ordinary entry | With free player and no menu, aim at source chest and press/release K. | Exactly one Window from the production ButtonsChanged path; captured chest matches actual source coordinates, provider GameInventory, no console command or acceptance factory call. A held/repeated binding must not create another Window. |
| 3. Pointer and text | Click `field/name` (Station name / Имя станции), type `native-` + first8 request hex characters. | New pointer press/release, exact focused field and exact rendered probe on two fresh frames; stations still0. |
| 4. Backspace | Press Backspace once with the same field focused. | New SButton.Back event removes exactly the final probe character; no other field or station changes. Clear remaining probe with bounded Backspace presses, then type source name `src_` + first6 request hex characters. |
| 5. Tab and first registration | Use Tab to reach `action/register` (Bind new station / Создать станцию), then Enter. | Focus moves to the visible enabled action through UI routing; exactly one station bound to source, localized Result, fresh composed frame. A repeat Enter/click adds no station or side effect. |
| 6. Close and second target | Escape closes Window; aim at destination chest and press K. | Prior handle is retired with no retained authority; new instance/session binding captures destination. Type distinct `dst_...`, Tab/Enter or click Register. Exactly two station tags, correct coordinates, no parcels. |
| 7. Empty target and reopen | Close, aim at an empty tile, press K. | Previous captured target is cleared; both existing stations remain. Register is unavailable until a supported current target is selected. No old capability can be reused. |
| 8. Select stations and route | Select source and destination using real controls `element/source-station` and `element/destination-station`. Edit `field/capacity` to999 and `field/ticks` to180. Activate `action/link`. | One directed link with exact endpoints/settings, route preview available, localized Result. Observe selection and form values; do not use fixture TrySelect or source.Value writes. |
| 9. Whole-stack action | Select the8-unit slot in `element/source-cargo`; activate `action/send` (Send whole stack / Отправить весь стек). | Exactly one Reserved8 parcel/cargo; physical source remains21 before extraction, destination0, no physical Apply call yet. Record full IDs/custody and visible localized Shipment created result. Repeat action adds no new cargo/operation. |
| 10. Selected quantity | Select the13-unit slot, inspect default `field/quantity` value1; enter5 and activate `action/send-quantity` (Send selected quantity / Отправить указанное количество). | Second Reserved5 parcel; original whole parcel unchanged, physical total21. Exact result publication is later than the second action even though the displayed success text is the same. |
| 11. Tab navigation regression | Traverse visible fields, lists and actions with Tab and Shift+Tab, including scrolled content. | Focused target is visible and reachable in both directions, no swallowed text-field transition, no implicit submit. WindowContained boundary need not wrap: at its end focus may remain and navigation is unconsumed. Never require wrapping contrary to owning policy. Preserve focused IDs from UI diagnostics and composed frames. |
| 12. Delivery and reopen | Escape, allow normal game ticks, then K to reopen. Select the partial shipment in `element/history`. | Same parcel/cargo IDs Delivered, destination stacks8 and5, source remainder8, total21, original quality/preserve/marker/XML fidelity. Visible localized history-detail result on a fresh Window instance. No manual session.Tick call is input-path proof. |
| 13. Search and Backspace regression | Click `element/history-search` (Find shipments / Поиск отправлений). Type the partial parcel ID's first8hex characters, then append `x`, then Backspace once. | Exact focused query renders; result count1→0→1 with no domain/inventory mutation. Backspace affects the focused query, not station name or quantity. Clear the query by bounded Backspace presses; both shipments reappear. Tab away and Shift+Tab back preserves value and does not submit shipment commands. |
| 14. Save/restart | Close normally. Execute canonical real Saving/Saved, full process restart, then normal K entry. | Exact candidate DLLs, source/destination XML and parcel/cargo identities preserved; no redispatch of Delivered parcels and no changed input settings written by the harness. Keep the original in-transit/crash boundary cases as separately required native runs. |
| 15. Save switch and input profile | Switch A→B→A through the canonical isolated lifecycle. Repeat entry with configured controller shoulder chord in its separate declared input segment. | No cross-save target/window/actions/subscriptions; controller admission is a real chord event, not a selected controller profile. All captured settings restored exactly. Old handles/commands cannot affect the current world. |

Use a nonempty unsupported fixture only in the explicit target-rejection subcase, preserving its original items: aiming at it must not revive the previous supported chest capability. Recovery mode may be inspected; it must not recapture a target or resume unknown physical work. Existing managed target/recovery tests support these invariants but do not replace this native input path.

## Failure and recovery card

Retain original «Управление диагностической перевозкой» create/dispatch/cancel/retry/refusal/stale/retired criteria. In real inventories, exercise cancel before extraction, full destination→DeliveryRejected→free space→permitted Retry, and permitted Return with full-source refusal/retry. Preserve full item XML and exact cargo/parcel/receipt identity across retries. Unknown outcome keeps the recovery fence and must not replay; only known receipt reconciliation is permitted. Failures expected by the fixture are distinguished from unexpected host/Pump/input errors. An unexpected error forces terminal FAIL even if a later frame renders correctly.

For stale/repeated input, record both actual native gestures and resulting command/effect counts. A direct supplemental stale-command call may prove the owner layer, but cannot be labelled as the physical gesture itself. A native text-input change to a focused field is legitimate even when the global K entry guard rejects reopening; target/Window/domain invariants are the guard criteria.

## Original matrix and completion

Keep EN/RU,75/100/125/150%, keyboard/pointer and controller coverage, real save/restart/switching and CA coexistence on the fixed combined candidate. Record a row for each original required case, with request/DLL identity, input origin, screenshots, domain/inventory deltas and verdict. Do not reduce the original matrix to this first EN100 run or to profile-only controller captures. Every remaining original row stays OPEN until matching evidence exists.

`flow.ui.player` first smoke proves only prepared real Window actions plus the existing real-chest save/reload scenario. This run card covers the ordinary trigger/forms/search/input gap. It is not currently an implemented new scenario ID, a new public API, or an assertion that a human operator is present. Before its run, attach the existing native-input protocol to the ordinary Window through the UI owner and add the bounded Flow command/inventory supplement, then use the already authorized canonical harness. GQ owns final acceptance of the first alpha.

Execution coordination update: GQ has confirmed a successful CUA surface inventory and will own interaction with the real game window after the Reveal correction. The initial pointer/keyboard segment therefore declares `os-injected`; no human-input or controller-chord evidence follows from this capability check. UI event/frame observation and the Flow command journal remain required before the scenario runs. This does not change any original matrix row or count as a completed input run.
