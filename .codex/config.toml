# Shared Hatifect settings. Model, reasoning, credentials, MCP and permissions
# are inherited from the user's installation and managed policy.
project_doc_max_bytes = 32768

[agents]
max_concurrent_threads_per_session = 3
